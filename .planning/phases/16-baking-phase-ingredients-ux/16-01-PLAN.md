---
phase: 16-baking-phase-ingredients-ux
plan: 01
type: execute
wave: 1
depends_on: [15-03]
files_modified:
  - Levain/Models/FormulaStepTemplate.swift
  - Levain/Models/BakeStep.swift
  - scripts/format_content.py
  - Levain/Resources/system_formulas.json
  - Levain/Features/Bakes/BakeStepDetailView.swift
autonomous: true
user_setup: []
must_haves:
  truths:
    - Baking phases in the modal must display only ingredients associated with that specific step.
    - Ingredient data must be automatically derived from the formula content via scripts.
    - UI must remain consistent with the existing Design System and BakeIngredientsView styling.
---

<objective>
Enable step-specific ingredient visibility within the baking phase modal to prevent users from needing to navigate back to the recipe view.
</objective>

<context>
@.planning/phases/16-baking-phase-ingredients-ux/16-CONTEXT.md
@Levain/Models/FormulaStepTemplate.swift
@Levain/Models/BakeStep.swift
@scripts/format_content.py
@Levain/Features/Bakes/BakeStepDetailView.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Model Schema Updates</name>
  <files>Levain/Models/FormulaStepTemplate.swift, Levain/Models/BakeStep.swift</files>
  <action>
    Add `ingredientsPayload: Data?` to both `FormulaStepTemplate` and `BakeStep` models to store step-specific ingredients. Provide computed properties for [String] access.
  </action>
  <verify>Ensure models compile and internal initialization correctly handles the new field.</verify>
  <done>Models are capable of storing and retrieving step-level ingredients.</done>
</task>

<task type="auto">
  <name>Task 2: Content Formatter Enhancement</name>
  <files>scripts/format_content.py, Levain/Resources/system_formulas.json</files>
  <action>
    Update the Python formatter to parse step-specific ingredients from formula Markdown. Re-generate `system_formulas.json`.
  </action>
  <verify>Check `system_formulas.json` to ensure "Impasto" and other relevant steps now contain ingredient arrays.</verify>
  <done>System formulas include the necessary metadata for step-specific ingredients.</done>
</task>

<task type="auto">
  <name>Task 3: Phase Modal UI Update</name>
  <files>Levain/Features/Bakes/BakeStepDetailView.swift</files>
  <action>
    Introduce a new section in the `BakeStepDetailView` modal that displays ingredients if available. Reuse the styling from `BakeIngredientsView`.
  </action>
  <verify>Open a phase modal in the app (e.g., Impasto) and verify ingredients and weights are visible.</verify>
  <done>Users can see what they need to add/do without leaving the phase modal.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `system_formulas.json` contains step-level ingredients
- [ ] `BakeStepDetailView` displays ingredients correctly
- [ ] No regressions in general bake execution flow
</verification>

<success_criteria>
- Step-specific ingredients are visible in the phase modal.
- Content pipeline is fully automated for new formula additions.
</success_criteria>
