# Phase 1 Context

Phase 1 establishes the real native project shell: repository setup, XcodeGen manifest, SwiftData container, shared environment objects, four-tab navigation, and design-system primitives. The phase must produce a runnable iPhone app with seed data support and no dependency on backend infrastructure.

