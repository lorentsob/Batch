---
phase: 23-guides-glossary-term-linking
plan: 01
type: execute
wave: 1
depends_on: ["21-03"]
files_modified:
  - Levain/Models/KnowledgeItem.swift
  - Levain/Services/KnowledgeLoader.swift
  - Levain/Resources/knowledge.json
  - Levain/Services/KnowledgeGlossaryIndex.swift
  - Levain/Features/Shared/GlossaryLinkedText.swift
  - LevainTests/KnowledgeLoaderTests.swift
  - LevainTests/KnowledgeLibraryTests.swift
autonomous: true
user_setup: []
must_haves:
  truths:
    - One concept must resolve to one canonical Knowledge article even when recipes or guides use different Italian/English aliases.
    - Glossary lookup must be centralized and reusable instead of hidden inside individual SwiftUI views.
    - The linked-text primitive must remain visually secondary and safe to reuse across read-only recipe and guide surfaces.
  artifacts:
    - Levain/Services/KnowledgeGlossaryIndex.swift
    - Levain/Features/Shared/GlossaryLinkedText.swift
    - Levain/Resources/knowledge.json
  key_links:
    - Knowledge schema changes and glossary lookup must preserve existing Knowledge loading and search behavior while adding alias resolution.
    - The new linked-text primitive must be driven by the glossary index so later screens reuse the same term policy.
---

<objective>
Establish the canonical terminology and rendering foundation for glossary linking.

Purpose: Phase 23 cannot safely wire inline links until the app knows which article each term should open and how those terms should render consistently across screens.
Output: Alias-aware knowledge data, a centralized glossary resolver, a reusable linked-text component, and tests that lock the indexing behavior down.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-guides-glossary-term-linking/23-CONTEXT.md
@.planning/phases/23-guides-glossary-term-linking/DISCOVERY.md
@Levain/Models/KnowledgeItem.swift
@Levain/Services/KnowledgeLoader.swift
@Levain/Resources/knowledge.json
@LevainTests/KnowledgeLoaderTests.swift
@LevainTests/KnowledgeLibraryTests.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend bundled Knowledge data for canonical terms and aliases</name>
  <files>Levain/Models/KnowledgeItem.swift, Levain/Resources/knowledge.json, Levain/Services/KnowledgeLoader.swift</files>
  <action>Add the minimum schema needed to support canonical glossary entries and alias/search-term expansion while preserving deterministic bundled JSON loading. Encode the term policy approved in Phase 23 discovery instead of leaving synonym handling implicit in prose.</action>
  <verify>xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived -destination 'platform=iOS Simulator,name=iPhone 15 Pro,OS=26.4' test -only-testing:LevainTests/KnowledgeLoaderTests</verify>
  <done>Bundled Knowledge content can decode and expose canonical terminology plus aliases without breaking the current local-content flow.</done>
</task>

<task type="auto">
  <name>Task 2: Create one glossary resolver and one linked-text primitive</name>
  <files>Levain/Services/KnowledgeGlossaryIndex.swift, Levain/Features/Shared/GlossaryLinkedText.swift, Levain/Services/KnowledgeLoader.swift</files>
  <action>Implement a shared term-resolution service and a reusable read-only text renderer for tappable glossary terms. Keep the renderer subtle, cap repeated links, and avoid embedding per-screen string-matching heuristics.</action>
  <verify>xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived CODE_SIGNING_ALLOWED=NO build</verify>
  <done>The codebase has one reusable glossary-resolution path and one SwiftUI component ready to embed in recipe and guide surfaces.</done>
</task>

<task type="auto">
  <name>Task 3: Lock alias behavior down with targeted knowledge tests</name>
  <files>LevainTests/KnowledgeLoaderTests.swift, LevainTests/KnowledgeLibraryTests.swift</files>
  <action>Add tests for canonical entry lookup, alias resolution, and no-regression Knowledge loading so later UI integration work cannot silently drift the term map.</action>
  <verify>xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived -destination 'platform=iOS Simulator,name=iPhone 15 Pro,OS=26.4' test -only-testing:LevainTests/KnowledgeLibraryTests</verify>
  <done>Alias-aware term lookup is covered by tests and existing Knowledge behavior still loads locally and deterministically.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived CODE_SIGNING_ALLOWED=NO build`
- [ ] `xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived -destination 'platform=iOS Simulator,name=iPhone 15 Pro,OS=26.4' test -only-testing:LevainTests/KnowledgeLoaderTests`
- [ ] `xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived -destination 'platform=iOS Simulator,name=iPhone 15 Pro,OS=26.4' test -only-testing:LevainTests/KnowledgeLibraryTests`
- [ ] Manual code review confirms the glossary model expresses one canonical article per concept rather than duplicating synonyms
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Canonical terminology and aliases are represented once and reusable by later UI plans

</success_criteria>

<output>
After completion, create `.planning/phases/23-guides-glossary-term-linking/23-01-SUMMARY.md`
</output>
