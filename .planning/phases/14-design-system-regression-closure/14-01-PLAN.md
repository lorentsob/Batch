---
phase: 14-design-system-regression-closure
plan: 01
type: execute
wave: 1
depends_on: [13-03]
files_modified:
  - Levain/App/LevainApp.swift
  - Levain/Features/Shared/RootTabView.swift
  - Levain/Features/Bakes/BakeCreationView.swift
  - Levain/Features/Bakes/BakeDetailView.swift
  - Levain/Features/Bakes/BakeStepCardView.swift
  - Levain/DesignSystem/Components/DesignPrimitives.swift
  - Levain/DesignSystem/Components/StateBadge.swift
  - Levain/Services/BakeReminderPlanner.swift
autonomous: true
user_setup: []
must_haves:
  truths:
    - Dark mode must not change the app chrome or modal background.
    - Cancel/delete bake confirmation must appear in the correct bottom position and stay inside the design language.
    - Cancelling a bake must make the detail screen read as terminal, not still actionable.
    - Timeline rows and overdue chips must remain visually legible after the design-system rollout.
---

<objective>
Close the post-design-system visual regressions in bake creation, navigation chrome, destructive bake flows, and bake-detail timeline rendering.

Purpose: restore visual trust without opening new product scope.
Output: light-only chrome enforcement, bottom destructive confirmation sheet, cancelled-state restyling, overdue chip border, centered timeline rail.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@docs/DESIGN-SYSTEM.md
@Levain/App/LevainApp.swift
@Levain/Features/Shared/RootTabView.swift
@Levain/Features/Bakes/BakeCreationView.swift
@Levain/Features/Bakes/BakeDetailView.swift
@Levain/Features/Bakes/BakeStepCardView.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enforce light-only surfaces and chrome</name>
  <files>Levain/App/LevainApp.swift, Levain/Features/Shared/RootTabView.swift, Levain/Features/Bakes/BakeCreationView.swift</files>
  <action>
    Force the app into light appearance at the root, ensure tab/navigation bars declare a light toolbar color scheme, and keep the bake-creation modal background pinned to the design-system app surface instead of inheriting a dark system material.
  </action>
  <verify>Build the app and inspect the bake-creation sheet plus tab/navigation chrome in dark iOS appearance.</verify>
  <done>Modal and navigator stay light, with no dark-system background leakage.</done>
</task>

<task type="auto">
  <name>Task 2: Fix destructive bake confirmation flow</name>
  <files>Levain/Features/Bakes/BakeDetailView.swift, Levain/Services/BakeReminderPlanner.swift</files>
  <action>
    Replace the misplaced system confirmation dialog with a bottom-anchored custom destructive sheet. Ensure cancel/delete update notifications correctly and deletion returns the user to the bake list without leaving stale detail state on screen.
  </action>
  <verify>Trigger cancel and delete flows; confirm the sheet appears bottom-aligned and reminder planning removes cancelled/deleted bake notifications.</verify>
  <done>Destructive flow is visually correct and terminal bake actions no longer leave stale reminders or stale navigation state.</done>
</task>

<task type="auto">
  <name>Task 3: Rework cancelled and overdue bake-detail visuals</name>
  <files>Levain/Features/Bakes/BakeDetailView.swift, Levain/Features/Bakes/BakeStepCardView.swift, Levain/DesignSystem/Components/DesignPrimitives.swift, Levain/DesignSystem/Components/StateBadge.swift</files>
  <action>
    Make cancelled bake headers read as red terminal cards, archive remaining future steps visually, remove still-active contextual guidance from cancelled bakes, add bordered danger chips, and center the timeline dot on its rail with left-aligned time metadata.
  </action>
  <verify>Inspect cancelled bake detail and overdue timeline rows against the screenshots and the design-system semantic rules.</verify>
  <done>Cancelled bakes read as terminal, overdue chips stay grounded, and the timeline rail no longer has top-floating dots.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] App build passes
- [ ] Light-only chrome applied to modal + navigation/tab bars
- [ ] Destructive bake sheet is bottom-aligned and custom-styled
- [ ] Cancelled bake detail reads as terminal / archived
</verification>

<success_criteria>

- No dark-mode leakage into app chrome or bake-creation sheet
- Delete/cancel bake confirmation appears in the correct position
- Cancelled bake detail is unmistakably terminal
- Timeline rows and overdue chips feel visually stable again

</success_criteria>
