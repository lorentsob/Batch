---
phase: 17-shell-preparations-hubs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Levain/App/AppRouter.swift
  - Levain/Features/Shared/RootTabView.swift
  - Levain/Services/NotificationService.swift
autonomous: true
user_setup: []
must_haves:
  truths:
    - The app shell exposes exactly three top-level tabs: Oggi, Preparazioni, Knowledge.
    - Knowledge is reachable as a dedicated tab without breaking existing article navigation.
    - Existing bake and starter deep links still select the correct top-level tab and open the underlying detail directly after migration.
  artifacts:
    - RootTab and AppRouter paths represent the v2 shell instead of direct bakes/starter top-level tabs.
    - RootTabView composes the new tabs using current Theme tokens and navigation stacks.
  key_links:
    - NotificationService pending URLs and AppRouter deep links resolve through the migrated shell without forcing Preparazioni traversal.
---

<objective>
Migrate the root shell from the v1 tab layout to the v2 layout and prepare router state for Preparazioni.

Purpose: Open the milestone with the smallest possible structural change while keeping existing bread/starter entry points intact.
Output: Updated top-level tabs, router enums/paths, and launch/deep-link behavior aligned to the new shell and direct-object routing rule.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@Levain/App/AppRouter.swift
@Levain/Features/Shared/RootTabView.swift
@Levain/Services/NotificationService.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the root tab model without breaking existing bread callers</name>
  <files>Levain/App/AppRouter.swift</files>
  <action>Refactor `RootTab` and router-owned navigation state around the v2 shell. Keep helper methods such as `openBake`, `openStarter`, and `openFormula` stable so current bread and starter features compile unchanged while the top-level selection moves to `Oggi`, `Preparazioni`, and `Knowledge`.</action>
  <verify>Update and review `AppRouterTests` expectations for tab selection and deep-link resolution after the enum/path migration.</verify>
  <done>Router-facing APIs still open the correct domain objects, but the selected top-level destination now targets the v2 shell.</done>
</task>

<task type="auto">
  <name>Task 2: Recompose RootTabView into the v2 three-tab shell</name>
  <files>Levain/Features/Shared/RootTabView.swift</files>
  <action>Promote `Knowledge` from sheet to full tab, introduce a Preparazioni tab placeholder entry point, and preserve the existing toolbar, tab-bar, banner, and background styling by reusing the current `Theme` tokens and navigation-stack patterns instead of inventing a parallel shell implementation.</action>
  <verify>Static review confirms banner handling, tab tint, toolbar backgrounds, and `onOpenURL` / launch bootstrap behavior still flow through one root view.</verify>
  <done>The app renders three top-level tabs with the existing visual system and no duplicate shell logic.</done>
</task>

<task type="auto">
  <name>Task 3: Preserve launch and notification routing through one entry path</name>
  <files>Levain/App/AppRouter.swift, Levain/Features/Shared/RootTabView.swift, Levain/Services/NotificationService.swift</files>
  <action>Keep `pendingURL`, `onOpenURL`, and direct router helpers converging on one route-opening path after the shell migration. Preserve the addendum rule that operational taps and deep links go straight to the underlying object detail instead of requiring Preparazioni → hub traversal.</action>
  <verify>`NotificationRouteUITests` and `LifecycleUITests` still pass with the new shell selected-tab behavior.</verify>
  <done>Local-notification entry remains safe and deterministic after the shell migration.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived CODE_SIGNING_ALLOWED=NO build`
- [ ] `xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived -destination 'platform=iOS Simulator,name=iPhone 17 Pro' test -only-testing:LevainTests/AppRouterTests`
- [ ] `xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived -destination 'platform=iOS Simulator,name=iPhone 17 Pro' test -only-testing:LevainUITests/NotificationRouteUITests`
- [ ] `xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived -destination 'platform=iOS Simulator,name=iPhone 17 Pro' test -only-testing:LevainUITests/LifecycleUITests`
- [ ] The shell renders `Oggi`, `Preparazioni`, and `Knowledge` while existing bread/starter deep links still land directly in the correct objects
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The root shell is ready to host the new Preparazioni hub without breaking current bread/starter entry points

</success_criteria>

<output>
After completion, create `.planning/phases/17-shell-preparations-hubs/17-01-SUMMARY.md`
</output>
