---
phase: 17-shell-preparations-hubs
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - Levain/Persistence/LevainSchema.swift
  - Levain/Persistence/ModelContainerFactory.swift
  - LevainTests/PersistenceMigrationTests.swift
autonomous: true
user_setup: []
must_haves:
  truths:
    - The v1 → v2 SwiftData migration path is prepared before kefir models ship.
    - Migration is additive only and does not alter or delete existing bread/starter/user data.
    - The app can continue bootstrapping the current store safely under the new schema version.
  artifacts:
    - A new additive schema version and migration plan exist in persistence code.
    - ModelContainerFactory uses the migration plan without destructive recovery shortcuts.
    - Migration-focused automated coverage exists for the current store bootstrap path.
  key_links:
    - LevainSchema and ModelContainerFactory stay aligned on the additive v2 migration path.
---

<objective>
Prepare the additive SwiftData migration needed for the v2 milestone before any kefir models enter the store.

Purpose: Convert the addendum's migration rule into executable persistence scaffolding early, so Phase 19 can add kefir models without improvising schema work.
Output: Updated schema versioning, migration-plan plumbing, and tests that protect the existing store against destructive regressions.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@Levain/Persistence/LevainSchema.swift
@Levain/Persistence/ModelContainerFactory.swift
@LevainTests/TestSupport/ModelTestSupport.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add an explicit additive v2 schema version and migration plan</name>
  <files>Levain/Persistence/LevainSchema.swift</files>
  <action>Extend the existing schema/versioning setup with a new v2 milestone version that is clearly additive. Do not introduce kefir models yet if they are not implemented, but establish the version boundary and migration ownership now so future model additions land on a prepared path instead of a rushed one.</action>
  <verify>Code review shows a clear new schema version and migration-plan structure ready for additive model introduction.</verify>
  <done>The project has an explicit v2 schema checkpoint instead of implicit store evolution.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the additive migration plan into container bootstrap safely</name>
  <files>Levain/Persistence/ModelContainerFactory.swift</files>
  <action>Update the container factory to use the migration plan explicitly while preserving the existing non-destructive bootstrap guarantees. Avoid any recovery path that deletes or silently recreates the user store.</action>
  <verify>Static review confirms container bootstrap uses the new migration plan and still avoids destructive recovery.</verify>
  <done>Store bootstrap remains safe for existing users while becoming ready for v2 schema growth.</done>
</task>

<task type="auto">
  <name>Task 3: Add migration-focused automated coverage</name>
  <files>LevainTests/PersistenceMigrationTests.swift</files>
  <action>Add focused tests around schema/bootstrap behavior so the new migration boundary is validated automatically. Reuse the existing model-test support instead of building a separate persistence test harness.</action>
  <verify>The new persistence migration tests run alongside the existing model/bootstrap test support without flaky setup.</verify>
  <done>Phase 17 leaves behind a regression net for schema evolution before Phase 19 changes the data layer.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived CODE_SIGNING_ALLOWED=NO build`
- [ ] `xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived -destination 'platform=iOS Simulator,name=iPhone 17 Pro' test -only-testing:LevainTests/PersistenceMigrationTests`
- [ ] `xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived -destination 'platform=iOS Simulator,name=iPhone 17 Pro' test -only-testing:LevainTests/BackupServiceTests`
- [ ] The persistence layer now declares an additive v2 migration boundary without destructive data-loss paths
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The data layer is explicitly prepared for v2 additive growth before kefir models are implemented

</success_criteria>

<output>
After completion, create `.planning/phases/17-shell-preparations-hubs/17-04-SUMMARY.md`
</output>
