---
phase: 17-shell-preparations-hubs
plan: 02
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - Levain/App/AppRouter.swift
  - Levain/Features/Shared/RootTabView.swift
  - Levain/Features/Preparations/PreparationsView.swift
  - Levain/Features/Preparations/PreparationHubCardView.swift
  - Levain/Features/BreadHub/BreadHubView.swift
  - Levain/Features/Kefir/KefirHubView.swift
autonomous: true
user_setup: []
must_haves:
  truths:
    - Preparazioni shows two domain entry points: Pane e lievito madre and Milk kefir.
    - Compact quick actions for Nuovo impasto, Nuovo starter, and Nuovo batch kefir are always visible in the root.
    - The bread hub exposes Impasti, Starter, and Formule without cloning existing screen logic.
    - Hub cards remain visible when empty and expose inline CTAs, including a lightweight milk kefir first-open state.
  artifacts:
    - Preparations root and hub-card views exist in a dedicated feature area.
    - Bread hub view and routes bridge into existing bread destinations.
    - Milk kefir hub placeholder exists without pretending unimplemented flows are complete.
  key_links:
    - Preparazioni routes into existing Bakes, Starter, and FormulaList destinations through AppRouter.
---

<objective>
Build the new Preparazioni root and the internal bread hub that rehouses the current bread workflows.

Purpose: Deliver the structural UX promised by the PRD while keeping the proven bread domain behavior almost intact.
Output: New Preparazioni screens, always-visible quick actions, domain hub cards, and routed entry points for Pane e lievito madre and Milk kefir.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-shell-preparations-hubs/17-01-PLAN.md
@Levain/Features/Bakes/BakesView.swift
@Levain/Features/Starter/StarterView.swift
@Levain/Features/Bakes/FormulaListView.swift
@Levain/DesignSystem/Components/SectionCard.swift
@Levain/DesignSystem/Theme.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the Preparazioni root with reusable domain hub cards and always-visible quick actions</name>
  <files>Levain/Features/Preparations/PreparationsView.swift, Levain/Features/Preparations/PreparationHubCardView.swift</files>
  <action>Build the new Preparazioni root around two lightweight hub cards and a permanently visible compact quick-action strip for `Nuovo impasto`, `Nuovo starter`, and `Nuovo batch kefir`. Reuse existing card, spacing, badge, and button patterns instead of introducing a new visual grammar.</action>
  <verify>Visual code review confirms the new root uses existing Theme tokens and shared card/button conventions rather than custom one-off styling, and that quick actions remain compact.</verify>
  <done>Preparazioni communicates the two domains clearly, exposes fast creation paths immediately, and still feels like part of the current app rather than a new product.</done>
</task>

<task type="auto">
  <name>Task 2: Compose a bread hub that routes into existing flows</name>
  <files>Levain/Features/BreadHub/BreadHubView.swift, Levain/App/AppRouter.swift, Levain/Features/Shared/RootTabView.swift</files>
  <action>Create a `Pane e lievito madre` hub with explicit entries for `Impasti`, `Starter`, and `Formule`. Route these entries into the existing bread feature views and destinations so Phase 17 changes how the user reaches bread workflows, not the workflows themselves.</action>
  <verify>`BakesFlowUITests` continues to reach bake and formula flows through the migrated navigation structure.</verify>
  <done>The bread domain is rehomed under Preparazioni without duplicated editors, detail screens, or scheduler logic.</done>
</task>

<task type="auto">
  <name>Task 3: Add empty-state-safe hub cards and a lightweight Milk kefir first-open state</name>
  <files>Levain/Features/Kefir/KefirHubView.swift, Levain/Features/Preparations/PreparationsView.swift</files>
  <action>Ensure both hub cards remain visible even with zero active objects and expose inline CTAs. For the milk kefir entry, provide a first-open empty state with `Nessun batch attivo` and `Nuovo batch`, without forcing culture creation before the first batch.</action>
  <verify>Manual review confirms the empty hub states are visually integrated, keep both domains visible, and stage the milk kefir flow honestly for later phases.</verify>
  <done>Preparazioni feels complete as a structure even though the internal kefir feature set lands in later phases, and empty states never hide the hubs.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived CODE_SIGNING_ALLOWED=NO build`
- [ ] `xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived -destination 'platform=iOS Simulator,name=iPhone 17 Pro' test -only-testing:LevainUITests/BakesFlowUITests`
- [ ] `xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived -destination 'platform=iOS Simulator,name=iPhone 17 Pro' test -only-testing:LevainUITests/LevainUITests`
- [ ] Preparazioni exposes the two domain hubs, always-visible quick actions, and bread hub access to `Impasti`, `Starter`, and `Formule` through reused flows
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Preparazioni exists as the new structural entry point without regressing the current bread workflows or hiding empty domains

</success_criteria>

<output>
After completion, create `.planning/phases/17-shell-preparations-hubs/17-02-SUMMARY.md`
</output>
