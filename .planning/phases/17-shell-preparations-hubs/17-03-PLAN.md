---
phase: 17-shell-preparations-hubs
plan: 03
type: execute
wave: 3
depends_on: ["17-01", "17-02"]
files_modified:
  - Levain/Features/Today/TodayView.swift
  - Levain/Features/Preparations/PreparationsView.swift
  - Levain/DesignSystem/Components/EmptyStateView.swift
  - LevainUITests/PreparationsFlowUITests.swift
  - LevainUITests/LevainUITests.swift
  - LevainUITests/BakesFlowUITests.swift
autonomous: true
user_setup: []
must_haves:
  truths:
    - Shell copy and empty-state messaging refer to the broader multi-fermentation product without losing urgency or clarity.
    - Regression coverage exists for the three-tab shell and Preparazioni-driven access to the bread domain.
    - Always-visible quick actions and empty hub states remain lightweight and reuse existing styles.
  artifacts:
    - Preparations regression UI test coverage exists.
    - Multi-domain empty-state and CTA copy is aligned with the v2 PRD.
  key_links:
    - Preparazioni navigation and preserved bread access are covered by automated UI verification.
---

<objective>
Polish the migrated shell, align copy and empty states to the v2 PRD, and add regression coverage for the new navigation surface.

Purpose: Close Phase 17 with a shell that feels intentionally migrated instead of mechanically rearranged.
Output: Updated multi-domain copy, lightweight shell polish, and UI tests that lock the new navigation baseline in place.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-shell-preparations-hubs/17-01-PLAN.md
@.planning/phases/17-shell-preparations-hubs/17-02-PLAN.md
@Levain/Features/Today/TodayView.swift
@Levain/Features/Preparations/PreparationsView.swift
@Levain/DesignSystem/Components/EmptyStateView.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update shell copy and empty states for the broader product scope</name>
  <files>Levain/Features/Today/TodayView.swift, Levain/Features/Preparations/PreparationsView.swift, Levain/DesignSystem/Components/EmptyStateView.swift</files>
  <action>Replace v1 shell copy that assumes top-level Impasti/Starter navigation with v2 language centered on `Nuova preparazione`, `Pane e lievito madre`, and `Milk kefir`. Keep the empty states action-first, make the hub-card CTAs readable when empty, and avoid descriptive dashboard prose.</action>
  <verify>Manual review confirms top-level CTAs and empty-state messaging match the v2 PRD and still surface one obvious next action.</verify>
  <done>The shell reads like a broader product without losing the operational tone of the current app.</done>
</task>

<task type="auto">
  <name>Task 2: Add regression UI coverage for the migrated shell</name>
  <files>LevainUITests/PreparationsFlowUITests.swift, LevainUITests/LevainUITests.swift, LevainUITests/BakesFlowUITests.swift</files>
  <action>Add or update UI tests so the new three-tab shell, Preparazioni domain cards, bread hub navigation, and preserved bake/formula access are all exercised automatically. Reuse the existing UI-test style and launch helpers instead of creating a separate testing harness.</action>
  <verify>The new `PreparationsFlowUITests` suite and updated smoke tests run deterministically on the simulator.</verify>
  <done>Phase 17 leaves behind a regression net for the new shell, not just ad-hoc manual confidence.</done>
</task>

<task type="auto">
  <name>Task 3: Final shell polish without widening phase scope</name>
  <files>Levain/Features/Preparations/PreparationsView.swift, Levain/Features/Today/TodayView.swift</files>
  <action>Tighten spacing, CTA emphasis, and quick-action placement only where it improves navigational clarity. Do not start cross-domain Today aggregation or kefir internals here; keep the phase focused on shell quality and preserved bread access.</action>
  <verify>Code review confirms the final polish reuses current Theme/Button/Card patterns and does not introduce stray kefir logic or duplicate bread workflows.</verify>
  <done>Phase 17 ends with a clean migration baseline that is ready for cross-domain Today work in Phase 18.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived CODE_SIGNING_ALLOWED=NO build`
- [ ] `xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived -destination 'platform=iOS Simulator,name=iPhone 17 Pro' test -only-testing:LevainUITests/PreparationsFlowUITests`
- [ ] `xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived -destination 'platform=iOS Simulator,name=iPhone 17 Pro' test -only-testing:LevainUITests/LevainUITests`
- [ ] `xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived -destination 'platform=iOS Simulator,name=iPhone 17 Pro' test -only-testing:LevainUITests/BakesFlowUITests`
- [ ] Empty states, inline hub CTAs, and quick actions now refer to the multi-domain shell while bread access still works through Preparazioni
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 17 closes with copy, navigation, empty-state behavior, and regression coverage aligned to the v2 shell

</success_criteria>

<output>
After completion, create `.planning/phases/17-shell-preparations-hubs/17-03-SUMMARY.md`
</output>
