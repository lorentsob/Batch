---
phase: 13-mvp-closure
plan: 02
type: execute
wave: 2
depends_on: [13-01]
files_modified:
  - Levain/Features/Bakes/BakeDetailView.swift
  - Levain/Features/Bakes/BakeStepCardView.swift
  - Levain/Services/NotificationService.swift
  - Levain/Services/BakeReminderPlanner.swift
  - Levain/Services/StarterReminderPlanner.swift
  - Levain/App/AppRouter.swift
  - Levain/Features/Starter/StarterDetailView.swift
  - Levain/Features/Starter/RefreshLogView.swift
autonomous: true
user_setup: []
must_haves:
  truths:
    - Lo step attivo è sempre l'elemento più evidente nel bake detail.
    - Il recovery fuori ordine è accessibile e non comunica un errore dell'app.
    - Le notifiche vengono rischeduled dopo creazione bake, shift timeline, e modifica starter.
    - Il fallback notifica su entità mancante o terminale è non bloccante.
    - Il log refresh starter richiede ≤ 2 tap principali.
---

<objective>
Rifinire l'esecuzione bake, rendere le notifiche robuste end-to-end, e rendere lo starter flow il più rapido possibile.

Purpose: chiudere i tre flussi operativi più critici che bloccano la chiusura dell'MVP.
Output: bake execution leggibile e affidabile, notifiche robuste in tutti gli scenari, starter flow veloce.
</objective>

<context>
@.planning/PROJECT.md
@docs/levain-user-flows.md
@Levain/Features/Bakes/BakeDetailView.swift
@Levain/Features/Bakes/BakeStepCardView.swift
@Levain/Services/NotificationService.swift
@Levain/App/AppRouter.swift
@Levain/Features/Starter/RefreshLogView.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit e polish esecuzione bake</name>
  <files>Levain/Features/Bakes/BakeDetailView.swift, Levain/Features/Bakes/BakeStepCardView.swift</files>
  <action>
    Verificare e correggere:
    - Lo step attivo (running o prossimo da avviare) è sempre visivamente prominente.
    - La differenza tra step completabile e step futuro è inequivocabile.
    - Il recovery fuori ordine non sembra un errore ma una scelta dell'utente — il badge "Fuori ordine" è persistente ma neutro.
    - I messaggi di urgenza sono coerenti con le finestre temporali reali.
    - Il linguaggio è uniforme in tutto il flow bake.
  </action>
  <verify>Seguire il flow bake completo su simulator: creazione → esecuzione in ordine → recovery fuori ordine → completamento.</verify>
  <done>Esecuzione bake leggibile e fidata in tutti i percorsi, incluso il recovery.</done>
</task>

<task type="auto">
  <name>Task 2: Notifiche robuste in tutti gli scenari</name>
  <files>Levain/Services/NotificationService.swift, Levain/Services/BakeReminderPlanner.swift, Levain/Services/StarterReminderPlanner.swift, Levain/App/AppRouter.swift</files>
  <action>
    Verificare e correggere:
    - Notifiche rischeduled dopo ogni creazione bake.
    - Notifiche rischeduled dopo ogni shift timeline.
    - Notifiche starter rischeduled dopo ogni modifica lastRefresh.
    - Deep link da notifica valida apre il contesto corretto (warm e cold launch).
    - Deep link su entità mancante: toast transiente + fallback a tab safe, nessun crash.
    - Deep link su entità terminale (bake completato/cancellato): feedback contestuale non bloccante.
    - Comportamento con app in foreground coerente.
  </action>
  <verify>Eseguire i test esistenti AppRouterTests e NotificationRouteUITests. Aggiungere coverage per i nuovi scenari se necessario.</verify>
  <done>Tutti gli scenari notifica reggono senza crash o navigazione inconsistente.</done>
</task>

<task type="auto">
  <name>Task 3: Starter flow veloce e chiaro</name>
  <files>Levain/Features/Starter/StarterDetailView.swift, Levain/Features/Starter/RefreshLogView.swift</files>
  <action>
    Verificare e migliorare:
    - Log refresh raggiungibile in ≤ 2 tap dalla Home o dalla tab Starter.
    - Form refresh con tre campi principali visibili, avanzati collassati.
    - Valori precompilati sensati (ratio default, peso standard dallo starter).
    - Risultato del refresh immediatamente visibile dopo save.
    - Prossimo refresh dovuto chiaro nella card starter.
    - Reminder e stato visibile nella Home dopo refresh.
  </action>
  <verify>Misurare tap count dal Today al form refresh compilato e salvato.</verify>
  <done>Refresh starter in ≤ 2 tap principali, risultato visibile immediatamente.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `xcodebuild ... -only-testing:LevainTests/BakeSchedulerTests`
- [ ] `xcodebuild ... -only-testing:LevainTests/BakeReminderPlannerTests`
- [ ] `xcodebuild ... -only-testing:LevainTests/StarterReminderPlannerTests`
- [ ] `xcodebuild ... -only-testing:LevainTests/AppRouterTests`
- [ ] `xcodebuild ... -only-testing:LevainUITests/NotificationRouteUITests`
- [ ] `xcodebuild ... -only-testing:LevainUITests/BakesFlowUITests`
</verification>

<success_criteria>

- Esecuzione bake leggibile: step attivo sempre evidente, recovery non ambiguo
- Notifiche robuste in tutti gli scenari (warm, cold, missing, terminal)
- Starter refresh in ≤ 2 tap principali

</success_criteria>
