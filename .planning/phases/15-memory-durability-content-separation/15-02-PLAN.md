---
phase: 15-memory-durability-content-separation
plan: 02
type: execute
wave: 1
depends_on: [15-01]
files_modified:
  - Levain/Services/BackupService.swift
  - Levain/Features/Shared/ActivityView.swift
  - Levain/Features/Starter/SettingsView.swift
  - Levain/Features/Starter/StarterView.swift
  - LevainTests/BackupServiceTests.swift
autonomous: true
user_setup: []
must_haves:
  truths:
    - Backup format must be explicit, versioned, and independent from SwiftData internals.
    - Restore must replace current user data instead of merging blindly.
    - Technical app flags must stay out of the backup payload.
---

<objective>
Add a manual JSON backup and restore safety net for user-owned data.

Purpose: give the user an explicit escape hatch for device migration, reinstall, or recovery without adding backend infrastructure.
Output: `BackupService`, versioned JSON DTOs, minimal settings UI, and restore-triggered notification resync.
</objective>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@Levain/Services/NotificationService.swift
@Levain/Features/Starter/StarterView.swift
@Levain/Models/AppSettings.swift
@Levain/Models/Bake.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define backup payload and export path</name>
  <files>Levain/Services/BackupService.swift</files>
  <action>
    Create `BackupPayloadV1` and dedicated record DTOs for starters, refresh logs, saved recipes, bakes, and bake steps. Encode them as JSON with schema-version validation.
  </action>
  <verify>Export a non-empty in-memory dataset and inspect that JSON contains stable identifiers and version metadata.</verify>
  <done>Backup files are portable and do not depend on direct `@Model` encoding.</done>
</task>

<task type="auto">
  <name>Task 2: Implement replace-current restore and reminder resync</name>
  <files>Levain/Services/BackupService.swift, Levain/Features/Starter/SettingsView.swift</files>
  <action>
    Restore by clearing current user-owned SwiftData records, recreating the object graph from DTOs, rejecting unsupported schema versions, and resyncing notifications after a successful import.
  </action>
  <verify>Run restore twice on an empty store and confirm counts remain stable. Confirm invalid schema versions are rejected.</verify>
  <done>Restore is deterministic, explicit, and safe against unsupported payloads.</done>
</task>

<task type="auto">
  <name>Task 3: Add minimal settings surface for export/import</name>
  <files>Levain/Features/Shared/ActivityView.swift, Levain/Features/Starter/SettingsView.swift, Levain/Features/Starter/StarterView.swift</files>
  <action>
    Add a small settings sheet reachable from the Starter tab that exposes `Esporta backup` and `Importa backup`, with destructive confirmation before import.
  </action>
  <verify>Open the settings sheet, export a backup, and trigger the import flow with confirmation UI.</verify>
  <done>Backup utilities are reachable without expanding the app's IA.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Backup export produces versioned JSON
- [ ] Restore rejects unsupported payload versions
- [ ] Import replaces current user data and triggers reminder resync
- [ ] Settings sheet exposes both export and import
</verification>

<success_criteria>

- User-owned data can be exported and restored manually
- Backup payload excludes technical flags and bundled content
- Restore path is explicit replace-current-data, not merge or sync

</success_criteria>
