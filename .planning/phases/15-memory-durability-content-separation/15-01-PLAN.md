---
phase: 15-memory-durability-content-separation
plan: 01
type: execute
wave: 1
depends_on: [14-01]
files_modified:
  - Levain/Persistence/LevainSchema.swift
  - Levain/Persistence/ModelContainerFactory.swift
  - LevainTests/TestSupport/ModelTestSupport.swift
  - LevainTests/SeedDataLoaderTests.swift
autonomous: true
user_setup: []
must_haves:
  truths:
    - Persisted user data must no longer depend on implicit SwiftData schema inference.
    - Persistent bootstrap must not erase the existing store automatically.
    - Future model changes need an explicit schema-versioning rule recorded in project memory.
---

<objective>
Introduce explicit SwiftData schema ownership and safe container bootstrap so app updates do not silently wipe local user data.

Purpose: protect real-device persistence before any further model churn.
Output: `LevainSchemaV1`, `LevainMigrationPlan`, safe `ModelContainerFactory`, and tests using the same schema source of truth.
</objective>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@Levain/Persistence/ModelContainerFactory.swift
@Levain/Persistence/SeedDataLoader.swift
@Levain/Models/Starter.swift
@Levain/Models/RecipeFormula.swift
@Levain/Models/Bake.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Introduce explicit schema source of truth</name>
  <files>Levain/Persistence/LevainSchema.swift</files>
  <action>
    Define `LevainSchemaV1` with every persisted model currently in the app and connect it to an empty `LevainMigrationPlan` as the starting versioned baseline.
  </action>
  <verify>Build and test against an in-memory container created from the new schema.</verify>
  <done>All persistence entry points reference the explicit versioned schema instead of raw model lists.</done>
</task>

<task type="auto">
  <name>Task 2: Remove destructive persistent-store recovery</name>
  <files>Levain/Persistence/ModelContainerFactory.swift</files>
  <action>
    Update the container factory to use `LevainSchema.current` and `LevainMigrationPlan.self`, and remove any automatic persistent-store deletion fallback from normal app bootstrap.
  </action>
  <verify>Review the error path and confirm no code path deletes the on-disk store during persistent container creation.</verify>
  <done>Persistent bootstrap either succeeds or falls back to an in-memory session without mutating the user's store.</done>
</task>

<task type="auto">
  <name>Task 3: Align tests with the schema baseline</name>
  <files>LevainTests/TestSupport/ModelTestSupport.swift, LevainTests/SeedDataLoaderTests.swift</files>
  <action>
    Ensure test helpers and seed-loader tests instantiate model contexts from the new schema and migration plan.
  </action>
  <verify>Run unit tests that exercise seed loading and in-memory model contexts.</verify>
  <done>Tests no longer bypass the production schema definition.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Build passes with `LevainSchemaV1` wired in
- [ ] No destructive persistent-store reset remains in production bootstrap
- [ ] Test support uses the same schema and migration plan as app code
</verification>

<success_criteria>

- Explicit `VersionedSchema` is present and used by the app
- Persistent bootstrap never auto-deletes the store
- Future model evolution now has a documented schema-ownership rule

</success_criteria>
