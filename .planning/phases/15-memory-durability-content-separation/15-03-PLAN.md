---
phase: 15-memory-durability-content-separation
plan: 03
type: execute
wave: 1
depends_on: [15-01]
files_modified:
  - Levain/Models/SystemFormula.swift
  - Levain/Services/SystemFormulaLoader.swift
  - Levain/Resources/system_formulas.json
  - Levain/Features/Bakes/BakeCreationView.swift
  - Levain/Features/Bakes/FormulaListView.swift
  - LevainTests/SystemFormulaLoaderTests.swift
autonomous: true
user_setup: []
must_haves:
  truths:
    - System recipe templates must come from bundled content, not hardcoded arrays or demo seed data.
    - System templates must remain usable in bake creation even when the user has no saved recipes.
    - Bundled system content should remain read-only and separate from persisted user recipes.
---

<objective>
Separate official system formulas from demo seed data and make them available as bundled read-only content.

Purpose: keep official app-provided templates deterministic and independent from both user persistence and internal test fixtures.
Output: `SystemFormula`, `SystemFormulaLoader`, `system_formulas.json`, bake-creation integration, and tests for bundled decode behavior.
</objective>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@Levain/Models/RecipeFormula.swift
@Levain/Persistence/SeedDataLoader.swift
@Levain/Features/Bakes/BakeCreationView.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace hardcoded template source with bundled JSON</name>
  <files>Levain/Models/SystemFormula.swift, Levain/Services/SystemFormulaLoader.swift, Levain/Resources/system_formulas.json</files>
  <action>
    Introduce a read-only `SystemFormula` value type and a loader that decodes `system_formulas.json` from the app bundle as the official system-template source.
  </action>
  <verify>Decode bundled formulas in tests and confirm expected templates and default steps load correctly.</verify>
  <done>System formulas are now repository-versioned bundle content instead of hardcoded code structures.</done>
</task>

<task type="auto">
  <name>Task 2: Keep system templates available in bake creation without persisting them by default</name>
  <files>Levain/Features/Bakes/BakeCreationView.swift</files>
  <action>
    Update the bake-creation flow to present user recipes plus bundled system templates, generate transient formulas for system selections, and avoid saving those system templates into the user's recipe list automatically.
  </action>
  <verify>Open `Nuovo bake` on an empty store and confirm system templates are selectable and can generate a bake.</verify>
  <done>`Nuovo bake` works even with zero saved recipes and does not treat bundled templates as user-owned recipes.</done>
</task>

<task type="auto">
  <name>Task 3: Clarify user-facing separation from saved recipes and demo seed</name>
  <files>Levain/Features/Bakes/FormulaListView.swift, LevainTests/SystemFormulaLoaderTests.swift</files>
  <action>
    Adjust copy and tests so the app communicates that system templates live in the bake-creation flow, while demo seed remains an internal harness path.
  </action>
  <verify>Review recipe-list copy and run bundled-loader tests.</verify>
  <done>The official system-content path is explicit and no longer conflated with saved recipes or sample seed data.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `system_formulas.json` loads successfully
- [ ] `Nuovo bake` shows templates with zero saved recipes
- [ ] Selecting a system template creates a bake without auto-saving a new recipe
- [ ] UI copy no longer implies hardcoded or demo-seed ownership of system templates
</verification>

<success_criteria>

- System templates are bundled, read-only content
- Demo seed is no longer the official template source
- Bake creation remains usable on a fresh empty store

</success_criteria>
