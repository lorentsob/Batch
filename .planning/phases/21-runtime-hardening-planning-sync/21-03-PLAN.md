---
phase: 21-runtime-hardening-planning-sync
plan: 03
type: execute
wave: 3
depends_on: ["21-01", "21-02"]
files_modified:
  - Levain/Persistence/ModelContainerFactory.swift
  - Levain/Services/NotificationService.swift
  - LevainTests/ModelContainerFactoryTests.swift
  - LevainUITests/NotificationRouteUITests.swift
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
  - .planning/codebase/ARCHITECTURE.md
  - .planning/codebase/STRUCTURE.md
  - .planning/codebase/CONCERNS.md
  - .planning/codebase/TESTING.md
autonomous: true
user_setup: []
must_haves:
  truths:
    - Persistent bootstrap and save failures must become explicit; a local-first app cannot silently drift into degraded behavior.
    - Reminder-routing code must never fabricate random identifiers for missing targets.
    - Phase 21 only closes once the planning/codebase docs match the real three-tab shell and shipped feature map.
  artifacts:
    - Levain/Persistence/ModelContainerFactory.swift
    - Levain/Services/NotificationService.swift
    - .planning/codebase/ARCHITECTURE.md
  key_links:
    - Persistence failure surfacing and routing guardrails must stay consistent with the existing direct-object router contract.
    - The codebase docs are part of the runtime hardening scope because stale memory already delayed correct execution once during Phase 20 closeout.
---

<objective>
Finish the hardening milestone by making persistence/routing failures explicit and bringing the codebase memory back in sync with the shipped app.

Purpose: Runtime safety is incomplete if persistence can degrade silently, routing can synthesize fake targets, and the docs still describe an outdated architecture.
Output: Explicit persistence error handling, route guardrails for notifications, and refreshed `.planning/codebase` documents that match the real app.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-runtime-hardening-planning-sync/21-CONTEXT.md
@Levain/Persistence/ModelContainerFactory.swift
@Levain/Services/NotificationService.swift
@LevainUITests/NotificationRouteUITests.swift
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/CONCERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Surface persistence bootstrap and save failures explicitly</name>
  <files>Levain/Persistence/ModelContainerFactory.swift, LevainTests/ModelContainerFactoryTests.swift</files>
  <action>Replace silent degraded persistence fallback with explicit handling and test coverage so bootstrap or save failures are visible and reviewable instead of quietly degrading the app into an in-memory or partially-saved state.</action>
  <verify>xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived -destination 'platform=iOS Simulator,name=iPhone 15 Pro,OS=26.4' test -only-testing:LevainTests/ModelContainerFactoryTests</verify>
  <done>Persistence failures are explicit and covered instead of silently ignored.</done>
</task>

<task type="auto">
  <name>Task 2: Add routing guardrails for reminder and deep-link edge cases</name>
  <files>Levain/Services/NotificationService.swift, LevainUITests/NotificationRouteUITests.swift</files>
  <action>Remove any route construction that can fabricate random identifiers for missing objects, especially around fridge reminder handling, and lock the result down with route regression coverage.</action>
  <verify>xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived -destination 'platform=iOS Simulator,name=iPhone 15 Pro,OS=26.4' test -only-testing:LevainUITests/NotificationRouteUITests</verify>
  <done>Notification routes either resolve to real objects or fail safely without manufacturing fake targets.</done>
</task>

<task type="auto">
  <name>Task 3: Refresh the codebase and planning docs to match the shipped app</name>
  <files>.planning/STATE.md, .planning/ROADMAP.md, .planning/PROJECT.md, .planning/REQUIREMENTS.md, .planning/codebase/ARCHITECTURE.md, .planning/codebase/STRUCTURE.md, .planning/codebase/CONCERNS.md, .planning/codebase/TESTING.md</files>
  <action>Update the codebase memory so it accurately describes the three-tab shell, shipped kefir vertical, current test surfaces, and the hardening decisions introduced in Phase 21. Treat stale documentation as a real blocker to the next milestone, not cleanup work.</action>
  <verify>Manual review confirms active planning docs and `.planning/codebase/*.md` describe the same architecture and current feature map.</verify>
  <done>The codebase memory is trustworthy again before Phase 22 begins.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived CODE_SIGNING_ALLOWED=NO build`
- [ ] `xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived -destination 'platform=iOS Simulator,name=iPhone 15 Pro,OS=26.4' test -only-testing:LevainTests/ModelContainerFactoryTests`
- [ ] `xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived -destination 'platform=iOS Simulator,name=iPhone 15 Pro,OS=26.4' test -only-testing:LevainUITests/NotificationRouteUITests`
- [ ] Active planning docs and `.planning/codebase/*.md` reflect the same shipped architecture and phase ordering
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Persistence/routing behavior is safer and the project memory is current again

</success_criteria>

<output>
After completion, create `.planning/phases/21-runtime-hardening-planning-sync/21-03-SUMMARY.md`
</output>
