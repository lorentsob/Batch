---
phase: 21-runtime-hardening-planning-sync
plan: 01
type: execute
wave: 1
depends_on: ["20-04"]
files_modified:
  - Levain/Features/Today/TodayView.swift
  - Levain/Models/Bake.swift
  - Levain/Services/TodayAgendaBuilder.swift
  - LevainTests/TodayAgendaBuilderTests.swift
  - LevainUITests/TodayFlowUITests.swift
autonomous: true
user_setup: []
must_haves:
  truths:
    - `Oggi` should derive one stable operational snapshot per data update instead of repeating wide scans and sorts inside the render path.
    - Ordered-step and derived-status helpers must stop re-sorting the same relationships whenever SwiftUI recomputes a view.
    - User-facing Today behavior stays the same; this wave hardens runtime cost without changing the product grammar or routes.
  artifacts:
    - Levain/Features/Today/TodayView.swift
    - Levain/Models/Bake.swift
    - LevainTests/TodayAgendaBuilderTests.swift
  key_links:
    - Today snapshot reduction and bake helper normalization must preserve the same urgency/order semantics already validated in Phase 18 and Phase 19.
    - `TodayFlowUITests` and `TodayAgendaBuilderTests` are the first guardrails against performance fixes that accidentally change behavior.
---

<objective>
Reduce obvious operational render cost in `Oggi` and bread helper code without changing the shipped user flow.

Purpose: The dashboard is now the center of the v2 shell, so repeated render-time scans, sorts, and derived-state rebuilding create unnecessary runtime cost every time operational data changes.
Output: Narrower Today invalidation, shared ordered-step helpers, and regression coverage that locks the current semantics in place.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-runtime-hardening-planning-sync/21-CONTEXT.md
@Levain/Features/Today/TodayView.swift
@Levain/Services/TodayAgendaBuilder.swift
@Levain/Models/Bake.swift
@LevainTests/TodayAgendaBuilderTests.swift
@LevainUITests/TodayFlowUITests.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Narrow Today snapshot invalidation and render-time work</name>
  <files>Levain/Features/Today/TodayView.swift, Levain/Services/TodayAgendaBuilder.swift</files>
  <action>Reduce the root-level data and snapshot work `Oggi` performs during body recomputation so the screen stops rebuilding broad derived collections when only a smaller operational subset has changed. Preserve the existing ranked-feed behavior and routing contract.</action>
  <verify>xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived -destination 'platform=iOS Simulator,name=iPhone 15 Pro,OS=26.4' test -only-testing:LevainTests/TodayAgendaBuilderTests</verify>
  <done>`Oggi` builds its operational snapshot with less repeated work while keeping the same order and urgency semantics.</done>
</task>

<task type="auto">
  <name>Task 2: Consolidate ordered-step and derived-status helpers for bread</name>
  <files>Levain/Models/Bake.swift, Levain/Features/Today/TodayView.swift</files>
  <action>Move repeated bake-step sorting and derived-status lookups behind shared helpers so render paths do not rescan or resort the same relationships on every update. Keep the existing bake model and operational UI behavior intact.</action>
  <verify>xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived CODE_SIGNING_ALLOWED=NO build</verify>
  <done>Bread-derived operational state is computed once per update path instead of repeatedly inside SwiftUI view trees.</done>
</task>

<task type="auto">
  <name>Task 3: Lock the behavior down with Today regressions and one profiling pass</name>
  <files>LevainTests/TodayAgendaBuilderTests.swift, LevainUITests/TodayFlowUITests.swift</files>
  <action>Add or update targeted tests for snapshot reduction, then rerun the shipped Today UI flows. Capture one profiling pass for launch and `Oggi` interactions so this hardening wave is validated with real execution evidence, not only code review.</action>
  <verify>xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived -destination 'platform=iOS Simulator,name=iPhone 15 Pro,OS=26.4' test -only-testing:LevainUITests/TodayFlowUITests</verify>
  <done>The new Today data flow is both regression-covered and runtime-checked.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived CODE_SIGNING_ALLOWED=NO build`
- [ ] `xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived -destination 'platform=iOS Simulator,name=iPhone 15 Pro,OS=26.4' test -only-testing:LevainTests/TodayAgendaBuilderTests`
- [ ] `xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived -destination 'platform=iOS Simulator,name=iPhone 15 Pro,OS=26.4' test -only-testing:LevainUITests/TodayFlowUITests`
- [ ] One profiling pass confirms `Oggi` launch/update behavior improved without semantics drift
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- `Oggi` keeps the same behavior while doing materially less repeated render-time work

</success_criteria>

<output>
After completion, create `.planning/phases/21-runtime-hardening-planning-sync/21-01-SUMMARY.md`
</output>
