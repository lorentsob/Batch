---
phase: 21-runtime-hardening-planning-sync
plan: 02
type: execute
wave: 2
depends_on: ["21-01"]
files_modified:
  - Levain/Features/Shared/RootTabView.swift
  - Levain/Features/Knowledge/KnowledgeView.swift
  - Levain/App/AppEnvironment.swift
  - Levain/Services/KnowledgeLoader.swift
  - Levain/Features/Preparations/PreparationsView.swift
  - Levain/Features/Preparations/PreparationHubCardView.swift
  - Levain/Features/Kefir/KefirBatchDetailView.swift
  - Levain/Features/Kefir/KefirJournalView.swift
  - Levain/Features/Kefir/KefirArchiveView.swift
  - Levain/Features/Kefir/KefirBatchComparisonView.swift
  - Levain/Features/Kefir/KefirLineageIndex.swift
  - LevainTests/KnowledgeLibraryTests.swift
  - LevainUITests/KnowledgeFlowUITests.swift
  - LevainUITests/KefirFlowUITests.swift
autonomous: true
user_setup: []
must_haves:
  truths:
    - `RootTabView` remains the only owner of knowledge navigation; `KnowledgeView` must not create a nested stack.
    - Knowledge-facing views should observe `KnowledgeLibrary` directly so state changes come from the real publisher instead of environment indirection.
    - Kefir detail, journal, archive, and comparison flows should share one lineage/index layer instead of recomputing full-batch maps per screen.
    - The kefir quick-actions card must keep its secondary actions aligned and actually tappable; layout polish that breaks interaction is part of the same hardening scope.
    - The `Preparazioni` hub must use coherent domain icons: bread/lievito madre should use the existing `bake.svg` asset, and kefir should use a plain drop icon aligned with the design system instead of `drop.triangle.fill`.
  artifacts:
    - Levain/Features/Shared/RootTabView.swift
    - Levain/Features/Knowledge/KnowledgeView.swift
    - Levain/Features/Kefir/KefirLineageIndex.swift
    - Levain/Features/Preparations/PreparationHubCardView.swift
  key_links:
    - Fixing knowledge ownership and kefir lineage indexing in the same wave keeps the state-propagation hardening coherent across the two most obviously duplicated surfaces.
    - `KnowledgeFlowUITests` and `KefirFlowUITests` must stay green because this wave changes navigation/state ownership and shipped kefir/preparations interactions rather than new product scope.
---

<objective>
Unify state ownership for Knowledge and kefir lineage-heavy screens so the shipped navigation and data flow stay stable under change.

Purpose: The current code splits navigation/state ownership in Knowledge and repeats lineage recomputation across multiple kefir screens, which raises render cost and makes future fixes harder to reason about.
Output: One knowledge navigation owner, direct observation of `KnowledgeLibrary`, a shared kefir lineage/index helper reused by every lineage surface, and corrected kefir/preparations interaction polish.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-runtime-hardening-planning-sync/21-CONTEXT.md
@Levain/Features/Shared/RootTabView.swift
@Levain/Features/Knowledge/KnowledgeView.swift
@Levain/App/AppEnvironment.swift
@Levain/Services/KnowledgeLoader.swift
@Levain/Features/Preparations/PreparationsView.swift
@Levain/Features/Preparations/PreparationHubCardView.swift
@Levain/Features/Kefir/KefirBatchDetailView.swift
@Levain/Features/Kefir/KefirJournalView.swift
@LevainUITests/KnowledgeFlowUITests.swift
@LevainUITests/KefirFlowUITests.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix knowledge navigation and observation ownership</name>
  <files>Levain/Features/Shared/RootTabView.swift, Levain/Features/Knowledge/KnowledgeView.swift, Levain/App/AppEnvironment.swift, Levain/Services/KnowledgeLoader.swift</files>
  <action>Remove nested knowledge navigation ownership and wire the knowledge UI to observe `KnowledgeLibrary` directly so load state, search, and article navigation update from one consistent source. Keep the existing knowledge feature set and copy unchanged.</action>
  <verify>xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived -destination 'platform=iOS Simulator,name=iPhone 15 Pro,OS=26.4' test -only-testing:LevainTests/KnowledgeLibraryTests</verify>
  <done>Knowledge navigation and state updates flow from one predictable ownership path.</done>
</task>

<task type="auto">
  <name>Task 2: Introduce shared lineage indexing and fix shipped kefir/preparations interaction polish</name>
  <files>Levain/Features/Kefir/KefirLineageIndex.swift, Levain/Features/Kefir/KefirBatchDetailView.swift, Levain/Features/Kefir/KefirJournalView.swift, Levain/Features/Kefir/KefirArchiveView.swift, Levain/Features/Kefir/KefirBatchComparisonView.swift, Levain/Features/Preparations/PreparationsView.swift, Levain/Features/Preparations/PreparationHubCardView.swift</files>
  <action>Create a reusable lineage/index helper and adopt it across every kefir surface that currently recomputes source/derived/archive maps from the full batch collection. In the same wave, fix the kefir detail quick-actions layout so the secondary buttons stay aligned and tappable, and replace the mismatched Preparazioni hub icons with design-system-consistent domain icons: bread/lievito madre from `Levain/icons/bake.svg`, kefir with a plain drop icon instead of `drop.triangle.fill`.</action>
  <verify>xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived CODE_SIGNING_ALLOWED=NO build</verify>
  <done>Kefir lineage-heavy screens reuse one indexed data path, the kefir quick-action card behaves correctly, and the Preparazioni hub icons are coherent with the rest of the design system.</done>
</task>

<task type="auto">
  <name>Task 3: Re-run knowledge and kefir regressions on the hardened ownership model</name>
  <files>LevainUITests/KnowledgeFlowUITests.swift, LevainUITests/KefirFlowUITests.swift</files>
  <action>Run the shipped knowledge and kefir UI flows against the new ownership model and add targeted assertions where needed so future work cannot quietly reintroduce nested stacks or repeated lineage state drift.</action>
  <verify>xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived -destination 'platform=iOS Simulator,name=iPhone 15 Pro,OS=26.4' test -only-testing:LevainUITests/KnowledgeFlowUITests -only-testing:LevainUITests/KefirFlowUITests</verify>
  <done>The state-ownership changes are protected by the same UI flows users rely on today.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived CODE_SIGNING_ALLOWED=NO build`
- [ ] `xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived -destination 'platform=iOS Simulator,name=iPhone 15 Pro,OS=26.4' test -only-testing:LevainTests/KnowledgeLibraryTests`
- [ ] `xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived -destination 'platform=iOS Simulator,name=iPhone 15 Pro,OS=26.4' test -only-testing:LevainUITests/KnowledgeFlowUITests -only-testing:LevainUITests/KefirFlowUITests`
- [ ] Knowledge load/search/article flows and kefir lineage flows behave the same from the user's point of view
- [ ] The kefir quick-actions card stays visually aligned and every visible secondary action is tappable
- [ ] The Preparazioni bread and kefir hub icons are domain-correct and visually consistent with the design system
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Knowledge and kefir lineage screens share cleaner ownership and less duplicated recomputation, and the shipped kefir/preparations interaction polish is corrected

</success_criteria>

<output>
After completion, create `.planning/phases/21-runtime-hardening-planning-sync/21-02-SUMMARY.md`
</output>
