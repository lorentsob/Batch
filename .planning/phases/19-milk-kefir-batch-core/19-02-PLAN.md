---
phase: 19-milk-kefir-batch-core
plan: 02
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - Levain/Features/Kefir/KefirHubView.swift
  - Levain/Features/Kefir/KefirBatchListView.swift
  - Levain/Features/Kefir/KefirBatchCardView.swift
  - Levain/Features/Kefir/KefirBatchDetailView.swift
  - Levain/Features/Preparations/PreparationsView.swift
  - LevainUITests/KefirFlowUITests.swift
  - Levain.xcodeproj/project.pbxproj
autonomous: true
user_setup: []
must_haves:
  truths:
    - Milk kefir is no longer a placeholder hub: users can see persisted batch state through a real hub/list/detail flow.
    - Batch cards and detail communicate storage mode, derived urgency, and last/next management context without needing journal-heavy UI.
    - Preparazioni reflects real kefir activity counts and links into the batch vertical through the same design system and navigation language as the bread hub.
  artifacts:
    - Levain/Features/Kefir/KefirHubView.swift
    - Levain/Features/Kefir/KefirBatchListView.swift
    - Levain/Features/Kefir/KefirBatchDetailView.swift
  key_links:
    - PreparationsView uses persisted `KefirBatch` queries to show live root-hub state instead of a hard-coded placeholder.
    - KefirBatchListView and KefirBatchCardView present active, warning, paused, and archived groupings from the model’s derived semantics.
    - KefirFlowUITests cover the end-to-end hub and detail navigation baseline for the new vertical.
---

<objective>
Replace the kefir placeholder with the first real batch hub, list, and detail surfaces.

Purpose: Phase 19 needs a visible vertical users can enter and understand before creation/manage flows and reminder plumbing make it operationally complete.
Output: Real kefir hub/list/detail UI, live Preparazioni integration, and initial UI coverage for the batch vertical.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-milk-kefir-batch-core/19-CONTEXT.md
@.planning/phases/19-milk-kefir-batch-core/19-01-PLAN.md
@Levain/Features/Kefir/KefirHubView.swift
@Levain/Features/Preparations/PreparationsView.swift
@Levain/DesignSystem/Components/SectionCard.swift
@Levain/DesignSystem/Components/EmptyStateView.swift
@Levain/Models/KefirBatch.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Turn the kefir hub into a real batch-entry surface</name>
  <files>Levain/Features/Kefir/KefirHubView.swift, Levain/Features/Kefir/KefirBatchListView.swift, Levain/Features/Kefir/KefirBatchCardView.swift, Levain.xcodeproj/project.pbxproj</files>
  <action>Replace the placeholder hub with a real batch view that separates active, warning, paused, and archived items in a way that stays operational rather than analytical. Each batch card should surface name, storage mode, derived state, last/next management context, and a clear primary CTA while reusing current card and badge primitives.</action>
  <verify>xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived CODE_SIGNING_ALLOWED=NO build</verify>
  <done>The kefir vertical has a live hub/list that reads like part of Levain instead of a stub screen.</done>
</task>

<task type="auto">
  <name>Task 2: Build the core batch detail with one dominant action and quick actions</name>
  <files>Levain/Features/Kefir/KefirBatchDetailView.swift, Levain/Features/Kefir/KefirBatchCardView.swift</files>
  <action>Create a batch detail screen that shows the header, storage-aware operational status, last/next management timing, concise use/difference context, and one dominant action plus quick actions for renew, derive, storage/state change, and archive. Keep the screen focused on operational clarity; do not start building the structured journal UI owned by Phase 20.</action>
  <verify>Manual review confirms the detail view makes the next action obvious and does not rely on heavy timeline or journal scaffolding.</verify>
  <done>Users can open a batch and immediately understand what it is, what state it is in, and what to do next.</done>
</task>

<task type="auto">
  <name>Task 3: Reflect live kefir activity back into Preparazioni and lock the baseline with UI tests</name>
  <files>Levain/Features/Preparations/PreparationsView.swift, LevainUITests/KefirFlowUITests.swift</files>
  <action>Update the Preparazioni root so the Milk kefir hub card shows real batch counts and CTA behavior instead of static placeholder text, then add a focused UI suite that verifies the hub opens, batch cards appear in seeded scenarios, and detail navigation works. Reuse the existing launch harness rather than inventing a separate kefir test bootstrap path.</action>
  <verify>xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived -destination 'platform=iOS Simulator,name=iPhone 17 Pro' test -only-testing:LevainUITests/KefirFlowUITests</verify>
  <done>The kefir vertical is visible from the root shell and covered by a minimal regression net.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived CODE_SIGNING_ALLOWED=NO build`
- [ ] `xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived -destination 'platform=iOS Simulator,name=iPhone 17 Pro' test -only-testing:LevainUITests/KefirFlowUITests`
- [ ] The kefir placeholder is replaced by a real hub/list/detail baseline wired into Preparazioni
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 19 has a visible kefir vertical baseline that users can enter, inspect, and navigate before editing/reminder work lands

</success_criteria>

<output>
After completion, create `.planning/phases/19-milk-kefir-batch-core/19-02-SUMMARY.md`
</output>
