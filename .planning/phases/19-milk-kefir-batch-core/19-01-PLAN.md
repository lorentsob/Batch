---
phase: 19-milk-kefir-batch-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Levain/Models/KefirBatch.swift
  - Levain/Models/DomainEnums.swift
  - Levain/Persistence/LevainSchema.swift
  - LevainTests/KefirBatchTests.swift
  - LevainTests/PersistenceMigrationTests.swift
  - LevainTests/TestSupport/DomainFixtures.swift
  - Levain.xcodeproj/project.pbxproj
autonomous: true
user_setup: []
must_haves:
  truths:
    - The app can persist multiple milk kefir batches without requiring a culture model to exist first.
    - Batch urgency and next-action semantics derive from batch timestamps, routine, storage mode, and archive/paused data rather than a second persisted status truth.
    - The SwiftData schema grows additively to include kefir batch persistence without risking existing bread or starter data.
  artifacts:
    - Levain/Models/KefirBatch.swift
    - Levain/Persistence/LevainSchema.swift
    - LevainTests/KefirBatchTests.swift
  key_links:
    - KefirBatch persistence fields support both first-batch creation and future derived-batch flows through an optional source-batch reference.
    - DomainEnums provides kefir-specific storage and derived-state vocabulary without mutating starter semantics into a generic abstraction.
    - PersistenceMigrationTests verify the store can bootstrap across the new additive schema boundary with existing data intact.
---

<objective>
Lay the persistent and derived-state foundation for the milk kefir vertical.

Purpose: Every later kefir screen, reminder, and Oggi integration depends on having one sane operational model with explicit storage-aware timing semantics.
Output: `KefirBatch` model, kefir-specific enums and derived-state helpers, additive schema migration, and focused tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/19-milk-kefir-batch-core/19-CONTEXT.md
@docs/levain-prd-v2-multi-fermentations.md
@docs/levain-prd-v2-addendum.md
@Levain/Models/Starter.swift
@Levain/Models/DomainEnums.swift
@Levain/Persistence/LevainSchema.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a batch-first kefir persistence model with derived operational timing</name>
  <files>Levain/Models/KefirBatch.swift, Levain/Models/DomainEnums.swift</files>
  <action>Create a `KefirBatch` model that stores only the durable batch facts needed for core operations: identity, creation/management timestamps, expected routine, storage mode, optional use/difference notes, archive flags, alerts preference, optional reactivation data, and optional `sourceBatchId`. Keep urgency, next-due semantics, and primary-action suggestions derived from those fields rather than persisting a second operational state that can drift.</action>
  <verify>xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived -destination 'platform=iOS Simulator,name=iPhone 17 Pro' test -only-testing:LevainTests/KefirBatchTests</verify>
  <done>The codebase has one durable kefir batch model with domain-specific enums and derived helpers suited to room, fridge, and freezer usage.</done>
</task>

<task type="auto">
  <name>Task 2: Extend the additive schema boundary to include persisted kefir batches safely</name>
  <files>Levain/Persistence/LevainSchema.swift, LevainTests/PersistenceMigrationTests.swift</files>
  <action>Advance the schema versioning introduced in Phase 17 so the new `KefirBatch` model lands through an explicit additive migration stage. Preserve the non-destructive bootstrap rule: existing bread/starter stores must remain readable, and no fallback path may delete persisted user data to recover from schema issues.</action>
  <verify>xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived -destination 'platform=iOS Simulator,name=iPhone 17 Pro' test -only-testing:LevainTests/PersistenceMigrationTests</verify>
  <done>The persistence layer is ready to store kefir batches without endangering the existing local store.</done>
</task>

<task type="auto">
  <name>Task 3: Add deterministic batch fixtures and derived-state coverage</name>
  <files>LevainTests/KefirBatchTests.swift, LevainTests/TestSupport/DomainFixtures.swift, Levain.xcodeproj/project.pbxproj</files>
  <action>Introduce fixtures for room-temperature, fridge, freezer, and derived-batch scenarios so core timing and state rules can be verified narrowly. Cover first-batch creation without culture linkage, source-batch derivation metadata, and the storage-aware differences in due/warning behavior before UI or reminders are layered on top.</action>
  <verify>xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived -destination 'platform=iOS Simulator,name=iPhone 17 Pro' test -only-testing:LevainTests/KefirBatchTests</verify>
  <done>Core kefir state semantics are protected by deterministic unit tests instead of being discovered only through UI behavior.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived CODE_SIGNING_ALLOWED=NO build`
- [ ] `xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived -destination 'platform=iOS Simulator,name=iPhone 17 Pro' test -only-testing:LevainTests/KefirBatchTests`
- [ ] `xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived -destination 'platform=iOS Simulator,name=iPhone 17 Pro' test -only-testing:LevainTests/PersistenceMigrationTests`
- [ ] The codebase can persist batch-first kefir data with derived storage-aware state and no destructive schema shortcuts
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 19 has a real data-layer foundation for kefir that later UI and reminder plans can build on directly

</success_criteria>

<output>
After completion, create `.planning/phases/19-milk-kefir-batch-core/19-01-SUMMARY.md`
</output>
