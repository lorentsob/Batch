---
phase: 19-milk-kefir-batch-core
plan: 04
type: execute
wave: 4
depends_on: ["19-01", "19-03"]
files_modified:
  - Levain/Services/KefirReminderPlanner.swift
  - Levain/Services/NotificationService.swift
  - Levain/Services/TodayAgendaBuilder.swift
  - Levain/Features/Today/TodayView.swift
  - Levain/App/AppRouter.swift
  - Levain/Persistence/SeedDataLoader.swift
  - LevainTests/KefirReminderPlannerTests.swift
  - LevainTests/TodayAgendaBuilderTests.swift
  - LevainUITests/NotificationRouteUITests.swift
  - LevainUITests/TodayFlowUITests.swift
  - LevainUITests/KefirFlowUITests.swift
  - Levain.xcodeproj/project.pbxproj
autonomous: true
user_setup: []
must_haves:
  truths:
    - Kefir reminders follow storage-aware defaults: room temperature 24h, fridge 7 days, freezer no automatic alerts unless reactivation is planned.
    - Oggi can now surface real kefir batch items through the Phase 18 feed contract with urgency and microcopy aligned to the reminder semantics.
    - Notification taps and Oggi taps open the actual batch detail directly through the shared router contract.
  artifacts:
    - Levain/Services/KefirReminderPlanner.swift
    - Levain/Services/NotificationService.swift
    - LevainTests/KefirReminderPlannerTests.swift
  key_links:
    - KefirReminderPlanner and TodayAgendaBuilder share the same storage-aware urgency rules so cards and notifications do not drift.
    - NotificationService resyncs kefir requests after creation, renew, storage changes, archive, and reactivation scheduling.
    - AppRouter and NotificationRouteUITests cover direct kefir batch routing through the same deep-link surface used by bread and starter.
---

<objective>
Finish the batch core by wiring storage-aware reminders, Oggi integration, and routing coverage around the new kefir model.

Purpose: A persistent batch vertical is still incomplete until it participates in the operational home screen and keeps notification timing aligned with real user actions.
Output: Dedicated kefir reminder planning, Oggi feed integration, seeded scenarios for automation, and routing/regression coverage.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/19-milk-kefir-batch-core/19-CONTEXT.md
@.planning/phases/19-milk-kefir-batch-core/19-01-PLAN.md
@.planning/phases/19-milk-kefir-batch-core/19-03-PLAN.md
@Levain/Services/NotificationService.swift
@Levain/Services/TodayAgendaBuilder.swift
@Levain/App/AppRouter.swift
@Levain/Persistence/SeedDataLoader.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add dedicated kefir reminder planning with storage-aware defaults</name>
  <files>Levain/Services/KefirReminderPlanner.swift, Levain/Services/NotificationService.swift, LevainTests/KefirReminderPlannerTests.swift, Levain.xcodeproj/project.pbxproj</files>
  <action>Introduce a pure reminder planner for `KefirBatch` that encodes the addendum defaults: 24h room-temperature cycle with warning window, 7-day fridge cycle with softer warning, and freezer with no automatic alert unless the batch has a planned reactivation. Keep the logic explicit and testable, and wire NotificationService to resync kefir notifications after the creation/manage mutations added in the earlier plans.</action>
  <verify>xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived -destination 'platform=iOS Simulator,name=iPhone 17 Pro' test -only-testing:LevainTests/KefirReminderPlannerTests</verify>
  <done>Kefir reminder timing is deterministic, storage-aware, and no longer hidden inside NotificationService side effects.</done>
</task>

<task type="auto">
  <name>Task 2: Plug real kefir batches into Oggi through the Phase 18 feed contract</name>
  <files>Levain/Services/TodayAgendaBuilder.swift, Levain/Features/Today/TodayView.swift, LevainTests/TodayAgendaBuilderTests.swift, Levain/Persistence/SeedDataLoader.swift</files>
  <action>Extend the Oggi feed so persisted kefir batches appear alongside bread and starter objects with domain labels, storage-aware urgency, and the appropriate primary action title. Reuse the Phase 18 ranked-feed contract; do not create a kefir-specific home section that bypasses the cross-domain dashboard design.</action>
  <verify>xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived -destination 'platform=iOS Simulator,name=iPhone 17 Pro' test -only-testing:LevainTests/TodayAgendaBuilderTests</verify>
  <done>Oggi now represents the full v2 operational surface by surfacing real kefir work through the same agenda system.</done>
</task>

<task type="auto">
  <name>Task 3: Lock in direct batch routing and automation coverage across app entry points</name>
  <files>Levain/App/AppRouter.swift, Levain/Services/NotificationService.swift, LevainUITests/NotificationRouteUITests.swift, LevainUITests/TodayFlowUITests.swift, LevainUITests/KefirFlowUITests.swift</files>
  <action>Extend the shared deep-link contract to cover kefir batches so notification taps and Oggi taps open batch detail directly. Add or update UI tests so seeded kefir scenarios validate Oggi presence, notification-route handling, and detail entry after management changes. Keep the route surface aligned with the direct-object rule from Phase 18.</action>
  <verify>xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived -destination 'platform=iOS Simulator,name=iPhone 17 Pro' test -only-testing:LevainUITests/NotificationRouteUITests</verify>
  <done>The kefir core feature participates in Oggi and notifications like a first-class domain, not a sidecar flow.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived CODE_SIGNING_ALLOWED=NO build`
- [ ] `xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived -destination 'platform=iOS Simulator,name=iPhone 17 Pro' test -only-testing:LevainTests/KefirReminderPlannerTests`
- [ ] `xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived -destination 'platform=iOS Simulator,name=iPhone 17 Pro' test -only-testing:LevainTests/TodayAgendaBuilderTests`
- [ ] `xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived -destination 'platform=iOS Simulator,name=iPhone 17 Pro' test -only-testing:LevainUITests/NotificationRouteUITests`
- [ ] `xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived -destination 'platform=iOS Simulator,name=iPhone 17 Pro' test -only-testing:LevainUITests/TodayFlowUITests`
- [ ] Real kefir batches now participate in Oggi, local reminders, and direct routing with storage-aware semantics
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 19 closes with a usable batch-first kefir core integrated into the shared shell, Oggi, and notification system

</success_criteria>

<output>
After completion, create `.planning/phases/19-milk-kefir-batch-core/19-04-SUMMARY.md`
</output>
