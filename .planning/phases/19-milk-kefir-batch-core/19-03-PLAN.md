---
phase: 19-milk-kefir-batch-core
plan: 03
type: execute
wave: 3
depends_on: ["19-01", "19-02"]
files_modified:
  - Levain/Features/Kefir/KefirBatchEditorView.swift
  - Levain/Features/Kefir/KefirBatchManageSheet.swift
  - Levain/Features/Kefir/KefirBatchDetailView.swift
  - Levain/Features/Kefir/KefirHubView.swift
  - Levain/Features/Preparations/PreparationsView.swift
  - Levain/App/AppRouter.swift
  - LevainUITests/KefirFlowUITests.swift
  - Levain.xcodeproj/project.pbxproj
autonomous: true
user_setup: []
must_haves:
  truths:
    - Users can create the first kefir batch directly from Preparazioni or the kefir empty state without creating a culture first.
    - Batch management supports renew, derive-new-batch, storage/state changes, and archive from the operational detail flow.
    - Derived-batch creation records source linkage now, while leaving richer lineage/journal UI for Phase 20.
  artifacts:
    - Levain/Features/Kefir/KefirBatchEditorView.swift
    - Levain/Features/Kefir/KefirBatchManageSheet.swift
    - LevainUITests/KefirFlowUITests.swift
  key_links:
    - Preparations quick actions and kefir empty-state CTAs route into the same no-culture-required batch editor flow.
    - KefirBatchDetailView exposes manage actions that mutate the persisted batch and can launch a prefilled derive flow.
    - AppRouter can open a kefir batch detail directly so creation and operational taps stay aligned with the broader v2 routing rule.
---

<objective>
Make the kefir vertical operational by adding first-batch creation and lightweight manage flows.

Purpose: The vertical is not usable until users can start a batch immediately and keep it alive through direct, low-friction actions.
Output: Batch editor, manage sheet/actions, derive-from-batch flow, and UI coverage for first-batch and management scenarios.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-milk-kefir-batch-core/19-CONTEXT.md
@.planning/phases/19-milk-kefir-batch-core/19-01-PLAN.md
@.planning/phases/19-milk-kefir-batch-core/19-02-PLAN.md
@Levain/Features/Kefir/KefirHubView.swift
@Levain/Features/Preparations/PreparationsView.swift
@Levain/App/AppRouter.swift
@Levain/Models/KefirBatch.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a no-culture-required batch editor for first and derived batch creation</name>
  <files>Levain/Features/Kefir/KefirBatchEditorView.swift, Levain/Features/Kefir/KefirHubView.swift, Levain/Features/Preparations/PreparationsView.swift, Levain/App/AppRouter.swift, Levain.xcodeproj/project.pbxproj</files>
  <action>Create the batch editor flow used by Preparazioni quick actions, kefir empty-state CTAs, and derive-new-batch entry. The editor should support a brand-new batch and a prefilled derived batch, require only the operational fields needed now, and never force culture linkage before the first batch is saved.</action>
  <verify>xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived CODE_SIGNING_ALLOWED=NO build</verify>
  <done>A user can start milk kefir tracking immediately from the root shell without extra prerequisite objects.</done>
</task>

<task type="auto">
  <name>Task 2: Add lightweight manage flows for renew, storage/state changes, derive, and archive</name>
  <files>Levain/Features/Kefir/KefirBatchManageSheet.swift, Levain/Features/Kefir/KefirBatchDetailView.swift</files>
  <action>Implement the operational batch actions as lightweight manage flows that update the persisted batch directly: renew/manage now, change storage/state, archive, and derive a new batch. Keep the UX sheet- or dialog-based if it preserves speed, and avoid sliding into event-history UI or culture management that belong to later phases.</action>
  <verify>Manual review confirms one dominant action remains obvious on detail and the secondary actions stay operational rather than form-heavy.</verify>
  <done>The batch detail can drive day-to-day kefir management without requiring journal screens or extra navigation.</done>
</task>

<task type="auto">
  <name>Task 3: Cover first-batch and manage flows with seeded UI scenarios</name>
  <files>LevainUITests/KefirFlowUITests.swift</files>
  <action>Expand the kefir UI suite so it verifies first-batch creation from an empty state, direct creation from Preparazioni, derived-batch creation from an existing batch, and at least one storage or archive management path. Use the existing launch-environment harness and seeded in-memory store patterns for determinism.</action>
  <verify>xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived -destination 'platform=iOS Simulator,name=iPhone 17 Pro' test -only-testing:LevainUITests/KefirFlowUITests</verify>
  <done>The core kefir creation/manage paths have regression coverage instead of relying on manual tapping only.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived CODE_SIGNING_ALLOWED=NO build`
- [ ] `xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived -destination 'platform=iOS Simulator,name=iPhone 17 Pro' test -only-testing:LevainUITests/KefirFlowUITests`
- [ ] Users can create the first batch without a culture and manage existing batches through renew, derive, storage/state change, and archive flows
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The kefir vertical now supports the essential operational lifecycle for a real batch-first workflow

</success_criteria>

<output>
After completion, create `.planning/phases/19-milk-kefir-batch-core/19-03-SUMMARY.md`
</output>
