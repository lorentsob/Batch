---
phase: 04-bake-creation-execution
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - Levain/Features/Bakes/BakeDetailView.swift
  - Levain/Features/Bakes/ShiftTimelineView.swift
  - Levain/Models/BakeStep.swift
  - Levain/Services/BakeScheduler.swift
  - LevainTests/BakeSchedulerTests.swift
  - LevainTests/TestSupport/DomainFixtures.swift
  - Levain.xcodeproj/project.pbxproj
autonomous: true
user_setup: []
must_haves:
  truths:
    - A running step shows timer guidance based on actual execution time and planned duration, but the timer never auto-completes the step.
    - Timeline shifting offers preset and custom minute offsets and changes only future incomplete steps while updating the bake target end accordingly.
    - Core bake-execution timing semantics are protected by focused automated tests rather than relying only on manual interaction.
  artifacts:
    - Levain/Features/Bakes/ShiftTimelineView.swift
    - Levain/Features/Bakes/BakeDetailView.swift
    - Levain/Services/BakeScheduler.swift
    - LevainTests/BakeSchedulerTests.swift
  key_links:
    - BakeDetailView presents timer guidance for running steps and opens ShiftTimelineView from the relevant step context.
    - ShiftTimelineView applies offsets through BakeScheduler.shiftFutureSteps and persists the changed schedule.
    - BakeSchedulerTests assert the invariants around shift scope, target-bake recalculation, and step timing helpers.
---

<objective>
Finish Phase 4 by making timing controls practical and trustworthy: the user can follow a running step, see time guidance, and shift the remaining schedule when reality slips.

Purpose: The app's core value depends on real-world flexibility, not just static schedule generation.
Output: Stronger timer UI, a deliberate shift-timeline flow, and focused automated coverage for bake-execution timing rules.
</objective>

<execution_context>
@/Users/lorentso/.codex/skills/gsd-workflow/references/get-shit-done/workflows/execute-plan.md
@/Users/lorentso/.codex/skills/gsd-workflow/references/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-bake-creation-execution/04-CONTEXT.md
@docs/levain-prd-complete-v2.md
@docs/UX-SPEC.md
@Levain/Features/Bakes/BakesView.swift
@Levain/Models/BakeStep.swift
@Levain/Services/BakeScheduler.swift
@Levain/Services/NotificationService.swift
@LevainTests/BakeSchedulerTests.swift
@LevainTests/TestSupport/DomainFixtures.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make timer guidance useful without changing step truth automatically</name>
  <files>Levain/Features/Bakes/BakeDetailView.swift, Levain/Models/BakeStep.swift</files>
  <action>Refine the running-step timer presentation so it clearly shows elapsed and remaining time using the actual start when present, while overdue or late remains a UI hint instead of a state mutation. Keep the timer as a helper, not an autonomous workflow engine: expiry can highlight urgency, but the step still requires an explicit user action to complete.</action>
  <verify>Manual review on a running step confirms the timer counts from actualStart, shows remaining guidance, and never flips the step to done by itself.</verify>
  <done>The timer supports execution confidence while preserving the product rule that the user stays in control of the real state.</done>
</task>

<task type="auto">
  <name>Task 2: Formalize timeline shifting around quick presets and custom offsets</name>
  <files>Levain/Features/Bakes/ShiftTimelineView.swift, Levain/Features/Bakes/BakeDetailView.swift, Levain/Services/BakeScheduler.swift, Levain.xcodeproj/project.pbxproj</files>
  <action>Move the shift flow into a dedicated view and align it with the PRD: preset offsets should cover the common delay cases, with a custom minute control available when needed. The implementation must only move future incomplete steps and their windows, update the bake target end from the new last step, and keep the existing notification resync hook clean for later Phase 5 reminder orchestration.</action>
  <verify>xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived CODE_SIGNING_ALLOWED=NO build</verify>
  <done>Timeline shifting becomes a first-class operational tool rather than an incidental inline control, and its scope stays aligned with the scheduling rules.</done>
</task>

<task type="auto">
  <name>Task 3: Expand automated coverage for bake timing and shift invariants</name>
  <files>LevainTests/BakeSchedulerTests.swift, LevainTests/TestSupport/DomainFixtures.swift, Levain/Services/BakeScheduler.swift, Levain/Models/BakeStep.swift</files>
  <action>Add focused tests around running-step progress, actual timestamp semantics, and timeline shifting so the most fragile timing rules are executable rather than implicit. Reuse the existing deterministic fixture approach, keep clock assumptions fixed, and cover the edge cases that would silently break the operational workflow if left to ad hoc manual testing.</action>
  <verify>xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived -destination 'platform=iOS Simulator,name=iPhone 17 Pro' test -only-testing:LevainTests/BakeSchedulerTests</verify>
  <done>The highest-risk phase-4 timing rules have automated regression coverage and no longer depend only on exploratory simulator runs.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived CODE_SIGNING_ALLOWED=NO build`
- [ ] `xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived -destination 'platform=iOS Simulator,name=iPhone 17 Pro' test -only-testing:LevainTests/BakeSchedulerTests`
- [ ] Running-step timer guidance and timeline shifting both respect the rule that planned versus actual timing remain distinct
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 4 closes with real timing controls and focused confidence around bake execution behavior
  </success_criteria>

<output>
After completion, create `.planning/phases/04-bake-creation-execution/04-03-SUMMARY.md`
</output>
