---
phase: 04-bake-creation-execution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Levain/Features/Bakes/BakesView.swift
  - Levain/Features/Bakes/BakeCreationView.swift
  - Levain/App/AppRouter.swift
  - Levain/Models/Bake.swift
  - Levain/Services/BakeScheduler.swift
  - Levain.xcodeproj/project.pbxproj
autonomous: true
user_setup: []
must_haves:
  truths:
    - The user can create a bake from a saved formula, choose an optional starter, and set a target bake date and time from a dedicated creation flow.
    - Generating a bake persists a real Bake record plus ordered BakeStep records derived from the selected formula instead of leaving the schedule implicit.
    - After creation, the app routes into the newly created bake detail and the bakes list stays scannable for planned versus in-progress work.
  artifacts:
    - Levain/Features/Bakes/BakeCreationView.swift
    - Levain/Features/Bakes/BakesView.swift
    - Levain/Services/BakeScheduler.swift
  key_links:
    - BakesView launches BakeCreationView from both the bakes area and formula-driven entry points.
    - BakeCreationView uses BakeScheduler.generateBake to materialize Bake and BakeStep entities in SwiftData.
    - AppRouter opens the newly created bake detail after save so creation flows directly into execution.
---

<objective>
Turn the current rough bake generator into a deliberate creation flow that produces a usable active bake from a reusable formula.

Purpose: Phase 4 starts when formulas stop being endpoints and become the source for real operational bakes the user can run.
Output: Dedicated bake-creation UI, cleaner routing into bake detail, and stronger persistence semantics around generated bakes.
</objective>

<execution_context>
@/Users/lorentso/.codex/skills/gsd-workflow/references/get-shit-done/workflows/execute-plan.md
@/Users/lorentso/.codex/skills/gsd-workflow/references/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-bake-creation-execution/04-CONTEXT.md
@docs/levain-prd-complete-v2.md
@docs/UX-SPEC.md
@Levain/App/AppRouter.swift
@Levain/Features/Bakes/BakesView.swift
@Levain/Models/Bake.swift
@Levain/Models/RecipeFormula.swift
@Levain/Models/Starter.swift
@Levain/Services/BakeScheduler.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract a dedicated bake-creation surface out of the monolithic Bakes screen</name>
  <files>Levain/Features/Bakes/BakesView.swift, Levain/Features/Bakes/BakeCreationView.swift, Levain.xcodeproj/project.pbxproj</files>
  <action>Move bake creation into its own SwiftUI file instead of keeping the whole flow embedded inside `BakesView.swift`. Keep the UX sheet-based per the spec, with the core fields always visible and optional starter or advanced sections progressively disclosed rather than cluttering the main form.</action>
  <verify>xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived CODE_SIGNING_ALLOWED=NO build</verify>
  <done>Bake creation lives in a dedicated file, BakesView becomes a thinner orchestration screen, and the new flow matches the roadmap's formula-first creation intent.</done>
</task>

<task type="auto">
  <name>Task 2: Make generation and save semantics explicit</name>
  <files>Levain/Features/Bakes/BakeCreationView.swift, Levain/Services/BakeScheduler.swift, Levain/Models/Bake.swift</files>
  <action>Refine the save path so choosing a formula, target bake time, and optional starter always generates a fully persisted Bake plus ordered BakeStep records with copied formula-derived metrics. Keep the creation flow pragmatic: allow a bake name override and notes, but avoid turning this into a heavyweight preflight editor before the user even starts execution.</action>
  <verify>Review the create flow: select formula -> choose target bake time -> optional starter -> create -> persisted bake has ordered steps and derived formula values.</verify>
  <done>Creating a bake consistently produces a usable persisted timeline instead of a loosely coupled draft object.</done>
</task>

<task type="auto">
  <name>Task 3: Route directly from creation into active bake review</name>
  <files>Levain/Features/Bakes/BakesView.swift, Levain/Features/Bakes/BakeCreationView.swift, Levain/App/AppRouter.swift</files>
  <action>After a successful creation, close the sheet cleanly and push the user into the new bake detail rather than dropping them back on the list with no confirmation. At the same time, keep the bakes list cards concise by surfacing only the schedule summary, derived status, and next actionable step preview that help the user re-enter a bake later.</action>
  <verify>Manual flow check: create a bake from the Bakes tab or from a formula context and confirm the app lands on the new Bake detail screen with the list updated behind it.</verify>
  <done>Creation feels like the start of a real workflow and the Bakes tab exposes planned versus in-progress bakes without becoming a detail dump.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived CODE_SIGNING_ALLOWED=NO build`
- [ ] Bake creation supports formula selection, target bake date/time, and optional starter selection in a dedicated sheet
- [ ] Saving a bake inserts Bake plus BakeStep records and routes into the created bake detail
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 4 has a stable bake-creation entry point that later execution work can build on
  </success_criteria>

<output>
After completion, create `.planning/phases/04-bake-creation-execution/04-01-SUMMARY.md`
</output>
