---
phase: 04-bake-creation-execution
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - Levain/Features/Bakes/BakesView.swift
  - Levain/Features/Bakes/BakeDetailView.swift
  - Levain/Features/Bakes/BakeStepCardView.swift
  - Levain/Features/Bakes/BakeStepDetailView.swift
  - Levain/Models/Bake.swift
  - Levain/Models/BakeStep.swift
  - Levain/App/AppRouter.swift
  - Levain.xcodeproj/project.pbxproj
autonomous: true
user_setup: []
must_haves:
  truths:
    - Bake detail shows large readable step cards with one obvious primary action for each step state.
    - Starting, completing, and skipping a step records actual timing separately from planned schedule values instead of mutating the schedule into the source of truth.
    - Bake detail surfaces linked formula, optional starter, and derived bake status without burying the active step workflow under secondary controls.
  artifacts:
    - Levain/Features/Bakes/BakeDetailView.swift
    - Levain/Features/Bakes/BakeStepCardView.swift
    - Levain/Models/BakeStep.swift
  key_links:
    - BakeDetailView renders the ordered BakeStep list through BakeStepCardView.
    - BakeStepCardView derives its primary CTA from BakeStep status and overdue state.
    - BakeStep mutations persist through modelContext saves while preserving planned timestamps and writing actualStart or actualEnd separately.
---

<objective>
Make bake detail the operational center of the app by turning step execution into a readable, state-driven workflow instead of a set of incidental buttons.

Purpose: The product promise depends on the user immediately understanding what to do next inside an active bake.
Output: Dedicated bake detail files, step-card execution UI, and cleaner separation between planned schedule data and real execution timestamps.
</objective>

<execution_context>
@/Users/lorentso/.codex/skills/gsd-workflow/references/get-shit-done/workflows/execute-plan.md
@/Users/lorentso/.codex/skills/gsd-workflow/references/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-bake-creation-execution/04-CONTEXT.md
@docs/levain-prd-complete-v2.md
@docs/UX-SPEC.md
@Levain/App/AppEnvironment.swift
@Levain/App/AppRouter.swift
@Levain/Features/Bakes/BakesView.swift
@Levain/Models/Bake.swift
@Levain/Models/BakeStep.swift
@Levain/Services/DateFormattingService.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract bake detail into focused view files and card components</name>
  <files>Levain/Features/Bakes/BakesView.swift, Levain/Features/Bakes/BakeDetailView.swift, Levain/Features/Bakes/BakeStepCardView.swift, Levain/Features/Bakes/BakeStepDetailView.swift, Levain.xcodeproj/project.pbxproj</files>
  <action>Split the bake detail implementation out of `BakesView.swift` so the main execution surface can evolve independently from creation and list concerns. Model the step list around large cards consistent with the UX spec, and keep any secondary inspection details in a subordinate detail sheet or view instead of overloading the card body.</action>
  <verify>xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived CODE_SIGNING_ALLOWED=NO build</verify>
  <done>Bake detail and step cards live in dedicated files, and the Bakes feature is no longer forced to grow inside one oversized source file.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce step execution semantics around actual timing</name>
  <files>Levain/Features/Bakes/BakeDetailView.swift, Levain/Features/Bakes/BakeStepCardView.swift, Levain/Models/BakeStep.swift, Levain/Models/Bake.swift</files>
  <action>Refine the start, complete, and skip actions so they clearly reflect the state machine from the PRD: pending -> running -> done or skipped, with overdue remaining a derived label instead of a logical status. Preserve planned schedule fields as historical planning data, and write actual start or end timestamps separately so future Today and notification work can trust both timelines.</action>
  <verify>Review the execution flow on one bake: pending step starts, running step completes, skipped step becomes terminal, and planned timestamps remain unchanged while actual timing fields update.</verify>
  <done>The execution model is explicit, durable, and no longer conflates the planned schedule with what happened in reality.</done>
</task>

<task type="auto">
  <name>Task 3: Surface only the metadata that helps active execution</name>
  <files>Levain/Features/Bakes/BakeDetailView.swift, Levain/Features/Bakes/BakeStepCardView.swift, Levain/App/AppRouter.swift</files>
  <action>Use the header and card metadata to show only what helps the user act now: bake status, target bake time, linked formula or starter, step timing context, and qualitative targets when present. Keep one primary action per card state, move extra inspection to a secondary detail surface, and avoid reintroducing a dense recipe-style layout that fights the planner-first product direction.</action>
  <verify>Manual review of the bake detail screen confirms each step card has a single main CTA and the header exposes formula or starter links without burying the active step context.</verify>
  <done>Bake detail reads like an operational workflow, not a crowded data sheet, and supports BAKE-04 plus STEP-01 cleanly.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived CODE_SIGNING_ALLOWED=NO build`
- [ ] Bake detail renders readable ordered step cards with one primary action per state
- [ ] Step execution preserves planned timing while persisting actual start or end timestamps separately
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Bake detail becomes the stable execution surface the roadmap expects for active bakes
  </success_criteria>

<output>
After completion, create `.planning/phases/04-bake-creation-execution/04-02-SUMMARY.md`
</output>
