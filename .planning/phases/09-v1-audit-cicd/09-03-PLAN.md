---
phase: 09-v1-audit-cicd
plan: 03
type: execute
wave: 3
depends_on: ["09-01", "09-02"]
files_modified:
  - .github/workflows/ios-release.yml
  - scripts/ci_release.sh
  - docs/release-runbook.md
  - docs/release-secrets.md
  - docs/release-checklist.md
autonomous: true
user_setup: []
must_haves:
  truths:
    - CD remains manual-triggered and secret-backed so signed distribution happens intentionally, not on every push.
    - Release automation reuses the same build assumptions validated by CI instead of inventing a parallel archive flow.
    - Signing inputs, App Store Connect credentials, and operator steps are documented explicitly so delivery is not tribal knowledge.
  artifacts:
    - .github/workflows/ios-release.yml
    - scripts/ci_release.sh
    - docs/release-runbook.md
    - docs/release-secrets.md
  key_links:
    - The release workflow should start from the same generated project and scheme assumptions already proven in CI.
    - Delivery automation must separate dry-run validation from secret-backed upload so the unsafe parts stay deliberate and reviewable.
    - The runbook should link the release checklist, required secrets, versioning expectations, and recovery steps in one place.
---

<objective>
Implement the controlled CD path so a v1 release candidate can be created repeatably without manual command archaeology.

Purpose: CI proves changes are healthy, but v1 still needs a deliberate path to produce and distribute a release candidate with documented secrets and operator steps.
Output: A manual-triggered release workflow, repo-local release script with dry-run support, and a concrete release runbook.
</objective>

<execution_context>
@/Users/lorentso/.codex/skills/gsd-workflow/references/get-shit-done/workflows/execute-plan.md
@/Users/lorentso/.codex/skills/gsd-workflow/references/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-v1-audit-cicd/09-CONTEXT.md
@project.yml
@Levain.xcodeproj/project.pbxproj
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the manual-triggered release workflow and local release script</name>
  <files>.github/workflows/ios-release.yml, scripts/ci_release.sh, project.yml</files>
  <action>Add a release workflow that runs only through explicit dispatch, prepares the project, builds the release configuration, and can proceed to signed distribution when the required secrets are present. Pair it with a repo-local script so maintainers can dry-run the same flow without editing workflow YAML by hand.</action>
  <verify>`bash scripts/ci_release.sh --dry-run` completes the non-secret parts of the release flow and documents what would require signing credentials.</verify>
  <done>The repository has one controlled entry point for delivery automation instead of scattered one-off release commands.</done>
</task>

<task type="auto">
  <name>Task 2: Define the secrets, signing contract, and failure handling explicitly</name>
  <files>docs/release-secrets.md, docs/release-runbook.md, .github/workflows/ios-release.yml</files>
  <action>Document every signing and App Store Connect input the release workflow needs, how it should be stored, and what failures mean. Do not leave certificates, provisioning, API-key shape, or recovery steps implied; CD is only useful if another maintainer can understand and operate it safely.</action>
  <verify>Manual review confirms the workflow inputs and the secrets document describe the same contract with no hidden dependencies.</verify>
  <done>The delivery path is understandable and operable instead of depending on personal setup knowledge.</done>
</task>

<task type="auto">
  <name>Task 3: Finish the release runbook and checklist around the automated path</name>
  <files>docs/release-runbook.md, docs/release-checklist.md, docs/v1-audit.md</files>
  <action>Write the end-to-end operator runbook for preparing a release candidate, running the final audit gate, triggering the workflow, validating the uploaded build, and responding to failures. Keep the checklist short and operational so it becomes the standard path for every future release candidate.</action>
  <verify>Manual review confirms the runbook links the audit gate, workflow trigger, post-upload validation, and rollback or retry steps coherently.</verify>
  <done>Phase 9 ends with a repeatable release procedure rather than a one-time setup that will be forgotten.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bash scripts/ci_release.sh --dry-run`
- [ ] `xcodebuild -project Levain.xcodeproj -scheme Levain -configuration Release -destination 'generic/platform=iOS' CODE_SIGNING_ALLOWED=NO build`
- [ ] Release workflow is manual-triggered, reuses CI assumptions, and documents every required secret and operator step
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No undocumented signing or distribution assumptions remain
- The repository can produce controlled release candidates through a documented CD path
  </success_criteria>

<output>
After completion, create `.planning/phases/09-v1-audit-cicd/09-03-SUMMARY.md`
</output>
