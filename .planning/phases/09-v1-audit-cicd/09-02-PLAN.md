---
phase: 09-v1-audit-cicd
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - .github/workflows/ios-ci.yml
  - scripts/ci_bootstrap.sh
  - scripts/ci_test.sh
  - project.yml
  - docs/ci-cd.md
autonomous: true
user_setup: []
must_haves:
  truths:
    - CI runs on clean macOS runners using the same XcodeGen and `xcodebuild` flow the repo uses locally.
    - Build and test failures are actionable because the workflow preserves enough logs or artifacts instead of only returning a red check.
    - Hosted validation stays deterministic by making simulator target, scheme, and project-generation behavior explicit.
  artifacts:
    - .github/workflows/ios-ci.yml
    - scripts/ci_bootstrap.sh
    - scripts/ci_test.sh
    - docs/ci-cd.md
  key_links:
    - `project.yml` remains the source of truth, so CI must generate or validate the project before invoking `xcodebuild`.
    - CI should keep the fast-fail build path and the higher-confidence automated test path explicit instead of hiding both inside one opaque step.
    - Repository-local scripts make hosted automation and local reproduction converge on the same commands.
---

<objective>
Implement the CI baseline so every future change is validated in a clean hosted environment instead of only on the maintainer's machine.

Purpose: v1 confidence is fragile if build and test success can only be reproduced locally.
Output: Repo-local CI scripts, a GitHub Actions workflow for build and test validation, and documentation for local reproduction and branch expectations.
</objective>

<execution_context>
@/Users/lorentso/.codex/skills/gsd-workflow/references/get-shit-done/workflows/execute-plan.md
@/Users/lorentso/.codex/skills/gsd-workflow/references/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-v1-audit-cicd/09-CONTEXT.md
@project.yml
@Levain.xcodeproj/project.pbxproj
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create repo-local CI bootstrap and test scripts</name>
  <files>scripts/ci_bootstrap.sh, scripts/ci_test.sh, project.yml</files>
  <action>Codify the project generation, simulator selection, build, and automated test commands in versioned scripts so local reproduction and hosted CI do not drift. Keep the scripts narrow and readable; they should expose the exact contract CI depends on instead of hiding important assumptions.</action>
  <verify>`bash scripts/ci_bootstrap.sh` and `bash scripts/ci_test.sh` complete successfully on a clean local environment with the expected toolchain installed.</verify>
  <done>The project has one repeatable CI command surface instead of undocumented terminal history.</done>
</task>

<task type="auto">
  <name>Task 2: Add a GitHub Actions workflow for build and automated test validation</name>
  <files>.github/workflows/ios-ci.yml, scripts/ci_bootstrap.sh, scripts/ci_test.sh</files>
  <action>Create the hosted CI workflow for pull requests and protected branch updates. The workflow should install or prepare the required XcodeGen tooling, run the repo-local scripts, and publish enough artifacts to debug failures without guessing. Do not over-automate release logic here; this workflow exists to validate changes, not distribute builds.</action>
  <verify>Manual review confirms the workflow triggers on the intended events, uses repository-local scripts, and preserves logs or `xcresult` output for failures.</verify>
  <done>Every important change path has clean-runner build and test validation attached to it.</done>
</task>

<task type="auto">
  <name>Task 3: Document how to reproduce CI locally and how the gate should be used</name>
  <files>docs/ci-cd.md, .github/workflows/ios-ci.yml</files>
  <action>Document the workflow purpose, local reproduction commands, expected secrets boundary, and the intended branch-protection role of the CI check. Keep the document operational: someone maintaining the repo should know exactly how to run the same checks locally and what a failing workflow means.</action>
  <verify>Manual review confirms the documentation maps directly to the committed scripts and workflow names.</verify>
  <done>The CI gate is maintainable because its contract is written down next to the implementation.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bash scripts/ci_bootstrap.sh`
- [ ] `bash scripts/ci_test.sh`
- [ ] CI workflow is wired to repository-local scripts and preserves actionable failure artifacts
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No drift between local validation commands and hosted workflow steps
- The repository has a real CI gate instead of local-only verification
  </success_criteria>

<output>
After completion, create `.planning/phases/09-v1-audit-cicd/09-02-SUMMARY.md`
</output>
