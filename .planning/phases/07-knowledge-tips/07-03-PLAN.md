---
phase: 07-knowledge-tips
plan: 03
type: execute
wave: 3
depends_on: ["07-01", "07-02"]
files_modified:
  - Levain/Features/Shared/TipGroupView.swift
  - Levain/Features/Bakes/BakeDetailView.swift
  - Levain/Features/Starter/StarterDetailView.swift
  - Levain/Services/KnowledgeLoader.swift
  - LevainTests/KnowledgeLibraryTests.swift
  - LevainTests/TestSupport/DomainFixtures.swift
  - Levain.xcodeproj/project.pbxproj
autonomous: true
user_setup: []
must_haves:
  truths:
    - Relevant knowledge tips appear inside bake and starter workflows based on persisted step type or starter due-state context.
    - Contextual tip surfacing stays supportive and lightweight instead of competing with the primary operational action on the screen.
    - Opening a contextual tip lands in the same Knowledge article detail flow used by the Knowledge tab, not in a duplicate content surface.
  artifacts:
    - Levain/Features/Shared/TipGroupView.swift
    - Levain/Services/KnowledgeLoader.swift
    - LevainTests/KnowledgeLibraryTests.swift
  key_links:
    - KnowledgeLibrary filters bundled KnowledgeItem values by step type and starter due-state for contextual usage.
    - BakeDetailView and StarterDetailView render TipGroupView from the contextual knowledge results relevant to their current state.
    - TipGroupView opens the shared Knowledge article route so browse and contextual entry points converge on the same article surfaces.
---

<objective>
Close Phase 7 by making the bundled knowledge useful at the point of work, not only as a separate browse tab.

Purpose: Contextual tips are what connect static knowledge to the operational workflows the app is built around.
Output: Refined contextual tip filtering, lightweight in-flow tip presentation, and focused tests around KnowledgeLibrary relevance behavior.
</objective>

<execution_context>
@/Users/lorentso/.codex/skills/gsd-workflow/references/get-shit-done/workflows/execute-plan.md
@/Users/lorentso/.codex/skills/gsd-workflow/references/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-knowledge-tips/07-CONTEXT.md
@docs/levain-prd-complete-v2.md
@docs/UX-SPEC.md
@Levain/Features/Shared/TipGroupView.swift
@Levain/Features/Bakes/BakeDetailView.swift
@Levain/Features/Starter/StarterDetailView.swift
@Levain/Services/KnowledgeLoader.swift
@Levain/Models/KnowledgeItem.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strengthen contextual tip filtering and add focused KnowledgeLibrary tests</name>
  <files>Levain/Services/KnowledgeLoader.swift, LevainTests/KnowledgeLibraryTests.swift, LevainTests/TestSupport/DomainFixtures.swift, Levain.xcodeproj/project.pbxproj</files>
  <action>Refine the library helper methods so contextual tip selection for step types and starter due-states is deliberate, deterministic, and easy to test. Add focused tests around the relevance filters rather than relying on manual visual inspection inside bake or starter screens to catch content-mapping regressions.</action>
  <verify>xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived -destination 'platform=iOS Simulator,name=iPhone 17 Pro' test -only-testing:LevainTests/KnowledgeLibraryTests</verify>
  <done>Contextual knowledge filtering is covered by a dedicated test suite and remains stable as content evolves.</done>
</task>

<task type="auto">
  <name>Task 2: Keep in-flow tips supportive inside bake and starter detail surfaces</name>
  <files>Levain/Features/Shared/TipGroupView.swift, Levain/Features/Bakes/BakeDetailView.swift, Levain/Features/Starter/StarterDetailView.swift</files>
  <action>Adjust the tip group presentation so it supports the current workflow without competing with the primary action on bake and starter detail screens. Keep the tip surface short, readable, and clearly secondary; the purpose is to help the user when context suggests it, not to derail the operational screen into a reading view.</action>
  <verify>Manual review confirms contextual tips remain lightweight and do not displace the main action controls in bake and starter detail flows.</verify>
  <done>Knowledge tips are present at the right moments but remain subordinate to the operational job-to-be-done on each screen.</done>
</task>

<task type="auto">
  <name>Task 3: Converge contextual tips and browse flow on one article destination</name>
  <files>Levain/Features/Shared/TipGroupView.swift, Levain/Features/Bakes/BakeDetailView.swift, Levain/Features/Starter/StarterDetailView.swift, Levain/Services/KnowledgeLoader.swift</files>
  <action>Ensure that opening a contextual tip always lands in the shared Knowledge article route already used by the Knowledge tab, instead of creating a second inline article paradigm for tips. This keeps content ownership, article rendering, and navigation semantics consistent across Phase 7.</action>
  <verify>xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived CODE_SIGNING_ALLOWED=NO build</verify>
  <done>KNOW-03 is fulfilled through one coherent content and navigation model shared between browsing and contextual entry points.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived CODE_SIGNING_ALLOWED=NO build`
- [ ] `xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived -destination 'platform=iOS Simulator,name=iPhone 17 Pro' test -only-testing:LevainTests/KnowledgeLibraryTests`
- [ ] Contextual tips appear in bake and starter flows while opening the shared Knowledge article destination
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 7 closes with bundled content that is both browsable and genuinely useful inside operational workflows
  </success_criteria>

<output>
After completion, create `.planning/phases/07-knowledge-tips/07-03-SUMMARY.md`
</output>
