---
phase: 07-knowledge-tips
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Levain/Resources/knowledge.json
  - Levain/Services/KnowledgeLoader.swift
  - Levain/Models/KnowledgeItem.swift
  - Levain/Models/DomainEnums.swift
  - LevainTests/KnowledgeLoaderTests.swift
  - Levain.xcodeproj/project.pbxproj
autonomous: true
user_setup: []
must_haves:
  truths:
    - The app ships a real bundled knowledge dataset as local JSON and can decode it offline at launch without depending on SwiftData or network calls.
    - Knowledge items expose enough stable metadata for both article browsing and contextual tip filtering in bake and starter flows.
    - The knowledge schema remains intentionally lightweight instead of becoming a second content-management system inside the app.
  artifacts:
    - Levain/Resources/knowledge.json
    - Levain/Services/KnowledgeLoader.swift
    - Levain/Models/KnowledgeItem.swift
    - LevainTests/KnowledgeLoaderTests.swift
  key_links:
    - KnowledgeLoader decodes the bundled JSON into KnowledgeItem values deterministically.
    - KnowledgeItem schema supports category browsing plus `relatedStepTypes` and `relatedStarterStates` filtering for contextual tips.
    - KnowledgeLoaderTests validate decode success and the core filtering-ready fields in the bundled content.
---

<objective>
Establish the bundled knowledge source of truth so the app can ship a reliable offline content set before refining browsing or contextual surfacing.

Purpose: Phase 7 starts with the content contract, because both the Knowledge tab and in-flow tips depend on a stable schema and bundled dataset.
Output: Hardened JSON bundle, clearer knowledge model and loader semantics, and focused tests for offline content loading.
</objective>

<execution_context>
@/Users/lorentso/.codex/skills/gsd-workflow/references/get-shit-done/workflows/execute-plan.md
@/Users/lorentso/.codex/skills/gsd-workflow/references/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-knowledge-tips/07-CONTEXT.md
@docs/levain-prd-complete-v2.md
@docs/levain-knowledge.md
@Levain/Resources/knowledge.json
@Levain/Services/KnowledgeLoader.swift
@Levain/Models/KnowledgeItem.swift
@Levain/Models/DomainEnums.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden the bundled knowledge schema and dataset for offline use</name>
  <files>Levain/Resources/knowledge.json, Levain/Models/KnowledgeItem.swift, Levain/Models/DomainEnums.swift</files>
  <action>Review the current bundled content and schema so the dataset is coherent, category coverage is intentional, and each item carries the metadata needed for both browsing and contextual tips. Keep the schema lean: title, summary, content, tags, category, and the relationship fields should be enough for v1 without creating editorial complexity that the app does not need.</action>
  <verify>Confirm the bundled JSON decodes into the KnowledgeItem model and still maps cleanly to the existing category and related-step or related-starter enums.</verify>
  <done>The knowledge bundle is a real offline source of truth with stable fields for browsing and in-flow tip relevance.</done>
</task>

<task type="auto">
  <name>Task 2: Make loading behavior explicit and testable</name>
  <files>Levain/Services/KnowledgeLoader.swift, LevainTests/KnowledgeLoaderTests.swift, Levain.xcodeproj/project.pbxproj</files>
  <action>Refine KnowledgeLoader and KnowledgeLibrary so bundled loading behavior, cache semantics, and empty-fallback behavior are explicit rather than just implicit best effort. Add narrow tests around decode success and the baseline item set so future content or schema edits fail loudly when they break the app's offline knowledge contract.</action>
  <verify>xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived -destination 'platform=iOS Simulator,name=iPhone 17 Pro' test -only-testing:LevainTests/KnowledgeLoaderTests</verify>
  <done>Knowledge loading is deterministic, offline, and backed by focused tests instead of relying on manual browsing to catch schema drift.</done>
</task>

<task type="auto">
  <name>Task 3: Keep the content layer lightweight and future-proof for Phase 7 usage</name>
  <files>Levain/Resources/knowledge.json, Levain/Services/KnowledgeLoader.swift, Levain/Models/KnowledgeItem.swift</files>
  <action>Preserve the project decision that knowledge stays bundled JSON rather than SwiftData. Do not introduce edit-time persistence, migrations, or local authoring flows here; the only goal is to provide a stable, shippable content layer the browse and tip surfaces can trust.</action>
  <verify>xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived CODE_SIGNING_ALLOWED=NO build</verify>
  <done>The knowledge data layer remains minimal, shippable, and aligned with KNOW-01.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived CODE_SIGNING_ALLOWED=NO build`
- [ ] `xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived -destination 'platform=iOS Simulator,name=iPhone 17 Pro' test -only-testing:LevainTests/KnowledgeLoaderTests`
- [ ] Bundled knowledge content loads offline into a stable schema that both browsing and contextual tips can use
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- KNOW-01 has a stable offline data foundation for the rest of Phase 7
  </success_criteria>

<output>
After completion, create `.planning/phases/07-knowledge-tips/07-01-SUMMARY.md`
</output>
