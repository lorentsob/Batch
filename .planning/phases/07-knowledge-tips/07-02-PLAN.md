---
phase: 07-knowledge-tips
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - Levain/Features/Knowledge/KnowledgeView.swift
  - Levain/Features/Knowledge/KnowledgeDetailView.swift
  - Levain/Features/Knowledge/KnowledgeRowView.swift
  - Levain/Features/Knowledge/KnowledgeCategoryPillView.swift
  - Levain/Services/KnowledgeLoader.swift
  - Levain.xcodeproj/project.pbxproj
autonomous: true
user_setup: []
must_haves:
  truths:
    - Users can browse the bundled knowledge library by category and article through a lightweight, offline-first Knowledge tab.
    - The Knowledge tab supports filtering and search without turning into a dense reference database UI that fights the app's planner-first nature.
    - Article detail presents readable long-form content with the category and tags needed for orientation, while remaining visually consistent with the rest of the app.
  artifacts:
    - Levain/Features/Knowledge/KnowledgeView.swift
    - Levain/Features/Knowledge/KnowledgeDetailView.swift
    - Levain/Features/Knowledge/KnowledgeRowView.swift
  key_links:
    - KnowledgeView uses KnowledgeLibrary as its source of truth and does not duplicate loading logic inline.
    - KnowledgeCategoryPillView and KnowledgeRowView keep filtering and browsing UI modular.
    - KnowledgeDetailView renders bundled article content from KnowledgeItem values opened through router navigation.
---

<objective>
Refine the Knowledge tab into a deliberate lightweight browsing experience that makes the bundled content discoverable without overpowering the operational parts of the app.

Purpose: The app needs a usable browse surface for offline reference content before contextual tips can rely on users jumping into article detail naturally.
Output: Modular Knowledge tab UI, category or search browsing, and a cleaner article detail presentation.
</objective>

<execution_context>
@/Users/lorentso/.codex/skills/gsd-workflow/references/get-shit-done/workflows/execute-plan.md
@/Users/lorentso/.codex/skills/gsd-workflow/references/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-knowledge-tips/07-CONTEXT.md
@docs/levain-prd-complete-v2.md
@docs/UX-SPEC.md
@Levain/Features/Knowledge/KnowledgeView.swift
@Levain/Services/KnowledgeLoader.swift
@Levain/Models/KnowledgeItem.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract modular browse components out of KnowledgeView</name>
  <files>Levain/Features/Knowledge/KnowledgeView.swift, Levain/Features/Knowledge/KnowledgeRowView.swift, Levain/Features/Knowledge/KnowledgeCategoryPillView.swift, Levain.xcodeproj/project.pbxproj</files>
  <action>Split the current Knowledge tab into dedicated browse components so the root screen no longer owns filtering logic, row rendering, and visual treatment all in one file. Keep the interface intentionally lightweight: category filtering, optional search, and article rows are enough for v1 without inventing a heavy documentation UI.</action>
  <verify>xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived CODE_SIGNING_ALLOWED=NO build</verify>
  <done>The Knowledge tab lives in modular files and supports lightweight category-driven browsing that matches the UX spec.</done>
</task>

<task type="auto">
  <name>Task 2: Improve article detail readability without bloating the browse surface</name>
  <files>Levain/Features/Knowledge/KnowledgeDetailView.swift, Levain/Features/Knowledge/KnowledgeView.swift, Levain/Models/KnowledgeItem.swift, Levain.xcodeproj/project.pbxproj</files>
  <action>Move article detail into its own file and present category, title, body, and tags in a more readable hierarchy than the current generic cards. Keep the list screen compact and let long-form article readability live in the detail screen, consistent with the minimal operational design language of the app.</action>
  <verify>Manual review confirms the Knowledge root remains compact while article detail presents bundled content in a clear reading layout.</verify>
  <done>Knowledge browsing feels intentional and KNOW-02 is supported by a clear root-to-article flow.</done>
</task>

<task type="auto">
  <name>Task 3: Keep browse behavior offline and tied to the bundled library</name>
  <files>Levain/Features/Knowledge/KnowledgeView.swift, Levain/Services/KnowledgeLoader.swift</files>
  <action>Ensure the Knowledge tab continues to use the bundled library as its only source of truth, with no placeholder network or persistence paths sneaking in through convenience code. The result should feel robust even on first launch and without prior interaction, because the whole point of Phase 7 is reliable offline reference content.</action>
  <verify>xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived CODE_SIGNING_ALLOWED=NO build</verify>
  <done>The browse experience remains fully offline and consistent with the app's bundled-content decision.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived CODE_SIGNING_ALLOWED=NO build`
- [ ] Knowledge tab supports category browsing and article detail from bundled data
- [ ] Article presentation is readable while the root screen stays lightweight
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- KNOW-02 is implemented with a clear, offline, lightweight browsing experience
  </success_criteria>

<output>
After completion, create `.planning/phases/07-knowledge-tips/07-02-SUMMARY.md`
</output>
