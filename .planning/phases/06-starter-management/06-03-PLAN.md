---
phase: 06-starter-management
plan: 03
type: execute
wave: 3
depends_on: ["06-02"]
files_modified:
  - Levain/Services/NotificationService.swift
  - Levain/Services/StarterReminderPlanner.swift
  - Levain/Features/Starter/StarterEditorView.swift
  - Levain/Features/Starter/RefreshLogView.swift
  - Levain/Services/TodayAgendaBuilder.swift
  - Levain/Features/Today/TodayView.swift
  - LevainTests/StarterReminderPlannerTests.swift
  - LevainTests/TestSupport/DomainFixtures.swift
  - Levain.xcodeproj/project.pbxproj
autonomous: true
user_setup: []
must_haves:
  truths:
    - Starter reminders can be enabled or disabled per starter profile without affecting the underlying due-state logic shown in the app.
    - Local notifications schedule a due reminder and an overdue follow-up from persisted starter data, and they resync when starter settings or refresh history change.
    - Today continues to surface due or overdue starter work consistently while notification scheduling follows the reminder preference and route contract.
  artifacts:
    - Levain/Services/NotificationService.swift
    - Levain/Services/StarterReminderPlanner.swift
    - LevainTests/StarterReminderPlannerTests.swift
  key_links:
    - StarterEditorView and RefreshLogView trigger starter reminder resync after profile or maintenance changes.
    - NotificationService uses StarterReminderPlanner to derive due and follow-up reminder requests deterministically.
    - TodayAgendaBuilder still surfaces due starter work from Starter derived state while notification scheduling respects remindersEnabled.
---

<objective>
Finish Phase 6 by making starter reminders reliable, preference-aware, and aligned with both Today and the local notification layer.

Purpose: The secondary starter workflow only feels complete once due reminders stay in sync with profile settings and logged refreshes.
Output: Deterministic starter reminder planning, unified resync hooks, and consistent Today versus notification behavior.
</objective>

<execution_context>
@/Users/lorentso/.codex/skills/gsd-workflow/references/get-shit-done/workflows/execute-plan.md
@/Users/lorentso/.codex/skills/gsd-workflow/references/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-starter-management/06-CONTEXT.md
@docs/levain-prd-complete-v2.md
@docs/UX-SPEC.md
@Levain/Services/NotificationService.swift
@Levain/Services/TodayAgendaBuilder.swift
@Levain/Features/Starter/StarterView.swift
@Levain/Models/Starter.swift
@Levain/Features/Today/TodayView.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract deterministic starter reminder planning from NotificationService</name>
  <files>Levain/Services/NotificationService.swift, Levain/Services/StarterReminderPlanner.swift, LevainTests/StarterReminderPlannerTests.swift, LevainTests/TestSupport/DomainFixtures.swift, Levain.xcodeproj/project.pbxproj</files>
  <action>Move starter reminder derivation into a pure planner helper so due reminders and next-day follow-ups can be tested without depending on notification center side effects. Keep the planner grounded in starter profile data, `lastRefresh`, `refreshIntervalDays`, and `remindersEnabled`; do not introduce a second reminder state model beyond what the product actually needs.</action>
  <verify>xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived -destination 'platform=iOS Simulator,name=iPhone 17 Pro' test -only-testing:LevainTests/StarterReminderPlannerTests</verify>
  <done>Starter reminder scheduling is deterministic, testable, and explicit about the due plus follow-up policy required by NOTIF-03.</done>
</task>

<task type="auto">
  <name>Task 2: Resync starter reminders from the real mutation points</name>
  <files>Levain/Services/NotificationService.swift, Levain/Features/Starter/StarterEditorView.swift, Levain/Features/Starter/RefreshLogView.swift</files>
  <action>Ensure starter reminder resync happens when a starter is created, edited, has reminders toggled, or receives a new refresh log. Keep the sync behavior centralized so starter views are not each hand-building notification requests, and make sure disabling reminders clears scheduled requests cleanly instead of leaving stale alerts behind.</action>
  <verify>Manual code-path review confirms starter create, edit, reminder-toggle, and refresh-save flows all trigger a consistent starter reminder sync or clear path.</verify>
  <done>STARTER-04 and the scheduling half of NOTIF-03 are implemented through one durable resync model instead of scattered ad hoc calls.</done>
</task>

<task type="auto">
  <name>Task 3: Keep Today and starter reminders aligned without coupling visibility to notification preference</name>
  <files>Levain/Services/TodayAgendaBuilder.swift, Levain/Features/Today/TodayView.swift, Levain/Services/NotificationService.swift</files>
  <action>Review starter reminder behavior across Today and notifications so due or overdue starters still appear in the operational Today experience even when notifications are disabled, while scheduled local alerts honor the user's reminder preference. Preserve the action-first Today flow from Phase 5 rather than turning reminder preferences into a visibility toggle for actual maintenance work.</action>
  <verify>xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived CODE_SIGNING_ALLOWED=NO build</verify>
  <done>Starter work remains visible in Today when it is operationally relevant, and notification scheduling stays correctly gated by `remindersEnabled`.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived CODE_SIGNING_ALLOWED=NO build`
- [ ] `xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived -destination 'platform=iOS Simulator,name=iPhone 17 Pro' test -only-testing:LevainTests/StarterReminderPlannerTests`
- [ ] Starter reminders resync from persisted data changes while Today still surfaces due starter work consistently
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 6 closes with starter CRUD, refresh logging, due-state derivation, and reminder integration working as one coherent secondary workflow
  </success_criteria>

<output>
After completion, create `.planning/phases/06-starter-management/06-03-SUMMARY.md`
</output>
