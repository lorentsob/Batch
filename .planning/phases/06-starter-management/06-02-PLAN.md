---
phase: 06-starter-management
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - Levain/Features/Starter/StarterDetailView.swift
  - Levain/Features/Starter/RefreshLogView.swift
  - Levain/Features/Starter/RefreshHistoryRow.swift
  - Levain/Models/Starter.swift
  - Levain/Models/StarterRefresh.swift
  - LevainTests/StarterTests.swift
  - LevainTests/TestSupport/DomainFixtures.swift
  - Levain.xcodeproj/project.pbxproj
autonomous: true
user_setup: []
must_haves:
  truths:
    - Users can log a starter refresh quickly from starter detail or Today-context entry points without wading through an oversized form.
    - Saving a refresh updates `lastRefresh`, appends a persisted StarterRefresh record, and keeps due-state derivation based on data instead of a separate state machine.
    - Starter due-state and refresh history semantics are protected by focused automated tests rather than only manual checks.
  artifacts:
    - Levain/Features/Starter/RefreshLogView.swift
    - Levain/Models/Starter.swift
    - LevainTests/StarterTests.swift
  key_links:
    - StarterDetailView presents RefreshLogView as the quick maintenance action for a starter.
    - RefreshLogView writes StarterRefresh records and updates Starter.lastRefresh in one persisted flow.
    - StarterTests verify nextDueDate, dueState, and refresh logging semantics against deterministic fixture data.
---

<objective>
Make refresh logging fast and trustworthy so starter maintenance stays lightweight while due-state logic remains data-driven and testable.

Purpose: Starter management is incomplete until the user can record a real refresh quickly and trust the resulting due-state calculations.
Output: Dedicated refresh log UI, clearer history rendering, and focused automated coverage for starter timing semantics.
</objective>

<execution_context>
@/Users/lorentso/.codex/skills/gsd-workflow/references/get-shit-done/workflows/execute-plan.md
@/Users/lorentso/.codex/skills/gsd-workflow/references/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-starter-management/06-CONTEXT.md
@docs/levain-prd-complete-v2.md
@docs/UX-SPEC.md
@Levain/Features/Starter/StarterView.swift
@Levain/Models/Starter.swift
@Levain/Models/StarterRefresh.swift
@LevainTests/TestSupport/DomainFixtures.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract a quick starter refresh logging surface and history components</name>
  <files>Levain/Features/Starter/StarterDetailView.swift, Levain/Features/Starter/RefreshLogView.swift, Levain/Features/Starter/RefreshHistoryRow.swift, Levain.xcodeproj/project.pbxproj</files>
  <action>Move refresh logging and refresh-history presentation into dedicated files so the detail screen can stay focused on maintenance context while the refresh flow stays fast. Keep the first-screen fields limited to the essential weights, date or time, and ratio preview, with more advanced details secondary per the PRD and UX spec.</action>
  <verify>xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived CODE_SIGNING_ALLOWED=NO build</verify>
  <done>Refresh logging and refresh-history rows live in dedicated files, and the starter detail screen presents them without becoming a monolithic maintenance form.</done>
</task>

<task type="auto">
  <name>Task 2: Preserve derived starter state by updating persisted refresh data, not by hard-coding statuses</name>
  <files>Levain/Features/Starter/RefreshLogView.swift, Levain/Models/Starter.swift, Levain/Models/StarterRefresh.swift, Levain/Features/Starter/StarterDetailView.swift</files>
  <action>Refine the refresh save path so logging a refresh updates Starter.lastRefresh, persists the StarterRefresh record, and lets nextDueDate plus dueState continue to derive from those base fields. Avoid special-case booleans or hand-maintained status flags; the starter workflow should remain simpler than a state machine even as the UI gets richer.</action>
  <verify>Manual flow review confirms a saved refresh updates the visible recent history and moves the starter due-state based on the new `lastRefresh` value.</verify>
  <done>Refresh logging reliably changes the starter's derived maintenance state without introducing parallel truth sources.</done>
</task>

<task type="auto">
  <name>Task 3: Add focused automated tests for starter due-state and refresh semantics</name>
  <files>LevainTests/StarterTests.swift, LevainTests/TestSupport/DomainFixtures.swift, Levain/Models/Starter.swift, Levain/Models/StarterRefresh.swift, Levain.xcodeproj/project.pbxproj</files>
  <action>Add a dedicated starter-focused test suite that covers nextDueDate calculation, due-state derivation, and the persistence semantics around logged refreshes. Reuse deterministic fixed-date fixtures instead of burying starter assertions in the broader bake scheduler tests, so phase-6 regressions surface in the right place.</action>
  <verify>xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived -destination 'platform=iOS Simulator,name=iPhone 17 Pro' test -only-testing:LevainTests/StarterTests</verify>
  <done>Starter maintenance logic has focused automated coverage and no longer depends on indirect bake tests to catch regressions.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived CODE_SIGNING_ALLOWED=NO build`
- [ ] `xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived -destination 'platform=iOS Simulator,name=iPhone 17 Pro' test -only-testing:LevainTests/StarterTests`
- [ ] Logging a refresh updates starter history and due-state through persisted data
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- STARTER-02 and STARTER-03 are implemented with a fast UI and focused confidence around derived state
  </success_criteria>

<output>
After completion, create `.planning/phases/06-starter-management/06-02-SUMMARY.md`
</output>
