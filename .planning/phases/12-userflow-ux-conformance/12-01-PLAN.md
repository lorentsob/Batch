---
phase: 12-userflow-ux-conformance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/levain-user-flows.md
  - Levain/Services/TodayAgendaBuilder.swift
  - Levain/Features/Today/TodayView.swift
  - Levain/Features/Bakes/BakeCreationView.swift
  - Levain/Services/BakeScheduler.swift
  - LevainTests/TodayAgendaBuilderTests.swift
  - LevainTests/BakeSchedulerTests.swift
  - LevainUITests/TodayFlowUITests.swift
  - LevainUITests/BakesFlowUITests.swift
autonomous: true
user_setup: []
must_haves:
  truths:
    - Today exposes explicit first-launch, all-clear, future-only, and active-agenda modes.
    - Bake creation keeps system templates available and treats bake name as optional.
    - Repository flow documentation matches the userflow v2 semantics for Today and bake creation.
---

<objective>
Align the repository source of truth and the Today / bake-creation surfaces to the userflow-v2 contract.

Purpose: close the mismatch between the updated flow document and the app's operational entry points.
Output: updated markdown flow doc, Today-state refactor, bake-creation semantics, and the first conformance test pass.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/levain-user-flows.md
@Levain/Services/TodayAgendaBuilder.swift
@Levain/Features/Today/TodayView.swift
@Levain/Features/Bakes/BakeCreationView.swift
@Levain/Services/BakeScheduler.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild the operational source of truth</name>
  <files>docs/levain-user-flows.md</files>
  <action>Rewrite the markdown flow document so it mirrors the attached HTML v2 and becomes the repository source of truth for the six operational flows.</action>
  <verify>Review the markdown against the HTML v2 structure and semantics.</verify>
  <done>The markdown doc captures the six flows, Today state matrix, and v2 deltas without relying on the external HTML at review time.</done>
</task>

<task type="auto">
  <name>Task 2: Make Today and bake creation conform to v2</name>
  <files>Levain/Services/TodayAgendaBuilder.swift, Levain/Features/Today/TodayView.swift, Levain/Features/Bakes/BakeCreationView.swift, Levain/Services/BakeScheduler.swift</files>
  <action>Expose explicit Today states, future preview, tomorrow cap, optional bake naming, template availability, and create-then-edit semantics. Keep the fallback bake name owned by the scheduler.</action>
  <verify>Run the targeted Today and bake-creation unit/UI tests.</verify>
  <done>Today and new-bake behavior match the v2 flow contract and remain deterministic under UI automation.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `xcodebuild ... -only-testing:LevainTests/TodayAgendaBuilderTests`
- [ ] `xcodebuild ... -only-testing:LevainTests/BakeSchedulerTests`
- [ ] `xcodebuild ... -only-testing:LevainUITests/TodayFlowUITests`
- [ ] `xcodebuild ... -only-testing:LevainUITests/BakesFlowUITests`
</verification>

<success_criteria>

- All tasks completed
- Flow markdown updated to v2
- Today state matrix implemented
- Bake creation semantics aligned to v2

</success_criteria>

<output>
After completion, create `.planning/phases/12-userflow-ux-conformance/12-01-SUMMARY.md`
</output>
