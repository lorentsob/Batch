---
phase: 12-userflow-ux-conformance
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Levain/Models/BakeStep.swift
  - Levain/Features/Bakes/BakeStepDetailView.swift
  - Levain/Features/Bakes/BakeStepCardView.swift
  - Levain/Features/Starter/RefreshLogView.swift
  - LevainTests/BakeSchedulerTests.swift
  - LevainUITests/TodayFlowUITests.swift
autonomous: true
user_setup: []
must_haves:
  truths:
    - Active bake execution is sequential by default, but still supports explicit out-of-order recovery.
    - Out-of-order starts stay visible via persistent `Fuori ordine` feedback.
    - Starter refresh remains a fast three-field flow with advanced fields collapsed by default.
---

<objective>
Bring active bake execution and starter refresh UX into conformance with userflow v2.

Purpose: make the app's most frequent operational loops prescriptive, fast, and legible under real use.
Output: sequential-default bake execution, persistent out-of-order state, and fast starter refresh UX.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@Levain/Models/BakeStep.swift
@Levain/Features/Bakes/BakeStepDetailView.swift
@Levain/Features/Bakes/BakeStepCardView.swift
@Levain/Features/Starter/RefreshLogView.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enforce sequential-default bake execution</name>
  <files>Levain/Models/BakeStep.swift, Levain/Features/Bakes/BakeStepDetailView.swift, Levain/Features/Bakes/BakeStepCardView.swift</files>
  <action>Model and present the next-step contract so future steps require explicit confirmation before starting, persist a `Fuori ordine` marker, and keep quick-shift actions scoped to operational contexts.</action>
  <verify>Run scheduler tests and the bake-execution UI path.</verify>
  <done>The recommended next step is visually obvious, out-of-order starts are deliberate, and the timeline remains readable after override.</done>
</task>

<task type="auto">
  <name>Task 2: Compress starter refresh into the fast path</name>
  <files>Levain/Features/Starter/RefreshLogView.swift, Levain/Features/Today/TodayView.swift</files>
  <action>Reduce starter refresh to three primary inputs with advanced details collapsed, and ensure the Today starter row disappears immediately after a successful save.</action>
  <verify>Run the Today UI flow covering starter refresh removal.</verify>
  <done>Starter refresh matches the v2 "2 tap / under 30 seconds" expectation without leaving stale operational residue.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `xcodebuild ... -only-testing:LevainTests/BakeSchedulerTests`
- [ ] `xcodebuild ... -only-testing:LevainUITests/TodayFlowUITests`
</verification>

<success_criteria>

- Sequential-default behavior implemented
- `Fuori ordine` feedback persists after override
- Starter refresh fast path is visible and verified

</success_criteria>

<output>
After completion, create `.planning/phases/12-userflow-ux-conformance/12-02-SUMMARY.md`
</output>
