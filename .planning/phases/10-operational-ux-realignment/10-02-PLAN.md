---
phase: 10-operational-ux-realignment
plan: 02
type: execute
wave: 1
depends_on: ["10-01"]
files_modified:
  - Levain/Models/DomainEnums.swift
  - Levain/Models/RecipeFormula.swift
  - Levain/Models/Starter.swift
  - Levain/Features/Bakes/FormulaEditorView.swift
  - Levain/Features/Bakes/BakeCreationView.swift
  - Levain/Features/Bakes/FormulaDetailView.swift
  - Levain/Features/Starter/StarterEditorView.swift
  - Levain/Persistence/SeedDataLoader.swift
  - LevainTests/RecipeFormulaTests.swift
  - LevainTests/StarterTests.swift
autonomous: true
user_setup: []
must_haves:
  truths:
    - "Formula" must become "Ricetta" in the user experience, but the deeper change is structural: recipes need categories, presets, and reusable ingredient choices instead of thin free-text fields.
    - Built-in template recipes should be directly usable during bake creation without forcing a duplicate-save detour.
    - Recipe and starter authoring need persistent labels and structured inputs for flour and yeast choices, otherwise the planner remains ambiguous during real use.
  artifacts:
    - Levain/Models/RecipeFormula.swift
    - Levain/Features/Bakes/FormulaEditorView.swift
    - Levain/Features/Bakes/BakeCreationView.swift
    - Levain/Features/Starter/StarterEditorView.swift
  key_links:
    - `RecipeFormula` currently stores a single `flourMix` string and a coarse `BakeType`; both need to evolve to support recipe categories and structured ingredient selection.
    - `BakeCreationView` already depends on saved formulas, making it the right place to expose direct-use presets and the chosen yeast strategy.
    - Starter editing must share the same flour-selection UX so recipes and starters stop diverging semantically.
---

<objective>
Turn formula authoring into recipe authoring that better matches how the product is actually used.

Purpose: The current model is adequate for a demo, but not for real recipe reuse, structured flour choices, or yeast-aware bake planning.
Output: Category-based Ricette with direct-use presets, labeled forms, flour multi-select, and yeast/starter configuration that survives into bake creation.
</objective>

<execution_context>
@/Users/lorentso/.codex/skills/gsd-workflow/references/get-shit-done/workflows/execute-plan.md
@/Users/lorentso/.codex/skills/gsd-workflow/references/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-operational-ux-realignment/10-CONTEXT.md
@.planning/REQUIREMENTS.md
@docs/levain-prd-complete-v2.md
@docs/UX-SPEC.md
@Levain/Models/RecipeFormula.swift
@Levain/Models/DomainEnums.swift
@Levain/Features/Bakes/FormulaEditorView.swift
@Levain/Features/Bakes/BakeCreationView.swift
@Levain/Features/Starter/StarterEditorView.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Promote formulas into recipe presets and category-driven recipes</name>
  <files>Levain/Models/DomainEnums.swift, Levain/Models/RecipeFormula.swift, Levain/Persistence/SeedDataLoader.swift, Levain/Features/Bakes/BakeCreationView.swift, Levain/Features/Bakes/FormulaDetailView.swift</files>
  <action>Introduce the user-facing Ricette model changes needed for real use: categories instead of the current dough type framing, "Pane" instead of "Pagnotta", direct-use template recipes or bundled presets, and copy updates from formula language to recipe language. Bake creation must be able to start from these presets immediately, without a mandatory save-as detour.</action>
  <verify>Manual review confirms bundled or preexisting template recipes appear as valid bake choices immediately and recipe lists are grouped or filterable by category.</verify>
  <done>Recipes become reusable operational inputs instead of half-finished templates the user must manually convert first.</done>
</task>

<task type="auto">
  <name>Task 2: Replace placeholder-only ingredient inputs with labeled recipe and starter forms</name>
  <files>Levain/Features/Bakes/FormulaEditorView.swift, Levain/Features/Starter/StarterEditorView.swift, Levain/Features/Bakes/FormulaDetailView.swift</files>
  <action>Refactor recipe and starter forms so each field has a persistent label, clearer descriptions where needed, and user-facing copy aligned with the new recipe vocabulary. Rename "Pezzi" to "Porzioni", improve step list contrast, and remove the current reliance on disappearing placeholders for comprehension.</action>
  <verify>Manual inspection confirms all primary fields remain understandable after values are entered and the step list is readable under the shipped theme.</verify>
  <done>Users can read and edit recipes and starters without guessing which value belongs to which field.</done>
</task>

<task type="auto">
  <name>Task 3: Add structured flour and yeast selection that flows into bake creation</name>
  <files>Levain/Models/RecipeFormula.swift, Levain/Models/Starter.swift, Levain/Features/Bakes/FormulaEditorView.swift, Levain/Features/Bakes/BakeCreationView.swift, Levain/Features/Starter/StarterEditorView.swift, LevainTests/RecipeFormulaTests.swift, LevainTests/StarterTests.swift</files>
  <action>Replace the current free-text flour mix fields with a reusable multi-select model that supports predefined flour categories and custom additions for both recipes and starters. Add yeast-family selection to recipes, including sourdough via saved starters plus common commercial yeast modes, and make the resulting yeast quantity or basis explicit during bake creation.</action>
  <verify>Unit tests and manual checks confirm flour selections persist correctly, custom flour entries remain reusable, and switching yeast type updates the expected quantity model instead of silently keeping the same inoculation semantics.</verify>
  <done>The recipe and starter domain becomes expressive enough to support the real baking choices the user makes.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived CODE_SIGNING_ALLOWED=NO build`
- [ ] `xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived -destination 'platform=iOS Simulator,name=iPhone 17 Pro' test -only-testing:LevainTests/RecipeFormulaTests`
- [ ] `xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived -destination 'platform=iOS Simulator,name=iPhone 17 Pro' test -only-testing:LevainTests/StarterTests`
- [ ] New recipe and starter forms remain understandable with all fields filled and no flour mix free-text dependency
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Recipes are directly reusable, category-based, and visibly clearer than the current formula flow
- Starter and recipe ingredient modeling no longer depend on ambiguous free-text fields
  </success_criteria>

<output>
After completion, create `.planning/phases/10-operational-ux-realignment/10-02-SUMMARY.md`
</output>
