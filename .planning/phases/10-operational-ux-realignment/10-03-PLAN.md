---
phase: 10-operational-ux-realignment
plan: 03
type: execute
wave: 2
depends_on: ["10-01", "10-02"]
files_modified:
  - Levain/Models/Bake.swift
  - Levain/Services/TodayAgendaBuilder.swift
  - Levain/Features/Bakes/BakeCreationView.swift
  - Levain/Features/Bakes/BakeDetailView.swift
  - Levain/Features/Bakes/BakesView.swift
  - Levain/DesignSystem/Theme.swift
  - Levain/DesignSystem/Components/StateBadge.swift
  - Levain/Assets.xcassets/AppIcon.appiconset/Contents.json
  - project.yml
  - Levain.xcodeproj/project.pbxproj
  - LevainTests/TodayAgendaBuilderTests.swift
  - LevainUITests/BakesFlowUITests.swift
autonomous: true
user_setup: []
must_haves:
  truths:
    - Operational trust breaks if cancelled or terminal bakes still leak into Home as pending work or cannot be deleted when no longer needed.
    - Bake creation semantics must reflect the user's real planning target, which is the usage/eating time rather than an implementation-centric "target bake time" label.
    - Visual polish must be treated as a real correctness issue here, including destructive state colors, readable contrast, and the missing App Icon.
  artifacts:
    - Levain/Models/Bake.swift
    - Levain/Features/Bakes/BakeCreationView.swift
    - Levain/DesignSystem/Components/StateBadge.swift
    - Levain/Assets.xcassets/AppIcon.appiconset/Contents.json
  key_links:
    - `Bake` and `TodayAgendaBuilder` define whether cancelled work still appears in the operational model.
    - `BakeCreationView` carries the misleading "target bake time" label and the current advanced-section structure.
    - Asset-catalog configuration already points to `AppIcon`, so App Icon work should focus on asset validity, naming, and bundle verification rather than inventing new plist keys.
---

<objective>
Close the lifecycle, design-system, and asset issues that still make the app feel unreliable after the IA and recipe work.

Purpose: UAT exposed several trust-breaking gaps that are smaller than an architecture rewrite but too important to leave as polish.
Output: Clean bake terminal behavior, clearer planning semantics, corrected state colors and contrast, plus a working App Icon pipeline.
</objective>

<execution_context>
@/Users/lorentso/.codex/skills/gsd-workflow/references/get-shit-done/workflows/execute-plan.md
@/Users/lorentso/.codex/skills/gsd-workflow/references/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-operational-ux-realignment/10-CONTEXT.md
@.planning/debug/2026-03-11-uat-ux-realignment.md
@.planning/REQUIREMENTS.md
@docs/levain-prd-complete-v2.md
@docs/UX-SPEC.md
@Levain/Models/Bake.swift
@Levain/Services/TodayAgendaBuilder.swift
@Levain/Features/Bakes/BakeCreationView.swift
@Levain/DesignSystem/Theme.swift
@Levain/DesignSystem/Components/StateBadge.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clean up bake terminal behavior, deletion, and target-time semantics</name>
  <files>Levain/Models/Bake.swift, Levain/Services/TodayAgendaBuilder.swift, Levain/Features/Bakes/BakeCreationView.swift, Levain/Features/Bakes/BakeDetailView.swift, Levain/Features/Bakes/BakesView.swift, LevainTests/TodayAgendaBuilderTests.swift, LevainUITests/BakesFlowUITests.swift</files>
  <action>Ensure cancelled bakes are excluded from operational agenda derivation, expose deletion for cancelled or completed bakes, and rename or remodel the planning target in new bake from a bake-time implementation detail to the user-facing target usage time. Flatten the new bake form so starter selection and key fields remain in the main flow instead of a vague advanced disclosure.</action>
  <verify>Tests and manual flow checks confirm a cancelled bake leaves no pending work in Home, terminal bakes can be deleted safely, and the new bake flow speaks in target usage time language throughout.</verify>
  <done>The bake lifecycle feels coherent from planning through cancellation or completion, without stale agenda artifacts.</done>
</task>

<task type="auto">
  <name>Task 2: Audit and fix contrast, chips, iconography, and design-system drift</name>
  <files>Levain/DesignSystem/Theme.swift, Levain/DesignSystem/Components/StateBadge.swift, Levain/Features/Bakes/BakeCreationView.swift, Levain/Features/Bakes/BakesView.swift, Levain/Features/Today/TodayView.swift</files>
  <action>Apply a focused visual compliance pass across the touched screens so state chips, contrast, typography, and iconography follow the intended design language. Cancelled badges must read as destructive red, weak text contrast must be corrected, and remaining legacy visual choices that conflict with the documented system should be removed.</action>
  <verify>Manual inspection across the main screens confirms text remains legible, state meaning is readable without guesswork, and the primary dough/bake iconography feels semantically correct.</verify>
  <done>The app looks internally consistent and trustworthy rather than visually half-finished.</done>
</task>

<task type="auto">
  <name>Task 3: Debug and verify the App Icon pipeline end to end</name>
  <files>Levain/Assets.xcassets/AppIcon.appiconset/Contents.json, project.yml, Levain.xcodeproj/project.pbxproj</files>
  <action>Investigate the App Icon issue to completion, starting from the current asset-catalog evidence: verify filename casing, asset validity, and build output recognition before adding any extra configuration. If the project file is already correct, prefer the smallest fix that makes the icon resolve reliably in build products and on simulator installs.</action>
  <verify>Build output and a fresh simulator install confirm the configured `AppIcon` set is picked up and displayed by the app, with no asset-catalog warnings.</verify>
  <done>The icon supplied by the user is no longer stranded in the asset catalog and the app presents the correct icon end to end.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived CODE_SIGNING_ALLOWED=NO build`
- [ ] `xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived -destination 'platform=iOS Simulator,name=iPhone 17 Pro' test -only-testing:LevainTests/TodayAgendaBuilderTests`
- [ ] `xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived -destination 'platform=iOS Simulator,name=iPhone 17 Pro' test -only-testing:LevainUITests/BakesFlowUITests`
- [ ] A fresh simulator install shows the intended App Icon and no residual cancelled-bake work appears on Home
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Terminal bake behavior no longer leaves stale operational noise
- Visual-system and App Icon issues are closed with concrete verification, not assumptions
  </success_criteria>

<output>
After completion, create `.planning/phases/10-operational-ux-realignment/10-03-SUMMARY.md`
</output>
