---
phase: 20-kefir-lineage-journal
plan: 02
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - Levain/Features/Kefir/KefirJournalView.swift
  - Levain/Features/Kefir/KefirEventRow.swift
  - Levain/Features/Kefir/KefirHubView.swift
  - Levain/Features/Kefir/KefirBatchDetailView.swift
  - Levain/Features/Kefir/KefirBatchPresentation.swift
  - Levain/Persistence/SeedDataLoader.swift
  - LevainTests/SeedDataLoaderTests.swift
  - LevainUITests/KefirFlowUITests.swift
  - Levain.xcodeproj/project.pbxproj
autonomous: true
user_setup: []
must_haves:
  truths:
    - The kefir vertical exposes a dedicated journal/history reading surface with typed event rows and batch context.
    - Batch detail shows concise recent-history and provenance context while preserving one dominant operational action.
    - Seeded scenarios cover live, derived, and archived batches with deterministic journal history for UI regression coverage.
  artifacts:
    - Levain/Features/Kefir/KefirJournalView.swift
    - Levain/Features/Kefir/KefirEventRow.swift
    - LevainUITests/KefirFlowUITests.swift
  key_links:
    - `KefirHubView` links into the journal/archive surface without hiding the batch-first entry points and creation CTA.
    - `KefirJournalView` renders `KefirEvent` history through a consistent design-system grammar across active, paused, and archived batches.
    - `SeedDataLoader` and UI tests use event-rich seeded data so journal behavior is reproducible instead of manual-only.
---

<objective>
Turn the new event model into a real kefir journal surface users can read and navigate.

Purpose: Typed events only matter if users can understand continuity, derivation, and exceptions through a journal that stays supportive rather than taking over the vertical.
Output: Full journal/history screen, recent-history previews on batch detail, and seeded UI coverage for event-driven kefir flows.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-kefir-lineage-journal/20-CONTEXT.md
@.planning/phases/20-kefir-lineage-journal/20-01-PLAN.md
@docs/levain-prd-v2-multi-fermentations.md
@docs/levain-prd-v2-addendum.md
@Levain/Features/Kefir/KefirHubView.swift
@Levain/Features/Kefir/KefirBatchDetailView.swift
@Levain/Persistence/SeedDataLoader.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the journal surface and reusable event row grammar</name>
  <files>Levain/Features/Kefir/KefirJournalView.swift, Levain/Features/Kefir/KefirEventRow.swift, Levain/Features/Kefir/KefirBatchPresentation.swift, Levain.xcodeproj/project.pbxproj</files>
  <action>Create a kefir journal/history surface that reads typed `KefirEvent` entries in a compact, comprehensible way. Group and sort events so renewals, storage changes, derivations, notes, and archive events help users understand continuity over time without creating an infinite noisy timeline.</action>
  <verify>xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived CODE_SIGNING_ALLOWED=NO build</verify>
  <done>The kefir vertical has a real journal screen and event row component instead of leaving history trapped in the model layer.</done>
</task>

<task type="auto">
  <name>Task 2: Add journal entry points from the hub and recent-history context on detail</name>
  <files>Levain/Features/Kefir/KefirHubView.swift, Levain/Features/Kefir/KefirBatchDetailView.swift, Levain/Features/Kefir/KefirJournalView.swift</files>
  <action>Expose the journal/archive reading flow from the hub and surface a concise recent-history excerpt on batch detail so lineage and lifecycle context are visible where users already manage batches. Keep the primary operational action obvious and avoid turning the detail screen into a diary-first layout.</action>
  <verify>Manual review confirms the journal is discoverable from the kefir vertical and the batch detail still reads as an operational screen first.</verify>
  <done>Users can move from batch management to history reading without losing the action-first structure of the kefir vertical.</done>
</task>

<task type="auto">
  <name>Task 3: Seed event-rich kefir scenarios and lock the journal surface with UI coverage</name>
  <files>Levain/Persistence/SeedDataLoader.swift, LevainTests/SeedDataLoaderTests.swift, LevainUITests/KefirFlowUITests.swift</files>
  <action>Extend seeded sample content with deterministic event histories for active, derived, paused, and archived batches, then add UI coverage that validates journal entry points, visible event content, and batch-detail history previews. Reuse the existing seeded launch harness rather than creating a one-off journal bootstrap path.</action>
  <verify>xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived -destination 'platform=iOS Simulator,name=iPhone 17 Pro' test -only-testing:LevainUITests/KefirFlowUITests</verify>
  <done>The first journal/history surface is backed by deterministic data and a real regression net.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived CODE_SIGNING_ALLOWED=NO build`
- [ ] `xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived -destination 'platform=iOS Simulator,name=iPhone 17 Pro' test -only-testing:LevainTests/SeedDataLoaderTests`
- [ ] `xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived -destination 'platform=iOS Simulator,name=iPhone 17 Pro' test -only-testing:LevainUITests/KefirFlowUITests`
- [ ] The kefir vertical exposes a readable journal/history surface with deterministic event content and discoverable entry points
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 20 has a usable journal surface that supports the batch workflow instead of competing with it

</success_criteria>

<output>
After completion, create `.planning/phases/20-kefir-lineage-journal/20-02-SUMMARY.md`
</output>
