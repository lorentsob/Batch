---
phase: 20-kefir-lineage-journal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Levain/Models/KefirBatch.swift
  - Levain/Models/KefirEvent.swift
  - Levain/Persistence/LevainSchema.swift
  - Levain/Services/KefirEventRecorder.swift
  - Levain/Features/Kefir/KefirBatchEditorView.swift
  - Levain/Features/Kefir/KefirBatchManageSheet.swift
  - Levain/Features/Kefir/KefirBatchDetailView.swift
  - Levain/Features/Kefir/KefirBatchPresentation.swift
  - LevainTests/KefirEventTests.swift
  - LevainTests/PersistenceMigrationTests.swift
  - LevainTests/TestSupport/DomainFixtures.swift
  - Levain.xcodeproj/project.pbxproj
autonomous: true
user_setup: []
must_haves:
  truths:
    - Derived-batch creation records typed provenance and lifecycle history instead of leaving lineage as a raw UUID only.
    - Core kefir mutations create structured history automatically, so journal value grows from real operations without requiring manual diary work.
    - SwiftData extends additively to persist kefir events without risking existing bread, starter, or batch data.
  artifacts:
    - Levain/Models/KefirEvent.swift
    - Levain/Services/KefirEventRecorder.swift
    - LevainTests/KefirEventTests.swift
  key_links:
    - `KefirBatchEditorView`, `KefirBatchManageSheet`, and archive/reactivation flows use one shared recorder so event creation stays consistent across mutation points.
    - `KefirBatchDetailView` and `KefirBatchPresentation` surface named provenance context from persisted batch relationships instead of generic placeholder copy.
    - `LevainSchemaV4` and `PersistenceMigrationTests` protect the additive introduction of `KefirEvent`.
---

<objective>
Lay the persistence and mutation foundation for visible kefir lineage and typed history.

Purpose: Phase 20 cannot build a useful journal or archive until provenance and lifecycle events are durable, automatic, and readable from the existing batch flows.
Output: `KefirEvent` model, additive schema migration, shared event-recording helper, and visible provenance context on batch flows.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/20-kefir-lineage-journal/20-CONTEXT.md
@.planning/phases/19-milk-kefir-batch-core/19-CONTEXT.md
@.planning/phases/19-milk-kefir-batch-core/19-03-PLAN.md
@.planning/phases/19-milk-kefir-batch-core/19-04-PLAN.md
@docs/levain-prd-v2-multi-fermentations.md
@docs/levain-prd-v2-addendum.md
@Levain/Models/KefirBatch.swift
@Levain/Features/Kefir/KefirBatchEditorView.swift
@Levain/Features/Kefir/KefirBatchManageSheet.swift
@Levain/Features/Kefir/KefirBatchDetailView.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a persisted kefir event model and additive schema stage</name>
  <files>Levain/Models/KefirEvent.swift, Levain/Persistence/LevainSchema.swift, LevainTests/KefirEventTests.swift, LevainTests/PersistenceMigrationTests.swift, LevainTests/TestSupport/DomainFixtures.swift, Levain.xcodeproj/project.pbxproj</files>
  <action>Create a `KefirEvent` model that stores typed lifecycle history for batch creation, renew/manage-now, storage changes, reactivation, derivation, archive, and optional manual notes. Extend SwiftData with an additive V4 migration stage so event persistence lands without altering or deleting existing bread, starter, or kefir batch records.</action>
  <verify>xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived -destination 'platform=iOS Simulator,name=iPhone 17 Pro' test -only-testing:LevainTests/PersistenceMigrationTests</verify>
  <done>The codebase can persist typed kefir lifecycle events safely through an explicit schema boundary.</done>
</task>

<task type="auto">
  <name>Task 2: Wire automatic event recording into the existing kefir mutation flows</name>
  <files>Levain/Services/KefirEventRecorder.swift, Levain/Features/Kefir/KefirBatchEditorView.swift, Levain/Features/Kefir/KefirBatchManageSheet.swift, Levain/Features/Kefir/KefirBatchDetailView.swift, LevainTests/KefirEventTests.swift</files>
  <action>Introduce a lightweight shared recorder/helper that is called whenever a batch is created, derived, renewed, storage-adjusted, reactivated, or archived. Keep event creation centralized and automatic-first so Phase 20 builds history from real operational gestures instead of manual log forms or copy-pasted ad-hoc writes.</action>
  <verify>xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived -destination 'platform=iOS Simulator,name=iPhone 17 Pro' test -only-testing:LevainTests/KefirEventTests</verify>
  <done>Normal kefir management now leaves behind durable, typed history without changing the action-first workflow.</done>
</task>

<task type="auto">
  <name>Task 3: Replace placeholder provenance copy with real source and derived context</name>
  <files>Levain/Models/KefirBatch.swift, Levain/Features/Kefir/KefirBatchDetailView.swift, Levain/Features/Kefir/KefirBatchPresentation.swift</files>
  <action>Use the stored lineage and newly recorded events to surface concrete provenance on batch detail: show the named source batch when present, expose whether a batch has spawned derived children, and make the derive flow feel like a continuation of the original batch rather than a disconnected duplicate. Avoid building a full genealogy graph or culture-linked UI here.</action>
  <verify>xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived CODE_SIGNING_ALLOWED=NO build</verify>
  <done>Derived batches have readable lineage context in the UI instead of a generic "Derivato" badge with no useful follow-through.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived CODE_SIGNING_ALLOWED=NO build`
- [ ] `xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived -destination 'platform=iOS Simulator,name=iPhone 17 Pro' test -only-testing:LevainTests/KefirEventTests`
- [ ] `xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived -destination 'platform=iOS Simulator,name=iPhone 17 Pro' test -only-testing:LevainTests/PersistenceMigrationTests`
- [ ] Derived batches show readable provenance and core kefir mutations emit structured history automatically
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 20 has a durable lineage and event foundation that the journal/archive surfaces can build on directly

</success_criteria>

<output>
After completion, create `.planning/phases/20-kefir-lineage-journal/20-01-SUMMARY.md`
</output>
