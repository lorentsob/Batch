---
phase: 20-kefir-lineage-journal
plan: 04
type: execute
wave: 4
depends_on: ["20-03"]
files_modified:
  - Levain/Features/Kefir/KefirBatchComparisonView.swift
  - Levain/Features/Preparations/PreparationsView.swift
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
autonomous: true
user_setup: []
must_haves:
  truths:
    - Phase 20 is not closed until the seeded kefir UI suite passes end-to-end on the current code, not just until archive and comparison features appear shipped.
    - Archive/comparison behavior stays unchanged; this gap-closure wave is limited to stable accessibility anchors and planning-memory reconciliation.
    - The roadmap, project digest, requirements traceability, and state snapshot must all agree that Phase 20 is complete and that runtime hardening now comes before culture tracking.
  artifacts:
    - Levain/Features/Kefir/KefirBatchComparisonView.swift
    - Levain/Features/Preparations/PreparationsView.swift
    - .planning/STATE.md
    - .planning/ROADMAP.md
    - .planning/PROJECT.md
  key_links:
    - The comparison surface needs a concrete primary-batch accessibility anchor because XCTest can already reach the screen but not resolve the primary card reliably from a container wrapper.
    - Seeded kefir UI tests relaunch through `Preparazioni`, so that root also needs a stable visible anchor instead of depending on `List` container accessibility behavior.
    - Closing Phase 20 now must also insert the new Phase 21 hardening milestone and move culture/grain work to Phase 22.
---

<objective>
Close the kefir lineage/history phase for real by fixing the remaining verification gap and syncing the project memory.

Purpose: Phase 20 is functionally complete in code, but it cannot be treated as done while the seeded UI regression suite is red and the planning docs still disagree on status and next steps.
Output: Stable accessibility anchors for the remaining seeded flows, green `KefirFlowUITests`, and a reconciled Phase 20/21/22 planning baseline.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/20-kefir-lineage-journal/20-CONTEXT.md
@.planning/phases/20-kefir-lineage-journal/20-03-PLAN.md
@.planning/phases/20-kefir-lineage-journal/20-03-SUMMARY.md
@Levain/Features/Kefir/KefirBatchComparisonView.swift
@Levain/Features/Preparations/PreparationsView.swift
@LevainUITests/KefirFlowUITests.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden the remaining UI-test anchors without redesigning the shipped flows</name>
  <files>Levain/Features/Kefir/KefirBatchComparisonView.swift, Levain/Features/Preparations/PreparationsView.swift</files>
  <action>Move the remaining flaky identifiers onto concrete, always-visible accessibility elements so the seeded kefir flows can resolve the comparison primary card and the Preparazioni root reliably across repeated relaunches. Keep user-facing behavior, routing, and copy unchanged.</action>
  <verify>xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived CODE_SIGNING_ALLOWED=NO build</verify>
  <done>The remaining seeded navigation paths now expose stable UI anchors on current sources.</done>
</task>

<task type="auto">
  <name>Task 2: Re-run the full seeded kefir regression suite on the latest code</name>
  <files>LevainUITests/KefirFlowUITests.swift</files>
  <action>Run the accepted Phase 20 verification target on the latest source tree and keep closing gaps until the entire kefir UI suite passes, not just the single comparison test that originally failed.</action>
  <verify>xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived -destination 'platform=iOS Simulator,name=iPhone 15 Pro,OS=26.4' test -only-testing:LevainUITests/KefirFlowUITests</verify>
  <done>The seeded kefir suite passes 14/14 and Phase 20 has a real verification closeout.</done>
</task>

<task type="auto">
  <name>Task 3: Reconcile planning memory and record the closeout</name>
  <files>.planning/STATE.md, .planning/ROADMAP.md, .planning/PROJECT.md, .planning/REQUIREMENTS.md</files>
  <action>Update the planning docs so they all reflect the same reality: Phase 20 is complete, the new Phase 21 hardening milestone is next, culture/knowledge expansion shifts to Phase 22, and all plan counts/progress metrics are recomputed from the new inventory.</action>
  <verify>Manual review confirms `STATE.md`, `ROADMAP.md`, `PROJECT.md`, and `REQUIREMENTS.md` no longer contradict each other about Phase 20 completion or the next phase number.</verify>
  <done>The project memory matches the codebase and the verified roadmap again.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived CODE_SIGNING_ALLOWED=NO build`
- [ ] `xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived -destination 'platform=iOS Simulator,name=iPhone 15 Pro,OS=26.4' test -only-testing:LevainUITests/KefirFlowUITests`
- [ ] Phase 20 docs all agree that the lineage/history milestone is complete and that Phase 21 is the new hardening milestone
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 20 closes with verified kefir archive/comparison flows and reconciled planning memory

</success_criteria>

<output>
After completion, create `.planning/phases/20-kefir-lineage-journal/20-04-SUMMARY.md`
</output>
