---
phase: 02-domain-scheduling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Levain/Models/Bake.swift
  - Levain/Models/BakeStep.swift
  - Levain/Models/RecipeFormula.swift
  - Levain/Models/Starter.swift
  - Levain/Models/StarterRefresh.swift
  - Levain/Models/FormulaStepTemplate.swift
  - Levain/Models/DomainEnums.swift
  - Levain/Persistence/ModelContainerFactory.swift
  - Levain/Persistence/SeedDataLoader.swift
autonomous: true
user_setup: []
must_haves:
  truths:
    - SwiftData persists formulas, bakes, steps, starters, and starter refreshes with explicit identifiers and stable inverse relationships.
    - Planned schedule data and actual execution timestamps remain separate so later bake execution work can be layered on without schema churn.
    - Overdue and due labels remain derived from timestamps instead of being stored as a persistent domain state.
  artifacts:
    - Levain/Models/RecipeFormula.swift
    - Levain/Models/Bake.swift
    - Levain/Models/BakeStep.swift
    - Levain/Models/Starter.swift
    - Levain/Models/StarterRefresh.swift
    - Levain/Persistence/ModelContainerFactory.swift
    - Levain/Persistence/SeedDataLoader.swift
  key_links:
    - RecipeFormula.defaultSteps payload stays compatible with BakeScheduler step generation.
    - Bake.steps uses a cascade relationship with BakeStep.bake as the inverse link.
    - Starter.refreshes uses a cascade relationship with StarterRefresh.starter as the inverse link.
---

<objective>
Stabilize the Phase 2 domain schema for formulas, bakes, steps, starters, and refresh logs so later features can build on reliable persisted relationships instead of reworking the model layer.

Purpose: Domain scheduling is the substrate for every later bake and Today workflow.
Output: Hardened SwiftData models, consistent derived helpers, and persistence/bootstrap wiring that matches the PRD.
</objective>

<execution_context>
@/Users/lorentso/.codex/skills/gsd-workflow/references/get-shit-done/workflows/execute-plan.md
@/Users/lorentso/.codex/skills/gsd-workflow/references/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-domain-scheduling/02-CONTEXT.md
@docs/levain-prd-complete-v2.md
@Levain/Models/RecipeFormula.swift
@Levain/Models/Bake.swift
@Levain/Models/BakeStep.swift
@Levain/Models/Starter.swift
@Levain/Models/StarterRefresh.swift
@Levain/Models/FormulaStepTemplate.swift
@Levain/Models/DomainEnums.swift
@Levain/Persistence/ModelContainerFactory.swift
@Levain/Persistence/SeedDataLoader.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Normalize the persisted model relationships</name>
  <files>Levain/Models/RecipeFormula.swift, Levain/Models/Bake.swift, Levain/Models/BakeStep.swift, Levain/Models/Starter.swift, Levain/Models/StarterRefresh.swift, Levain/Models/DomainEnums.swift</files>
  <action>Audit the Phase 2 models against the roadmap and PRD, then tighten the stored fields and inverse relationships so the app persists formulas, bakes, steps, starters, and refresh logs without relying on UI-specific state. Keep identifiers explicit, keep relationship rules conservative, and avoid storing "late" or "due" as first-class statuses because those must stay derived.</action>
  <verify>xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived CODE_SIGNING_ALLOWED=NO build</verify>
  <done>Model files compile, inverse relationships remain coherent, and no persisted enum or flag duplicates overdue/due presentation logic.</done>
</task>

<task type="auto">
  <name>Task 2: Lock formula step-template storage and derived helpers</name>
  <files>Levain/Models/RecipeFormula.swift, Levain/Models/FormulaStepTemplate.swift, Levain/Models/Bake.swift, Levain/Models/BakeStep.swift</files>
  <action>Make sure formula default-step storage stays lightweight and deterministic, and expose only the derived helpers the scheduler and later UI flows actually need. Preserve the separation between reusable formula defaults, planned bake timing, and actual execution timestamps so Phase 4 can extend execution behavior without re-modeling the data.</action>
  <verify>Confirm `RecipeFormula.defaultSteps`, `Bake.sortedSteps`, `Bake.activeStep`, and `BakeStep.plannedEnd` form a coherent compile-safe path from formula defaults to scheduled step presentation.</verify>
  <done>Formula default steps remain encodable and decodable, bake and step helpers stay small, and planned-vs-actual semantics are obvious from the model API.</done>
</task>

<task type="auto">
  <name>Task 3: Align schema registration and sample bootstrap data</name>
  <files>Levain/Persistence/ModelContainerFactory.swift, Levain/Persistence/SeedDataLoader.swift</files>
  <action>Update the SwiftData schema registration and seed data so the preview and first-launch bootstrap exercise the stabilized Phase 2 model graph. The seed path should create a realistic starter, formula, bake, and generated step set that future phases can reuse for manual validation.</action>
  <verify>xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived CODE_SIGNING_ALLOWED=NO build</verify>
  <done>ModelContainerFactory includes the final Phase 2 schema, seed data composes without schema mismatches, and the sample graph still reflects a real bake workflow.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived CODE_SIGNING_ALLOWED=NO build`
- [ ] Seed data still forms a valid starter -> formula -> bake -> steps graph
- [ ] No stored property introduces overdue or due state as persistent data
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The model layer is stable enough that later UI and notification work can depend on it without re-shaping persistence
  </success_criteria>

<output>
After completion, create `.planning/phases/02-domain-scheduling/02-01-SUMMARY.md`
</output>
