---
phase: 02-domain-scheduling
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - Levain/Services/BakeScheduler.swift
  - Levain/Models/Bake.swift
  - Levain/Models/BakeStep.swift
  - Levain/Models/FormulaStepTemplate.swift
  - Levain/DesignSystem/Extensions/Date+Levain.swift
  - Levain/Persistence/SeedDataLoader.swift
autonomous: true
user_setup: []
must_haves:
  truths:
    - A bake timeline is generated backward from the target bake time using the formula's ordered default steps.
    - Shifting a timeline only moves incomplete future steps and their windows while leaving completed or skipped steps untouched.
    - Derived bake and step helpers reflect planned, running, completed, and overdue presentation state without auto-mutating persisted step status.
  artifacts:
    - Levain/Services/BakeScheduler.swift
    - Levain/Models/Bake.swift
    - Levain/Models/BakeStep.swift
    - Levain/Models/FormulaStepTemplate.swift
  key_links:
    - RecipeFormula.defaultSteps -> BakeScheduler.generateSteps
    - BakeScheduler.generateBake -> Bake.steps and Bake.formula linkage
    - BakeScheduler.shiftFutureSteps -> Bake.targetBakeDateTime recalculation
---

<objective>
Implement the deterministic scheduling rules for Phase 2: backward step generation, derived timeline helpers, and safe future-step shifting.

Purpose: Later bake creation, execution, Today, and notification flows depend on one trusted scheduler instead of re-implementing domain timing in each feature.
Output: A small scheduling service and supporting model helpers that encode the PRD's backward-planning and timeline-shift rules.
</objective>

<execution_context>
@/Users/lorentso/.codex/skills/gsd-workflow/references/get-shit-done/workflows/execute-plan.md
@/Users/lorentso/.codex/skills/gsd-workflow/references/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-domain-scheduling/02-CONTEXT.md
@docs/levain-prd-complete-v2.md
@Levain/Services/BakeScheduler.swift
@Levain/Models/RecipeFormula.swift
@Levain/Models/FormulaStepTemplate.swift
@Levain/Models/Bake.swift
@Levain/Models/BakeStep.swift
@Levain/DesignSystem/Extensions/Date+Levain.swift
@Levain/Persistence/SeedDataLoader.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate bake steps backward from the target bake time</name>
  <files>Levain/Services/BakeScheduler.swift, Levain/Models/RecipeFormula.swift, Levain/Models/FormulaStepTemplate.swift, Levain/DesignSystem/Extensions/Date+Levain.swift</files>
  <action>Implement or refine the scheduler so it copies a formula into a bake instance and creates ordered steps by walking backward from `targetBakeDateTime`. Carry forward duration, reminder offsets, notes, temperature targets, and any flexible windows needed later, but keep the scheduling API intentionally small and deterministic.</action>
  <verify>xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived CODE_SIGNING_ALLOWED=NO build</verify>
  <done>The generated bake ends on the requested target time, step ordering is stable, and each step is populated from the formula defaults without UI-specific logic leaking into the scheduler.</done>
</task>

<task type="auto">
  <name>Task 2: Preserve planned-vs-actual execution semantics</name>
  <files>Levain/Models/Bake.swift, Levain/Models/BakeStep.swift, Levain/Services/BakeScheduler.swift</files>
  <action>Harden the derived helpers around bake and step state so later execution flows can reason about what is planned, what is currently running, and what has completed without auto-promoting statuses because time passed. Actual timestamps should be captured only when the user starts, completes, or skips a step.</action>
  <verify>Confirm `Bake.derivedStatus`, `Bake.activeStep`, `BakeStep.isOverdue(now:)`, `BakeStep.start`, `BakeStep.complete`, and `BakeStep.skip` still align with the PRD rules for real-world baking variance.</verify>
  <done>Late or overdue remains a derived label, actual timestamps are separate from planned timing, and the bake-level helpers expose the next actionable step cleanly.</done>
</task>

<task type="auto">
  <name>Task 3: Implement safe future-step shift behavior</name>
  <files>Levain/Services/BakeScheduler.swift, Levain/Models/Bake.swift, Levain/Models/BakeStep.swift, Levain/Persistence/SeedDataLoader.swift</files>
  <action>Implement the timeline-shift operation so only incomplete steps after the selected anchor move, including flexible windows and any reminder metadata stored on those steps. Recalculate the bake target bake time from the shifted timeline, and keep completed or skipped steps locked so history is not rewritten.</action>
  <verify>xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived CODE_SIGNING_ALLOWED=NO build</verify>
  <done>Timeline shifts move only future incomplete work, completed history stays intact, and the bake's final scheduled end stays synchronized with the last step.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived CODE_SIGNING_ALLOWED=NO build`
- [ ] Backward-generated steps end at `targetBakeDateTime` and stay ordered by `orderIndex`
- [ ] `shiftFutureSteps` changes only incomplete steps after the anchor and recalculates the bake target time
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Scheduling logic is deterministic enough that later UI and notification features can call it without duplicating timeline rules
  </success_criteria>

<output>
After completion, create `.planning/phases/02-domain-scheduling/02-02-SUMMARY.md`
</output>
