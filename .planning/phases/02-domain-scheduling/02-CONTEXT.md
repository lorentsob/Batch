# Phase 2 Context

Phase 2 defines the durable product language in code: core SwiftData models, derived status rules, bake-step timeline generation, and future-step shifting. The output must be stable enough that later UI work plugs into it rather than reinventing logic per screen.

