---
phase: 02-domain-scheduling
plan: 03
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - LevainTests/BakeSchedulerTests.swift
  - LevainTests/TestSupport/DomainFixtures.swift
autonomous: true
user_setup: []
must_haves:
  truths:
    - Unit tests cover backward schedule generation, future-step shifting, derived bake status, starter due-state derivation, and planned-vs-actual timestamp handling.
    - The test suite is lightweight and deterministic enough to serve as the baseline regression harness for future bake-execution work.
    - Core scheduling assertions live in the test target instead of being re-checked manually in UI flows.
  artifacts:
    - LevainTests/BakeSchedulerTests.swift
    - LevainTests/TestSupport/DomainFixtures.swift
  key_links:
    - DomainFixtures helpers construct RecipeFormula, Bake, and Starter values used by BakeSchedulerTests.
    - BakeSchedulerTests asserts behavior implemented in BakeScheduler, Bake, BakeStep, and Starter.
---

<objective>
Create the unit-test baseline for Phase 2 so the scheduler and derived-state rules are guarded before later execution and notification features expand on them.

Purpose: Scheduling bugs are high leverage and easy to regress if they are validated only through UI flows.
Output: Focused XCTest coverage plus simple domain fixtures that keep core logic tests readable and extendable.
</objective>

<execution_context>
@/Users/lorentso/.codex/skills/gsd-workflow/references/get-shit-done/workflows/execute-plan.md
@/Users/lorentso/.codex/skills/gsd-workflow/references/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-domain-scheduling/02-CONTEXT.md
@docs/levain-prd-complete-v2.md
@Levain/Services/BakeScheduler.swift
@Levain/Models/Bake.swift
@Levain/Models/BakeStep.swift
@Levain/Models/RecipeFormula.swift
@Levain/Models/Starter.swift
@LevainTests/BakeSchedulerTests.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add lightweight domain fixtures for scheduler tests</name>
  <files>LevainTests/TestSupport/DomainFixtures.swift, LevainTests/BakeSchedulerTests.swift</files>
  <action>Create small fixture helpers that build formulas, starters, and bake timelines without repeating setup across every test. Keep the helpers transparent and phase-scoped; do not introduce a heavy testing abstraction that hides the baker's-math values or timeline assumptions.</action>
  <verify>Fixture helpers compile inside the `LevainTests` target and reduce duplicated setup in the scheduling test file.</verify>
  <done>`BakeSchedulerTests` reads clearly, and future phases can reuse the same fixtures for bake-execution tests without rewriting the Phase 2 domain setup.</done>
</task>

<task type="auto">
  <name>Task 2: Expand scheduler and derived-state coverage</name>
  <files>LevainTests/BakeSchedulerTests.swift, LevainTests/TestSupport/DomainFixtures.swift</files>
  <action>Add or refine test cases so Phase 2 behavior is explicit: backward generation from target bake time, future-step-only shifting, derived bake status transitions, starter due-state derivation, and the separation between planned schedule data and actual execution timestamps. Keep assertions aligned with the PRD language, especially the rule that overdue is derived rather than persisted.</action>
  <verify>xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived -destination 'platform=iOS Simulator,name=iPhone 17' test -only-testing:LevainTests/BakeSchedulerTests</verify>
  <done>The focused unit test run passes and clearly documents the core scheduling rules that later phases must preserve.</done>
</task>

<task type="auto">
  <name>Task 3: Keep the regression harness small and deterministic</name>
  <files>LevainTests/BakeSchedulerTests.swift, LevainTests/TestSupport/DomainFixtures.swift</files>
  <action>Review the final test names and helpers for determinism. Use fixed dates and explicit expected offsets instead of relying on wall-clock time, and make sure the Phase 2 suite can run repeatedly without hidden seed-data or persistence dependencies.</action>
  <verify>Each test uses fixed inputs or injected `now` values, and the focused `BakeSchedulerTests` suite is independent from app-launch or SwiftData container side effects.</verify>
  <done>The Phase 2 test suite is fast, stable, and ready to catch regressions before work starts on bake execution and notification rescheduling.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived CODE_SIGNING_ALLOWED=NO build`
- [ ] `xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived -destination 'platform=iOS Simulator,name=iPhone 17' test -only-testing:LevainTests/BakeSchedulerTests`
- [ ] Tests explicitly cover schedule generation, future-step shifting, derived bake status, starter due state, and planned-vs-actual timestamp separation
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The repo has a reusable unit-test baseline for core scheduling logic before Phase 4 and Phase 5 expand on it
  </success_criteria>

<output>
After completion, create `.planning/phases/02-domain-scheduling/02-03-SUMMARY.md`
</output>
