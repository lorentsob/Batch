# 11-01 Summary: Naming Unification

## Outcome
The repo now uses `Levain` consistently in product-facing and AI-facing markdown context. `CLAUDE.md` was corrected to match the real repository structure, `README.md` no longer contains the final ambiguous uppercase `Lievito` product reference, and `project.yml` now sets the app display name explicitly to `Levain`.

## Notes
- The repository already used `com.lorentso.levain`; documentation was aligned to that reality instead of preserving stale assumptions.
- Internal domain language such as `lievito madre` remains where it describes the baking concept, not the product name.
