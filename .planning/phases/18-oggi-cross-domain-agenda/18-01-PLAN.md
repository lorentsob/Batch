---
phase: 18-oggi-cross-domain-agenda
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Levain/Services/TodayAgendaBuilder.swift
  - LevainTests/TodayAgendaBuilderTests.swift
  - LevainTests/TestSupport/DomainFixtures.swift
  - Levain.xcodeproj/project.pbxproj
autonomous: true
user_setup: []
must_haves:
  truths:
    - Oggi derives one ordered operational feed with explicit domain and urgency metadata instead of hard-coded `urgent`, `scheduled`, and `tomorrow` sections.
    - Ranking across bread and starter is time-based with no domain priority, and the agenda contract is ready for kefir items without forcing a later UI rewrite.
    - Empty, future-only, and active-overview states stay explicit so the view can remain action-first without rebuilding business rules inline.
  artifacts:
    - Levain/Services/TodayAgendaBuilder.swift
    - LevainTests/TodayAgendaBuilderTests.swift
    - LevainTests/TestSupport/DomainFixtures.swift
  key_links:
    - TodayAgendaBuilder consumes derived bake and starter state from existing models/services instead of duplicating urgency logic in TodayView.
    - TodayAgendaBuilderTests lock the addendum tie-breakers and future-preview behavior with deterministic fixtures.
    - DomainFixtures provides stable equal-urgency scenarios that Phase 19 can extend with kefir data later.
---

<objective>
Refactor the agenda foundation behind `Oggi` so the screen can become a true cross-domain operational dashboard.

Purpose: Phase 18 needs a feed contract and ranking model that can outlive the old bread/starter-only Today layout and absorb kefir items cleanly in the next phase.
Output: Updated agenda snapshot/ranking logic, explicit urgency metadata, and focused tests that freeze the new ordering rules.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-oggi-cross-domain-agenda/18-CONTEXT.md
@docs/levain-prd-v2-multi-fermentations.md
@docs/levain-prd-v2-addendum.md
@Levain/Services/TodayAgendaBuilder.swift
@Levain/Models/Bake.swift
@Levain/Models/BakeStep.swift
@Levain/Models/Starter.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace section buckets with a ranked, domain-tagged agenda feed</name>
  <files>Levain/Services/TodayAgendaBuilder.swift</files>
  <action>Rework the agenda output so `TodayView` receives a single ordered feed of operational items with explicit domain, urgency, and primary-action metadata rather than three rigid sections. Preserve current bread-step and starter due-state derivation from existing models, and add a kefir-ready agenda shape or source hook without dragging persistence work into this phase.</action>
  <verify>xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived -destination 'platform=iOS Simulator,name=iPhone 17 Pro' test -only-testing:LevainTests/TodayAgendaBuilderTests</verify>
  <done>The agenda builder expresses cross-domain ordering in one place and no longer assumes the UI will be bucketed by section.</done>
</task>

<task type="auto">
  <name>Task 2: Encode addendum tie-breakers and active-object ranking in deterministic tests</name>
  <files>LevainTests/TodayAgendaBuilderTests.swift, LevainTests/TestSupport/DomainFixtures.swift, Levain.xcodeproj/project.pbxproj</files>
  <action>Expand fixtures and tests so the builder is verified against the v2 ordering rules: overdue oldest first, warning nearest due first, and non-urgent active items ordered by recency with no domain bias. Cover the fallback between actionable work, future preview, and all-clear states so the view does not need hidden heuristics.</action>
  <verify>xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived -destination 'platform=iOS Simulator,name=iPhone 17 Pro' test -only-testing:LevainTests/TodayAgendaBuilderTests</verify>
  <done>The ordering contract is frozen by tests instead of living only in prose or UI screenshots.</done>
</task>

<task type="auto">
  <name>Task 3: Keep state outputs explicit for multi-domain empty and future behavior</name>
  <files>Levain/Services/TodayAgendaBuilder.swift, LevainTests/TodayAgendaBuilderTests.swift</files>
  <action>Make the snapshot distinguish between first-use, all-clear, future-only, and actionable-with-active-overview states in a way that matches the v2 shell language. Do not let the agenda force descriptive dashboard behavior; it should still answer what needs attention now, while leaving room for always-visible active objects.</action>
  <verify>Review the updated snapshot API and tests to confirm empty/future states are explicit and do not depend on view-side inference.</verify>
  <done>The Oggi view can render multi-domain states directly from service output without rebuilding agenda semantics inline.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived CODE_SIGNING_ALLOWED=NO build`
- [ ] `xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived -destination 'platform=iOS Simulator,name=iPhone 17 Pro' test -only-testing:LevainTests/TodayAgendaBuilderTests`
- [ ] The agenda service now outputs a ranked cross-domain feed contract with deterministic tie-breakers and explicit multi-domain state modes
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 18 has a reusable feed foundation that can accept real kefir items in Phase 19 without another agenda redesign

</success_criteria>

<output>
After completion, create `.planning/phases/18-oggi-cross-domain-agenda/18-01-SUMMARY.md`
</output>
