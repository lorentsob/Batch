---
phase: 18-oggi-cross-domain-agenda
plan: 03
type: execute
wave: 3
depends_on: ["18-01", "18-02"]
files_modified:
  - Levain/App/AppRouter.swift
  - Levain/Features/Shared/RootTabView.swift
  - Levain/Features/Today/TodayView.swift
  - LevainTests/AppRouterTests.swift
  - LevainUITests/NotificationRouteUITests.swift
  - LevainUITests/TodayFlowUITests.swift
autonomous: true
user_setup: []
must_haves:
  truths:
    - Tapping an Oggi card opens the underlying bake or starter directly under the v2 shell, without traversing Preparazioni.
    - The router and deep-link contract become kefir-ready in this phase so Phase 19 can plug in batch detail without changing Oggi behavior.
    - Regression coverage exists for direct object routing from Oggi, launch/openURL handling, and notification entry through the migrated shell.
  artifacts:
    - Levain/App/AppRouter.swift
    - LevainTests/AppRouterTests.swift
    - LevainUITests/NotificationRouteUITests.swift
  key_links:
    - TodayView uses AppRouter direct-open helpers for card taps and future preview navigation.
    - RootTabView continues to funnel pending URLs and openURL events through the same router contract after the Oggi refactor.
    - NotificationRouteUITests and TodayFlowUITests protect the direct-routing rule across app entry points.
---

<objective>
Lock in the direct-object routing semantics for Oggi and extend the router contract so the cross-domain dashboard behaves consistently across taps, deep links, and notification entry.

Purpose: Oggi is only credible as the operational home if its cards open the real underlying object directly and if the routing surface is already shaped for the upcoming kefir vertical.
Output: Updated router helpers and deep-link parsing, Oggi navigation wired through the router contract, and regression tests for the new shell behavior.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-oggi-cross-domain-agenda/18-CONTEXT.md
@.planning/phases/18-oggi-cross-domain-agenda/18-01-PLAN.md
@.planning/phases/18-oggi-cross-domain-agenda/18-02-PLAN.md
@Levain/App/AppRouter.swift
@Levain/Features/Shared/RootTabView.swift
@Levain/Features/Today/TodayView.swift
@Levain/Services/NotificationService.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend the router contract for direct object routes and future kefir detail</name>
  <files>Levain/App/AppRouter.swift, LevainTests/AppRouterTests.swift</files>
  <action>Update `AppRouter` so direct object opening under the v2 shell stays explicit for bake and starter objects and adds a kefir-batch route surface that Phase 19 can consume. Keep the route contract narrow, deterministic, and deep-link-driven; do not route Oggi cards through Preparazioni or hide assumptions in ad hoc view state.</action>
  <verify>xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived -destination 'platform=iOS Simulator,name=iPhone 17 Pro' test -only-testing:LevainTests/AppRouterTests</verify>
  <done>The router owns direct object semantics for Oggi and is ready for a future kefir batch destination without another route redesign.</done>
</task>

<task type="auto">
  <name>Task 2: Move Oggi card taps and previews onto router-owned navigation semantics</name>
  <files>Levain/Features/Today/TodayView.swift, Levain/Features/Shared/RootTabView.swift</files>
  <action>Ensure card taps and future-preview entry points use router helpers rather than local-only presentation paths when the user is opening the underlying object. Narrow or retire local modal-only navigation where it conflicts with the direct-detail rule, while preserving inline primary actions as fast operational shortcuts when appropriate.</action>
  <verify>Manual code review confirms Oggi taps now open the underlying object through router semantics and still preserve action-first shortcuts where intended.</verify>
  <done>Oggi behaves like the operational front door to real object detail, not like a parallel navigation system.</done>
</task>

<task type="auto">
  <name>Task 3: Add regression coverage for direct routing through the new shell</name>
  <files>LevainUITests/NotificationRouteUITests.swift, LevainUITests/TodayFlowUITests.swift, LevainTests/AppRouterTests.swift</files>
  <action>Update routing tests so app launch, openURL handling, Oggi taps, and notification entry all preserve the direct-object rule after the shell migration. Keep kefir coverage contract-level for now if real batch detail does not exist yet, but make sure the route surface is already protected by tests.</action>
  <verify>xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived -destination 'platform=iOS Simulator,name=iPhone 17 Pro' test -only-testing:LevainUITests/NotificationRouteUITests</verify>
  <done>Phase 18 leaves behind an automated regression net for the new Oggi routing contract across app entry points.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived CODE_SIGNING_ALLOWED=NO build`
- [ ] `xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived -destination 'platform=iOS Simulator,name=iPhone 17 Pro' test -only-testing:LevainTests/AppRouterTests`
- [ ] `xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived -destination 'platform=iOS Simulator,name=iPhone 17 Pro' test -only-testing:LevainUITests/NotificationRouteUITests`
- [ ] `xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived -destination 'platform=iOS Simulator,name=iPhone 17 Pro' test -only-testing:LevainUITests/TodayFlowUITests`
- [ ] Oggi taps, openURL handling, and notification entry all respect the direct-object routing rule under the v2 shell
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 18 closes with direct-routing semantics that Phase 19 can extend to kefir without reshaping Oggi behavior

</success_criteria>

<output>
After completion, create `.planning/phases/18-oggi-cross-domain-agenda/18-03-SUMMARY.md`
</output>
