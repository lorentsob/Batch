---
phase: 18-oggi-cross-domain-agenda
plan: 02
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - Levain/Features/Today/TodayView.swift
  - Levain/Features/Today/TodayOperationalCardView.swift
  - Levain/Features/Today/TodayStepCardView.swift
  - Levain/Features/Today/TodayStarterReminderRow.swift
  - LevainUITests/TodayFlowUITests.swift
  - Levain.xcodeproj/project.pbxproj
autonomous: true
user_setup: []
must_haves:
  truths:
    - Oggi shows all active objects in one operational stream with clear domain cues and card-level urgency instead of rigid section headers.
    - Every item still exposes one obvious primary action and action-first copy, even after the screen becomes cross-domain.
    - Empty and future states use multi-domain shell language without turning Oggi into a descriptive dashboard.
  artifacts:
    - Levain/Features/Today/TodayView.swift
    - Levain/Features/Today/TodayOperationalCardView.swift
    - LevainUITests/TodayFlowUITests.swift
  key_links:
    - TodayView renders the ordered feed from TodayAgendaBuilder without rebuilding ranking logic locally.
    - TodayStepCardView and TodayStarterReminderRow reuse a shared operational card grammar instead of drifting into unrelated layouts.
    - TodayFlowUITests cover the new empty, future-only, and actionable Oggi states under the v2 copy and presentation rules.
---

<objective>
Turn the existing Today UI into the v2 `Oggi` dashboard using the new feed contract and a shared operational card language.

Purpose: The ranking logic alone is not enough; the screen must read as one coherent cross-domain operational surface that still feels native to the current design system.
Output: Updated Oggi layout, shared card primitives for domain-tagged items, and UI coverage for the new state presentation.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-oggi-cross-domain-agenda/18-CONTEXT.md
@.planning/phases/18-oggi-cross-domain-agenda/18-01-PLAN.md
@Levain/Features/Today/TodayView.swift
@Levain/Features/Today/TodayStepCardView.swift
@Levain/Features/Today/TodayStarterReminderRow.swift
@Levain/DesignSystem/Theme.swift
@Levain/DesignSystem/Components/SectionCard.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract a shared operational card grammar with domain and urgency cues</name>
  <files>Levain/Features/Today/TodayOperationalCardView.swift, Levain/Features/Today/TodayStepCardView.swift, Levain/Features/Today/TodayStarterReminderRow.swift, Levain.xcodeproj/project.pbxproj</files>
  <action>Create a shared card container or supporting view model that gives bread, starter, and future kefir items the same structural grammar: domain cue, derived state, timing microcopy, and one dominant action. Reuse current tokens, badges, and spacing instead of inventing a second visual system for Oggi.</action>
  <verify>xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived CODE_SIGNING_ALLOWED=NO build</verify>
  <done>Oggi item families look intentionally related, and starter rows stop feeling like a special-case system exception.</done>
</task>

<task type="auto">
  <name>Task 2: Recompose TodayView into an ordered cross-domain Oggi feed</name>
  <files>Levain/Features/Today/TodayView.swift</files>
  <action>Replace section-driven rendering with a ranked operational feed that keeps active non-urgent objects visible, minimizes summary chrome, and uses the new multi-domain copy. Preserve one clear next action, but stop treating `Today` as a bucketed board. The screen should still support future preview and all-clear states without hiding active work.</action>
  <verify>Manual review confirms the screen surfaces the next meaningful action, keeps active objects visible when present, and no longer depends on hard-coded section headers for urgency.</verify>
  <done>Oggi reads like a daily operational dashboard for multiple preparation domains instead of a bread-first Today board.</done>
</task>

<task type="auto">
  <name>Task 3: Update UI coverage for the v2 Oggi states and copy</name>
  <files>LevainUITests/TodayFlowUITests.swift</files>
  <action>Refresh the Today UI tests so they validate the new Oggi empty state, future-only preview, and actionable feed behavior under the v2 shell copy. Keep seeded scenarios deterministic and avoid tests that only assert layout trivia; focus on operational meaning and the new action-first CTAs.</action>
  <verify>xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived -destination 'platform=iOS Simulator,name=iPhone 17 Pro' test -only-testing:LevainUITests/TodayFlowUITests</verify>
  <done>The new Oggi presentation is protected by UI coverage instead of relying on manual spot checks.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived CODE_SIGNING_ALLOWED=NO build`
- [ ] `xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived -destination 'platform=iOS Simulator,name=iPhone 17 Pro' test -only-testing:LevainUITests/TodayFlowUITests`
- [ ] Oggi now renders a unified operational feed with multi-domain copy, explicit domain cues, and action-first empty/future states
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The v2 Oggi screen is visually and behaviorally ready to host real kefir cards when Phase 19 plugs them into the feed

</success_criteria>

<output>
After completion, create `.planning/phases/18-oggi-cross-domain-agenda/18-02-SUMMARY.md`
</output>
