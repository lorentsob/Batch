---
phase: 05-today-notifications
plan: 03
type: execute
wave: 2
depends_on: ["05-01", "05-02"]
files_modified:
  - Levain/App/AppRouter.swift
  - Levain/Features/Shared/RootTabView.swift
  - Levain/Features/Today/TodayView.swift
  - Levain/Services/NotificationService.swift
  - LevainTests/AppRouterTests.swift
  - Levain.xcodeproj/project.pbxproj
autonomous: true
user_setup: []
must_haves:
  truths:
    - Tapping a bake notification reopens the related bake context, and tapping a starter notification reopens the related starter context.
    - Foreground delivery, background relaunch, and in-app Today actions all use the same routing semantics instead of separate one-off navigation code paths.
    - Deep-link parsing is explicit enough to be regression-tested without requiring manual notification delivery for every change.
  artifacts:
    - Levain/App/AppRouter.swift
    - Levain/Features/Shared/RootTabView.swift
    - LevainTests/AppRouterTests.swift
  key_links:
    - NotificationService emits route payloads AppRouter can parse deterministically.
    - RootTabView consumes pending notification URLs and openURL events through the same routing path.
    - TodayView actions reuse router entry points where appropriate so notification routing and in-app action handling stay aligned.
---

<objective>
Close Phase 5 by making routing from reminders trustworthy: notification taps and in-app actions should land the user in the correct operational context with no special-case navigation drift.

Purpose: The reminder system is incomplete unless acting on a notification reliably returns the user to the related bake or starter work.
Output: Centralized deep-link parsing, resilient notification-to-router handoff, and focused router tests.
</objective>

<execution_context>
@/Users/lorentso/.codex/skills/gsd-workflow/references/get-shit-done/workflows/execute-plan.md
@/Users/lorentso/.codex/skills/gsd-workflow/references/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-today-notifications/05-CONTEXT.md
@docs/levain-prd-complete-v2.md
@docs/UX-SPEC.md
@Levain/App/AppRouter.swift
@Levain/Features/Shared/RootTabView.swift
@Levain/Features/Today/TodayView.swift
@Levain/Services/NotificationService.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Centralize deep-link payload parsing and route semantics</name>
  <files>Levain/App/AppRouter.swift, Levain/Services/NotificationService.swift, LevainTests/AppRouterTests.swift, Levain.xcodeproj/project.pbxproj</files>
  <action>Make the route payload format between notifications and AppRouter explicit and stable instead of depending on stringly typed conventions spread across the app. Preserve the product requirement that reminder taps reopen the related bake or starter context, and keep the route surface narrow enough to test directly with pure URL inputs.</action>
  <verify>xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived -destination 'platform=iOS Simulator,name=iPhone 17 Pro' test -only-testing:LevainTests/AppRouterTests</verify>
  <done>Router parsing and notification payload generation agree on one deterministic route contract that can be covered by focused tests.</done>
</task>

<task type="auto">
  <name>Task 2: Unify notification and app-level URL handoff through RootTabView</name>
  <files>Levain/Features/Shared/RootTabView.swift, Levain/App/AppRouter.swift, Levain/Services/NotificationService.swift</files>
  <action>Review the app-level handoff so pending notification URLs, foreground notification taps, and ordinary `onOpenURL` events all pass through the same routing path. Avoid parallel navigation branches that would make notification behavior differ between cold launch, warm foreground, and background resume cases.</action>
  <verify>Manual code-path review confirms `pendingURL` handling and `onOpenURL` both route through the same router entry point and clear transient notification state correctly.</verify>
  <done>Notification entry behaves consistently across launch states instead of depending on whichever code path happened to fire first.</done>
</task>

<task type="auto">
  <name>Task 3: Align Today primary actions with the finalized routing model</name>
  <files>Levain/Features/Today/TodayView.swift, Levain/App/AppRouter.swift</files>
  <action>Adjust Today action handling where needed so item CTAs use the same operational navigation semantics as reminders: bake items should reopen the relevant bake context, while starter actions should remain fast but not bypass the broader route model accidentally. Keep the action-first behavior intact and avoid introducing a second navigation language that users only see on the Today screen.</action>
  <verify>xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived CODE_SIGNING_ALLOWED=NO build</verify>
  <done>Today actions and notification taps feel like two entry points into the same operational workflow rather than unrelated behaviors.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived CODE_SIGNING_ALLOWED=NO build`
- [ ] `xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived -destination 'platform=iOS Simulator,name=iPhone 17 Pro' test -only-testing:LevainTests/AppRouterTests`
- [ ] Notification taps reopen the related bake or starter context through one shared routing path
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 5 closes with Today, reminders, and deep-link routing working as one coherent operational system
  </success_criteria>

<output>
After completion, create `.planning/phases/05-today-notifications/05-03-SUMMARY.md`
</output>
