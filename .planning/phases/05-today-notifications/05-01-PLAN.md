---
phase: 05-today-notifications
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Levain/Features/Today/TodayView.swift
  - Levain/Features/Today/TodayStepCardView.swift
  - Levain/Features/Today/TodayStarterReminderRow.swift
  - Levain/Services/TodayAgendaBuilder.swift
  - LevainTests/TodayAgendaBuilderTests.swift
  - LevainTests/TestSupport/DomainFixtures.swift
  - Levain.xcodeproj/project.pbxproj
autonomous: true
user_setup: []
must_haves:
  truths:
    - Today prioritizes running and overdue bake work ahead of later-today items, starter reminders, and tomorrow preview items.
    - Each Today item exposes a clear title, timing context, derived state label, and one obvious primary action instead of a generic list row.
    - Bake-step work and starter reminders appear together in one operational view without turning Today into a passive dashboard.
  artifacts:
    - Levain/Features/Today/TodayView.swift
    - Levain/Services/TodayAgendaBuilder.swift
    - LevainTests/TodayAgendaBuilderTests.swift
  key_links:
    - TodayView renders sections from TodayAgendaBuilder instead of rebuilding priority logic inline.
    - TodayStepCardView and TodayStarterReminderRow present the two Today item families with distinct but consistent action-first UI.
    - TodayAgendaBuilderTests verify ordering, section assignment, and derived labels for the agenda output.
---

<objective>
Turn Today into the app's operational home screen by making prioritization, row structure, and empty-state behavior align with the product promise.

Purpose: Phase 5 starts with Today because the user should immediately understand what needs attention now instead of interpreting raw bake lists.
Output: Refined agenda grouping, dedicated Today row components, and focused tests around prioritization logic.
</objective>

<execution_context>
@/Users/lorentso/.codex/skills/gsd-workflow/references/get-shit-done/workflows/execute-plan.md
@/Users/lorentso/.codex/skills/gsd-workflow/references/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-today-notifications/05-CONTEXT.md
@docs/levain-prd-complete-v2.md
@docs/UX-SPEC.md
@Levain/Features/Today/TodayView.swift
@Levain/Services/TodayAgendaBuilder.swift
@Levain/Models/Bake.swift
@Levain/Models/BakeStep.swift
@Levain/Models/Starter.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tighten Today agenda grouping around the product priority order</name>
  <files>Levain/Services/TodayAgendaBuilder.swift, LevainTests/TodayAgendaBuilderTests.swift, LevainTests/TestSupport/DomainFixtures.swift, Levain.xcodeproj/project.pbxproj</files>
  <action>Refine the agenda builder so it explicitly models the Today priority stack from the PRD: now or overdue first, then later-today bake work, then due starters, then tomorrow or later preview. Keep overdue and running status derived from persisted bake state instead of inventing another state machine, and cover the ordering rules with narrow deterministic tests rather than relying on visual inspection alone.</action>
  <verify>xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived -destination 'platform=iOS Simulator,name=iPhone 17 Pro' test -only-testing:LevainTests/TodayAgendaBuilderTests</verify>
  <done>The agenda output is deterministic, product-prioritized, and covered by focused tests for the highest-risk sorting and labeling rules.</done>
</task>

<task type="auto">
  <name>Task 2: Extract action-first Today row components for steps and starter reminders</name>
  <files>Levain/Features/Today/TodayView.swift, Levain/Features/Today/TodayStepCardView.swift, Levain/Features/Today/TodayStarterReminderRow.swift, Levain.xcodeproj/project.pbxproj</files>
  <action>Split the Today presentation into dedicated components for bake-step cards and starter reminder rows so the root view stops carrying both layout and behavior details. Match the UX spec by keeping one primary action per item, readable timing context, and a lightweight tomorrow preview instead of a giant undifferentiated backlog list.</action>
  <verify>xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived CODE_SIGNING_ALLOWED=NO build</verify>
  <done>Today rows live in dedicated files, the root screen reads as an operational queue, and starter reminders no longer masquerade as generic bake cards.</done>
</task>

<task type="auto">
  <name>Task 3: Align the Today hero and empty state with operational intent</name>
  <files>Levain/Features/Today/TodayView.swift, Levain/Services/TodayAgendaBuilder.swift</files>
  <action>Use the agenda output to drive the hero copy, section visibility, and empty state so Today always answers “what now?” in one glance. Avoid dashboard metrics that compete with the action list; the point is to surface the next meaningful action, not summarize every entity in the database.</action>
  <verify>Manual review of Today confirms the hero references the top-priority work when present, sections collapse when empty, and the empty state points the user toward the next useful action.</verify>
  <done>Today feels like a to-do board for fermentation rather than a static overview with incidental buttons.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived CODE_SIGNING_ALLOWED=NO build`
- [ ] `xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived -destination 'platform=iOS Simulator,name=iPhone 17 Pro' test -only-testing:LevainTests/TodayAgendaBuilderTests`
- [ ] Today displays prioritized sections with one primary action per item and a clean empty state
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- TODAY-01 through TODAY-03 have a stable UI and agenda foundation for the rest of Phase 5
  </success_criteria>

<output>
After completion, create `.planning/phases/05-today-notifications/05-01-SUMMARY.md`
</output>
