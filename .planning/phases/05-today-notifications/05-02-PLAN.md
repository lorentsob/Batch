---
phase: 05-today-notifications
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Levain/Services/NotificationService.swift
  - Levain/Services/BakeReminderPlanner.swift
  - Levain/Features/Bakes/BakeCreationView.swift
  - Levain/Features/Bakes/BakeDetailView.swift
  - Levain/Features/Bakes/ShiftTimelineView.swift
  - LevainTests/BakeReminderPlannerTests.swift
  - LevainTests/TestSupport/DomainFixtures.swift
  - Levain.xcodeproj/project.pbxproj
autonomous: true
user_setup: []
must_haves:
  truths:
    - Upcoming bake-step reminders are scheduled from persisted bake data rather than ephemeral in-app timer state.
    - Reminder resync happens when a bake is created, when step execution changes, and when the remaining timeline shifts.
    - Reminder payloads remain stable enough that future deep-link work can trust bake and step identifiers.
  artifacts:
    - Levain/Services/NotificationService.swift
    - Levain/Services/BakeReminderPlanner.swift
    - LevainTests/BakeReminderPlannerTests.swift
  key_links:
    - BakeCreationView, BakeDetailView, and ShiftTimelineView call NotificationService through a consistent bake-sync path.
    - NotificationService uses BakeReminderPlanner to derive reminder fire dates, identifiers, and route payloads.
    - BakeReminderPlannerTests verify reminder scheduling invariants independently of UNUserNotificationCenter runtime behavior.
---

<objective>
Make bake reminders reliable with the app closed by treating notifications as a derived scheduling layer on top of persisted bake state.

Purpose: The app cannot be operational day to day unless reminders stay aligned with the actual bake timeline after real user actions.
Output: Deterministic bake reminder planning, unified resync hooks, and focused tests around reminder timing semantics.
</objective>

<execution_context>
@/Users/lorentso/.codex/skills/gsd-workflow/references/get-shit-done/workflows/execute-plan.md
@/Users/lorentso/.codex/skills/gsd-workflow/references/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-today-notifications/05-CONTEXT.md
@docs/levain-prd-complete-v2.md
@docs/UX-SPEC.md
@Levain/Services/NotificationService.swift
@Levain/Features/Bakes/BakeCreationView.swift
@Levain/Features/Bakes/BakeDetailView.swift
@Levain/Features/Bakes/ShiftTimelineView.swift
@Levain/Models/Bake.swift
@Levain/Models/BakeStep.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract deterministic bake reminder planning out of NotificationService</name>
  <files>Levain/Services/NotificationService.swift, Levain/Services/BakeReminderPlanner.swift, LevainTests/BakeReminderPlannerTests.swift, LevainTests/TestSupport/DomainFixtures.swift, Levain.xcodeproj/project.pbxproj</files>
  <action>Move the bake reminder derivation logic into a pure planning helper so reminder fire dates, identifiers, and route payloads can be verified without depending on the notification center runtime. Keep the helper scoped to bake-step reminders for this phase; starter-specific reminder orchestration remains a later Phase 6 concern even though the service already knows about starters.</action>
  <verify>xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived -destination 'platform=iOS Simulator,name=iPhone 17 Pro' test -only-testing:LevainTests/BakeReminderPlannerTests</verify>
  <done>Bake reminder scheduling semantics are explicit, testable, and no longer hidden inside side-effect-heavy notification code.</done>
</task>

<task type="auto">
  <name>Task 2: Normalize reminder resync across bake lifecycle mutations</name>
  <files>Levain/Services/NotificationService.swift, Levain/Features/Bakes/BakeCreationView.swift, Levain/Features/Bakes/BakeDetailView.swift, Levain/Features/Bakes/ShiftTimelineView.swift</files>
  <action>Review every phase-4 bake mutation point and ensure reminder resync happens consistently after creation, start, completion, skip, and timeline shifting. Avoid duplicating ad hoc scheduling logic in the views; the goal is a single reminder sync entry point that keeps notification state aligned with the persisted bake timeline.</action>
  <verify>Manual code-path review confirms every bake mutation funnels through a consistent notification sync call and no stale inline scheduling remains in feature views.</verify>
  <done>Notification updates are tied to real bake mutations instead of being partially handled in scattered UI actions.</done>
</task>

<task type="auto">
  <name>Task 3: Preserve notification behavior that matches the product rule for timers</name>
  <files>Levain/Services/BakeReminderPlanner.swift, Levain/Services/NotificationService.swift, Levain/Features/Bakes/BakeDetailView.swift</files>
  <action>Keep the distinction between timers and notifications sharp: in-app timers remain advisory, while the reminder planner only schedules local notifications from persisted step times and reminder offsets. Do not let running-step timer visuals quietly become the source of reminder truth; reliability with the app closed must come from persisted data plus scheduled local notifications.</action>
  <verify>xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived CODE_SIGNING_ALLOWED=NO build</verify>
  <done>NOTIF-01 and NOTIF-02 are implemented in a way that respects the product's closed-app reliability model.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived CODE_SIGNING_ALLOWED=NO build`
- [ ] `xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived -destination 'platform=iOS Simulator,name=iPhone 17 Pro' test -only-testing:LevainTests/BakeReminderPlannerTests`
- [ ] Bake reminder scheduling and resync are derived from persisted bake data across create, execute, and shift flows
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- NOTIF-01 and NOTIF-02 have a deterministic implementation the next deep-link plan can build on
  </success_criteria>

<output>
After completion, create `.planning/phases/05-today-notifications/05-02-SUMMARY.md`
</output>
