# Phase 3 Context

Phase 3 turns `RecipeFormula` into the reusable planning object. It needs practical form UX, editable ordered step templates, and enough baker's-math coherence to drive bake creation without overcomplicating the model.

