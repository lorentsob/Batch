---
phase: 03-formula-authoring
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - Levain/Features/Bakes/FormulaEditorView.swift
  - Levain/Features/Bakes/FormulaStepEditorView.swift
  - Levain/Models/FormulaStepTemplate.swift
  - Levain/Models/RecipeFormula.swift
  - Levain.xcodeproj/project.pbxproj
autonomous: true
user_setup: []
must_haves:
  truths:
    - Users can add, edit, delete, and reorder default step templates inside a formula.
    - The ordered step-template payload saved on RecipeFormula stays stable enough for BakeScheduler to consume later without reinterpretation.
    - Advanced qualitative step fields remain editable but do not dominate the main formula form.
  artifacts:
    - Levain/Features/Bakes/FormulaEditorView.swift
    - Levain/Features/Bakes/FormulaStepEditorView.swift
    - Levain/Models/FormulaStepTemplate.swift
  key_links:
    - FormulaEditorView owns the ordered array of FormulaStepTemplate values.
    - FormulaStepEditorView edits one template at a time and writes back through FormulaEditorView.
    - RecipeFormula.defaultSteps persists the ordered template payload used by BakeScheduler.
---

<objective>
Build a practical step-template editing flow so each formula owns an editable ordered set of default steps instead of a static baked-in list.

Purpose: Formula reuse only matters if the default step structure can be maintained without friction.
Output: Ordered step-template management in the formula editor plus a dedicated step template sheet for timing and qualitative fields.
</objective>

<execution_context>
@/Users/lorentso/.codex/skills/gsd-workflow/references/get-shit-done/workflows/execute-plan.md
@/Users/lorentso/.codex/skills/gsd-workflow/references/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-formula-authoring/03-CONTEXT.md
@docs/levain-prd-complete-v2.md
@docs/UX-SPEC.md
@Levain/Features/Bakes/FormulaEditorView.swift
@Levain/Models/FormulaStepTemplate.swift
@Levain/Models/RecipeFormula.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make the default-step list fully editable and ordered</name>
  <files>Levain/Features/Bakes/FormulaEditorView.swift, Levain/Models/FormulaStepTemplate.swift, Levain/Models/RecipeFormula.swift</files>
  <action>Refine the formula editor so the step-template list behaves like a first-class ordered collection: add, reorder, and delete should feel deliberate and preserve stable template identity. Keep the interaction model simple and sheet-based rather than inventing a more complex drag-and-drop builder.</action>
  <verify>Confirm the formula editor can add a new step, move it within the ordered list, and delete an existing step without corrupting the saved array.</verify>
  <done>Default steps behave like an intentional ordered sequence, and the saved order remains the same order later used for schedule generation.</done>
</task>

<task type="auto">
  <name>Task 2: Flesh out the step-template editing sheet</name>
  <files>Levain/Features/Bakes/FormulaStepEditorView.swift, Levain/Features/Bakes/FormulaEditorView.swift, Levain/Models/FormulaStepTemplate.swift, Levain.xcodeproj/project.pbxproj</files>
  <action>Give each step template its own editor surface for name, type, duration, reminder offset, and qualitative targets such as temperature and dough volume. Keep advanced fields available but secondary so the main formula flow stays short and practical, consistent with the PRD's minimal UX direction.</action>
  <verify>xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived CODE_SIGNING_ALLOWED=NO build</verify>
  <done>The sheet can edit all needed step-template fields, save them back into the formula editor, and still feel lightweight enough for repeated use.</done>
</task>

<task type="auto">
  <name>Task 3: Preserve clean persistence semantics for step templates</name>
  <files>Levain/Features/Bakes/FormulaEditorView.swift, Levain/Models/RecipeFormula.swift, Levain/Models/FormulaStepTemplate.swift</files>
  <action>Review how edited step templates are written back to RecipeFormula so the payload remains deterministic and compatible with the Phase 2 scheduler. Avoid hidden mutations that would reorder templates, strip metadata, or silently fall back to default steps when the user has explicitly customized them.</action>
  <verify>xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived CODE_SIGNING_ALLOWED=NO build</verify>
  <done>Custom step-template arrays persist cleanly, preserve metadata, and remain ready for later bake generation without additional transformation logic.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived CODE_SIGNING_ALLOWED=NO build`
- [ ] Formula editor supports add, edit, delete, and reorder for default step templates
- [ ] Saved step-template ordering and metadata remain intact on RecipeFormula
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Formula step-template editing is complete enough that generated bakes can trust the saved defaults
  </success_criteria>

<output>
After completion, create `.planning/phases/03-formula-authoring/03-02-SUMMARY.md`
</output>
