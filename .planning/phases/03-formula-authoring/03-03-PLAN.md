---
phase: 03-formula-authoring
plan: 03
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified:
  - Levain/Features/Bakes/BakesView.swift
  - Levain/Features/Bakes/FormulaDetailView.swift
  - Levain/Features/Bakes/FormulaEditorView.swift
  - Levain/Models/RecipeFormula.swift
  - LevainTests/RecipeFormulaTests.swift
  - LevainTests/TestSupport/DomainFixtures.swift
  - Levain.xcodeproj/project.pbxproj
autonomous: true
user_setup: []
must_haves:
  truths:
    - Users can duplicate a saved formula and reuse its defaults without rebuilding the step list from scratch.
    - Formula editing guards against invalid or incoherent baker's-math inputs while keeping derived hydration, salt, inoculation, and dough totals legible.
    - Formula persistence and math helpers are backed by focused automated tests, not only manual simulator checks.
  artifacts:
    - Levain/Features/Bakes/FormulaEditorView.swift
    - Levain/Features/Bakes/FormulaDetailView.swift
    - Levain/Models/RecipeFormula.swift
    - LevainTests/RecipeFormulaTests.swift
  key_links:
    - BakesView and FormulaDetailView expose formula duplication entry points.
    - RecipeFormula provides the derived metrics and duplication semantics consumed by the formula editor and detail views.
    - RecipeFormulaTests verifies formula calculations and persisted default-step behavior.
---

<objective>
Close Phase 3 by adding duplication, formula validation, and persistence confidence around the reusable formula workflow.

Purpose: Formula authoring is not finished until saved formulas can be safely reused, copied, and trusted to produce coherent bake inputs.
Output: Duplication flow, tighter validation and derived summaries, and focused tests for formula calculations and persistence.
</objective>

<execution_context>
@/Users/lorentso/.codex/skills/gsd-workflow/references/get-shit-done/workflows/execute-plan.md
@/Users/lorentso/.codex/skills/gsd-workflow/references/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-formula-authoring/03-CONTEXT.md
@.planning/phases/02-domain-scheduling/02-03-SUMMARY.md
@docs/levain-prd-complete-v2.md
@docs/UX-SPEC.md
@Levain/Features/Bakes/BakesView.swift
@Levain/Features/Bakes/FormulaDetailView.swift
@Levain/Features/Bakes/FormulaEditorView.swift
@Levain/Models/RecipeFormula.swift
@LevainTests/BakeSchedulerTests.swift
@LevainTests/TestSupport/DomainFixtures.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add formula duplication and reuse entry points</name>
  <files>Levain/Features/Bakes/BakesView.swift, Levain/Features/Bakes/FormulaDetailView.swift, Levain/Features/Bakes/FormulaEditorView.swift, Levain/Models/RecipeFormula.swift</files>
  <action>Implement a duplication path for saved formulas so the user can start from an existing formula and keep its default steps without manually rebuilding them. The duplicate flow should create a clean new formula identity while preserving the reusable structure the user intended to copy.</action>
  <verify>Confirm a saved formula can be duplicated from the formula list or detail flow into a new editable formula with copied values and default steps.</verify>
  <done>Formula duplication exists as an intentional workflow and satisfies the reuse requirement without forcing the user to rebuild defaults from scratch.</done>
</task>

<task type="auto">
  <name>Task 2: Tighten formula validation and derived baker's-math presentation</name>
  <files>Levain/Features/Bakes/FormulaEditorView.swift, Levain/Features/Bakes/FormulaDetailView.swift, Levain/Models/RecipeFormula.swift</files>
  <action>Improve the formula editor and detail views so derived hydration, salt percentage, inoculation, and dough totals stay legible and coherent as the user edits values. Guard against empty names and invalid numeric combinations, but avoid turning the form into a heavy validation maze; the goal is fast confidence, not enterprise-grade form bureaucracy.</action>
  <verify>xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived CODE_SIGNING_ALLOWED=NO build</verify>
  <done>Users can see whether a formula is coherent before saving, and the persisted formula values expose stable derived outputs for later bake creation.</done>
</task>

<task type="auto">
  <name>Task 3: Add focused automated coverage for formula persistence and math</name>
  <files>LevainTests/RecipeFormulaTests.swift, LevainTests/TestSupport/DomainFixtures.swift, Levain/Models/RecipeFormula.swift, Levain.xcodeproj/project.pbxproj</files>
  <action>Add narrow unit tests covering formula derived ratios, total dough calculation, default-step payload round-tripping, and duplication semantics. Reuse the Phase 2 test support patterns instead of inventing new infrastructure, and keep the suite deterministic with fixed values rather than wall-clock assumptions.</action>
  <verify>xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived -destination 'platform=iOS Simulator,name=iPhone 17 Pro' test -only-testing:LevainTests/RecipeFormulaTests</verify>
  <done>Formula calculations and persistence semantics are covered by a focused test target and no longer depend only on manual UI checks.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived CODE_SIGNING_ALLOWED=NO build`
- [ ] `xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived -destination 'platform=iOS Simulator,name=iPhone 17 Pro' test -only-testing:LevainTests/RecipeFormulaTests`
- [ ] Formula flows support create, edit, duplicate, and reusable default steps with coherent derived math
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 3 covers FORM-01 through FORM-04 with real UI flows and focused persistence confidence
  </success_criteria>

<output>
After completion, create `.planning/phases/03-formula-authoring/03-03-SUMMARY.md`
</output>
