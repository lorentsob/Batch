---
phase: 03-formula-authoring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Levain/Features/Bakes/BakesView.swift
  - Levain/Features/Bakes/FormulaDetailView.swift
  - Levain/Features/Bakes/FormulaEditorView.swift
  - Levain.xcodeproj/project.pbxproj
autonomous: true
user_setup: []
must_haves:
  truths:
    - The Bakes area exposes saved formulas as a first-class reusable planning object, not just an incidental side panel.
    - Users can create a new formula, open an existing one, and edit it locally without leaving the existing Bakes feature boundary.
    - Formula detail presents the key baker's-math outputs and the ordered default-step summary clearly enough to drive later bake creation.
  artifacts:
    - Levain/Features/Bakes/BakesView.swift
    - Levain/Features/Bakes/FormulaDetailView.swift
    - Levain/Features/Bakes/FormulaEditorView.swift
  key_links:
    - BakesView routes formula list interactions into FormulaDetailView and FormulaEditorView.
    - FormulaEditorView writes directly to RecipeFormula in SwiftData.
    - FormulaDetailView reflects RecipeFormula derived metrics and defaultSteps ordering.
---

<objective>
Turn the existing formula scaffolding into a coherent authoring flow with dedicated list, detail, and editor surfaces inside the Bakes feature.

Purpose: Phase 3 should make `RecipeFormula` feel like the reusable source object for later bake creation instead of leaving authoring logic buried inside one large file.
Output: Separated formula views, clearer CRUD entry points, and a durable navigation shape for formula management.
</objective>

<execution_context>
@/Users/lorentso/.codex/skills/gsd-workflow/references/get-shit-done/workflows/execute-plan.md
@/Users/lorentso/.codex/skills/gsd-workflow/references/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-formula-authoring/03-CONTEXT.md
@.planning/phases/02-domain-scheduling/02-03-SUMMARY.md
@docs/levain-prd-complete-v2.md
@docs/UX-SPEC.md
@Levain/Features/Bakes/BakesView.swift
@Levain/Models/RecipeFormula.swift
@Levain/Models/FormulaStepTemplate.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract formula authoring views out of the monolithic Bakes screen</name>
  <files>Levain/Features/Bakes/BakesView.swift, Levain/Features/Bakes/FormulaDetailView.swift, Levain/Features/Bakes/FormulaEditorView.swift, Levain.xcodeproj/project.pbxproj</files>
  <action>Split the formula list, detail, and editor flows into dedicated SwiftUI files so formula authoring is easier to evolve than the current everything-in-`BakesView.swift` approach. Keep the Bakes tab as the feature boundary, but stop hiding formula behavior in the same file as bake execution views.</action>
  <verify>xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived CODE_SIGNING_ALLOWED=NO build</verify>
  <done>Formula-related screens live in dedicated files, the project builds cleanly, and BakesView becomes a thinner orchestration surface.</done>
</task>

<task type="auto">
  <name>Task 2: Make formula CRUD entry points obvious in list and detail flows</name>
  <files>Levain/Features/Bakes/BakesView.swift, Levain/Features/Bakes/FormulaDetailView.swift, Levain/Features/Bakes/FormulaEditorView.swift</files>
  <action>Refine the formula section so create, open, and edit actions are discoverable from the list and detail screens. Preserve the current Italian copy and lightweight sheet-based editing model, but make sure empty states, context menus, and toolbar actions support the short practical flow described in the PRD.</action>
  <verify>Review the Bakes tab flow: empty state -> new formula, existing formula row -> detail, detail -> edit sheet.</verify>
  <done>The formula flow reads as a complete CRUD path and no longer depends on hidden incidental buttons or overloaded list cards.</done>
</task>

<task type="auto">
  <name>Task 3: Present core formula information without crowding the list screen</name>
  <files>Levain/Features/Bakes/FormulaDetailView.swift, Levain/Features/Bakes/BakesView.swift</files>
  <action>Keep the list screen lightweight while using the detail screen to surface the formula's core values, derived baker's-math stats, and ordered default-step preview. Avoid overloading the list with advanced fields; those belong in detail and edit flows per the project conventions.</action>
  <verify>xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived CODE_SIGNING_ALLOWED=NO build</verify>
  <done>Formula rows stay scannable, the detail screen carries the useful stats and preview, and the UI remains consistent with the minimal operational tone of the app.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived CODE_SIGNING_ALLOWED=NO build`
- [ ] Formula list, detail, and editor routes are distinct and compile from the Bakes tab flow
- [ ] Formula detail shows derived stats plus ordered default-step preview without bloating the list screen
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Formula CRUD has a stable screen structure that later Phase 4 bake creation can depend on
  </success_criteria>

<output>
After completion, create `.planning/phases/03-formula-authoring/03-01-SUMMARY.md`
</output>
