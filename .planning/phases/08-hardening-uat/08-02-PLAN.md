---
phase: 08-hardening-uat
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - Levain/Models/AppSettings.swift
  - Levain/Persistence/SeedDataLoader.swift
  - Levain/Features/Shared/RootTabView.swift
  - Levain/Features/Shared/EmptyStateView.swift
  - Levain/Features/Today/TodayView.swift
  - Levain/Features/Bakes/BakesView.swift
  - Levain/Features/Starter/StarterView.swift
  - Levain/Features/Knowledge/KnowledgeView.swift
  - LevainTests/SeedDataLoaderTests.swift
  - Levain.xcodeproj/project.pbxproj
autonomous: true
user_setup: []
must_haves:
  truths:
    - First launch is useful without forcing sample data or a heavyweight onboarding flow.
    - Sample seed data remains available for internal testing, but only through deliberate bootstrap or explicit action instead of an unconditional automatic insert.
    - Empty states across Today and the core tabs point the user toward the next useful action in line with the PRD.
  artifacts:
    - Levain/Persistence/SeedDataLoader.swift
    - Levain/Features/Today/TodayView.swift
    - Levain/Features/Bakes/BakesView.swift
    - LevainTests/SeedDataLoaderTests.swift
  key_links:
    - RootTabView no longer couples normal first launch to automatic sample-data insertion.
    - Today empty state exposes the practical next actions from the PRD: new bake, add starter, and low-emphasis tip exploration.
    - SeedDataLoader remains idempotent and can still populate realistic sample content for internal review when explicitly requested.
---

<objective>
Make the app feel useful on first launch while preserving one-tap sample data support for internal testing and demos.

Purpose: QUAL-04 is not met if the app always seeds data before the user sees empty states, because that hides whether the actual first-run experience is clear and actionable.
Output: Deliberate first-launch empty states, explicit sample-data entry, and tests around seed behavior.
</objective>

<execution_context>
@/Users/lorentso/.codex/skills/gsd-workflow/references/get-shit-done/workflows/execute-plan.md
@/Users/lorentso/.codex/skills/gsd-workflow/references/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-hardening-uat/08-CONTEXT.md
@.planning/REQUIREMENTS.md
@docs/levain-prd-complete-v2.md
@docs/UX-SPEC.md
@Levain/Persistence/SeedDataLoader.swift
@Levain/Features/Shared/RootTabView.swift
@Levain/Features/Today/TodayView.swift
@Levain/Features/Bakes/BakesView.swift
@Levain/Features/Starter/StarterView.swift
@Levain/Features/Knowledge/KnowledgeView.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Decouple first launch from automatic sample-data seeding</name>
  <files>Levain/Models/AppSettings.swift, Levain/Persistence/SeedDataLoader.swift, Levain/Features/Shared/RootTabView.swift</files>
  <action>Change bootstrap semantics so a normal first launch does not immediately inject demo content before the user sees the product's empty-state guidance. Keep the internal-testing path fast by allowing deliberate sample-data seeding through a controlled action or launch mode, and ensure the loader remains idempotent when triggered multiple times.</action>
  <verify>Manual code-path review confirms normal first launch stays empty while explicit sample-data bootstrap still populates the expected internal dataset.</verify>
  <done>First launch and internal sample seeding become separate, intentional behaviors instead of one coupled bootstrap path.</done>
</task>

<task type="auto">
  <name>Task 2: Align Today and tab empty states with the product's immediate-action promise</name>
  <files>Levain/Features/Shared/EmptyStateView.swift, Levain/Features/Today/TodayView.swift, Levain/Features/Bakes/BakesView.swift, Levain/Features/Starter/StarterView.swift, Levain/Features/Knowledge/KnowledgeView.swift</files>
  <action>Refine the empty states so the app answers “what now?” even with zero user data. Prioritize the Today empty state around the PRD actions for first launch, keep tab-specific copy short and practical, and avoid turning empty screens into onboarding essays that slow down the user.</action>
  <verify>Manual review confirms first launch surfaces `New bake`, `Add starter`, and `Explore tips` paths clearly, while Bakes, Starter, and Knowledge remain concise and action-first when empty.</verify>
  <done>QUAL-04 is visible in the real UI through useful empty states instead of being implied by documentation alone.</done>
</task>

<task type="auto">
  <name>Task 3: Add focused tests around seed and first-launch behavior</name>
  <files>LevainTests/SeedDataLoaderTests.swift, Levain/Persistence/SeedDataLoader.swift, Levain.xcodeproj/project.pbxproj</files>
  <action>Add narrow tests that prove the sample-data path is explicit, idempotent, and coherent with the app's first-launch rules. Keep these tests close to the seed loader and bootstrap contract rather than expanding into broad persistence integration cases that belong to full-phase verification.</action>
  <verify>xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived -destination 'platform=iOS Simulator,name=iPhone 17 Pro' test -only-testing:LevainTests/SeedDataLoaderTests</verify>
  <done>The first-launch and sample-seed contract is regression-tested instead of depending on manual simulator resets.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived CODE_SIGNING_ALLOWED=NO build`
- [ ] `xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived -destination 'platform=iOS Simulator,name=iPhone 17 Pro' test -only-testing:LevainTests/SeedDataLoaderTests`
- [ ] First launch shows practical empty-state actions while sample data remains available as an explicit internal-testing path
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Empty states and internal sample seed behavior align with QUAL-04 and the PRD first-launch guidance
  </success_criteria>

<output>
After completion, create `.planning/phases/08-hardening-uat/08-02-SUMMARY.md`
</output>
