---
phase: 08-hardening-uat
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Levain/App/AppLaunchOptions.swift
  - Levain/App/LevainApp.swift
  - Levain/Persistence/ModelContainerFactory.swift
  - Levain/Features/Shared/RootTabView.swift
  - Levain/Features/Today/TodayView.swift
  - Levain/Features/Bakes/BakesView.swift
  - Levain/Features/Starter/StarterView.swift
  - Levain/Features/Knowledge/KnowledgeView.swift
  - LevainUITests/LevainUITests.swift
  - LevainUITests/TodayFlowUITests.swift
  - LevainUITests/BakesFlowUITests.swift
  - LevainUITests/KnowledgeFlowUITests.swift
  - Levain.xcodeproj/project.pbxproj
autonomous: true
user_setup: []
must_haves:
  truths:
    - Core user journeys have baseline UI coverage that runs deterministically instead of depending on persisted simulator state, random sample seeding, or permission prompts.
    - UI test launches can explicitly choose empty or seeded app state and suppress notification side effects that would otherwise make the suite flaky.
    - The launch harness stays internal and lightweight, supporting confidence work without turning the app into a debug-settings project.
  artifacts:
    - Levain/App/AppLaunchOptions.swift
    - Levain/Features/Shared/RootTabView.swift
    - LevainUITests/LevainUITests.swift
    - LevainUITests/TodayFlowUITests.swift
  key_links:
    - App launch configuration controls store mode, sample-data mode, and notification suppression for UI test scenarios.
    - RootTabView and the tab roots expose stable hooks the UI suite can target without coupling to fragile visual copy alone.
    - LevainUITests cover the highest-risk shell and operational entry flows rather than remaining a single launch smoke test.
---

<objective>
Establish a deterministic UI test foundation so Phase 8 can raise confidence with repeatable automation instead of depending on manual simulator cleanup and one-off smoke checks.

Purpose: QUAL-03 is not credible until the app can launch into known states on demand and the UI suite covers more than the tab bar.
Output: Launch-time test harness, stable UI-test entry points, and baseline UI coverage for the core shell and operational flows.
</objective>

<execution_context>
@/Users/lorentso/.codex/skills/gsd-workflow/references/get-shit-done/workflows/execute-plan.md
@/Users/lorentso/.codex/skills/gsd-workflow/references/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-hardening-uat/08-CONTEXT.md
@.planning/REQUIREMENTS.md
@docs/levain-prd-complete-v2.md
@Levain/App/LevainApp.swift
@Levain/Persistence/ModelContainerFactory.swift
@Levain/Features/Shared/RootTabView.swift
@LevainUITests/LevainUITests.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a deterministic app launch harness for UI and confidence testing</name>
  <files>Levain/App/AppLaunchOptions.swift, Levain/App/LevainApp.swift, Levain/Persistence/ModelContainerFactory.swift, Levain/Features/Shared/RootTabView.swift</files>
  <action>Introduce explicit launch options so the app can boot into known empty or seeded states, use an isolated store for tests, and skip notification authorization or resync side effects when automation needs control. Keep the harness intentionally narrow and internal: it should exist to make confidence work deterministic, not to create a second runtime configuration system for end users.</action>
  <verify>Manual code-path review confirms UI tests can request empty or seeded launch behavior without inheriting stale simulator data or triggering permission prompts.</verify>
  <done>UI automation has a controlled launch surface instead of relying on incidental app bootstrap behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Expose stable UI-test hooks on the highest-value screens</name>
  <files>Levain/Features/Today/TodayView.swift, Levain/Features/Bakes/BakesView.swift, Levain/Features/Starter/StarterView.swift, Levain/Features/Knowledge/KnowledgeView.swift, Levain/Features/Shared/RootTabView.swift</files>
  <action>Give the root operational screens stable UI-test anchors so the suite is not forced to depend on brittle structure or incidental text placement. Keep this focused on the screens and actions that matter for MVP confidence: the shell, Today, Bakes, Starter, and Knowledge entry paths.</action>
  <verify>Manual inspection shows the UI suite can locate root content and primary actions on each tab through deliberate, stable hooks.</verify>
  <done>The app exposes enough stable surface area for repeatable UI testing without turning accessibility identifiers into noise everywhere.</done>
</task>

<task type="auto">
  <name>Task 3: Expand baseline UI coverage to the core shell and operational entry flows</name>
  <files>LevainUITests/LevainUITests.swift, LevainUITests/TodayFlowUITests.swift, LevainUITests/BakesFlowUITests.swift, LevainUITests/KnowledgeFlowUITests.swift, Levain.xcodeproj/project.pbxproj</files>
  <action>Replace the current single launch smoke test with a small but meaningful UI suite that covers app launch, the first operational Today path, key Bakes entry behavior, and lightweight Knowledge access. Do not chase exhaustive UI coverage; the goal is confidence on the highest-risk user journeys the MVP depends on.</action>
  <verify>xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived -destination 'platform=iOS Simulator,name=iPhone 17 Pro' test -only-testing:LevainUITests</verify>
  <done>QUAL-03 has a real baseline UI suite that exercises the app beyond a tab-bar existence check.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived CODE_SIGNING_ALLOWED=NO build`
- [ ] `xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived -destination 'platform=iOS Simulator,name=iPhone 17 Pro' test -only-testing:LevainUITests`
- [ ] UI tests can launch the app in deterministic empty and seeded modes without permission-prompt flakiness
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 8 starts with a deterministic UI confidence baseline rather than ad hoc smoke testing
  </success_criteria>

<output>
After completion, create `.planning/phases/08-hardening-uat/08-01-SUMMARY.md`
</output>
