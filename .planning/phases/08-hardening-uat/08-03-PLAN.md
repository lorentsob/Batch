---
phase: 08-hardening-uat
plan: 03
type: execute
wave: 3
depends_on: ["08-01", "08-02"]
files_modified:
  - Levain/App/AppRouter.swift
  - Levain/Services/NotificationService.swift
  - Levain/Features/Shared/RootTabView.swift
  - Levain/Persistence/ModelContainerFactory.swift
  - LevainTests/AppRouterTests.swift
  - LevainUITests/LifecycleUITests.swift
  - LevainUITests/NotificationRouteUITests.swift
  - Levain.xcodeproj/project.pbxproj
autonomous: true
user_setup: []
must_haves:
  truths:
    - The app survives relaunch, cold-launch deep links, and foreground notification-entry paths without diverging into separate routing behavior.
    - Persistence and bootstrap behavior remain stable across repeated launches once sample-data and test harness logic are made explicit.
    - Release-readiness verification ends with concrete evidence and clear residual risks instead of a hand-wavy “seems fine” signoff.
  artifacts:
    - Levain/Features/Shared/RootTabView.swift
    - Levain/Services/NotificationService.swift
    - LevainTests/AppRouterTests.swift
    - LevainUITests/LifecycleUITests.swift
  key_links:
    - NotificationService pending routes and ordinary openURL events still converge on one routing path through RootTabView and AppRouter.
    - Lifecycle UI tests use the deterministic launch harness from 08-01 to verify relaunch and route-handling behavior without simulator state leakage.
    - Final build and full test execution provide a real release-readiness baseline for continued internal testing.
---

<objective>
Close the roadmap with lifecycle hardening and end-to-end confidence checks that match how the app will actually be used during internal testing.

Purpose: Phase 8 is complete only when persistence, relaunch, and notification-entry behavior have been verified under controlled conditions, not just compiled successfully.
Output: Lifecycle polish where needed, focused routing regression coverage, and a final release-readiness test pass.
</objective>

<execution_context>
@/Users/lorentso/.codex/skills/gsd-workflow/references/get-shit-done/workflows/execute-plan.md
@/Users/lorentso/.codex/skills/gsd-workflow/references/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-hardening-uat/08-CONTEXT.md
@.planning/REQUIREMENTS.md
@docs/levain-prd-complete-v2.md
@Levain/App/AppRouter.swift
@Levain/Services/NotificationService.swift
@Levain/Features/Shared/RootTabView.swift
@LevainTests/AppRouterTests.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden lifecycle and route handoff across relaunch and notification entry</name>
  <files>Levain/Features/Shared/RootTabView.swift, Levain/App/AppRouter.swift, Levain/Services/NotificationService.swift, Levain/Persistence/ModelContainerFactory.swift</files>
  <action>Review and tighten the bootstrap, route handoff, and transient notification state so cold launch, warm resume, and ordinary deep-link opening all follow the same routing contract. Preserve the product rule that notification taps reopen the related operational context, and do not reintroduce parallel navigation paths while hardening lifecycle behavior.</action>
  <verify>Manual code-path review confirms `pendingURL`, `onOpenURL`, and relaunch behavior still converge on one routing path with stable entity lookup fallbacks.</verify>
  <done>Lifecycle and route behavior stay coherent under the same real-world entry conditions internal testing will use.</done>
</task>

<task type="auto">
  <name>Task 2: Add targeted regression coverage for relaunch and route handling</name>
  <files>LevainTests/AppRouterTests.swift, LevainUITests/LifecycleUITests.swift, LevainUITests/NotificationRouteUITests.swift, Levain.xcodeproj/project.pbxproj</files>
  <action>Extend regression coverage around deep-link parsing, launch-state routing, and relaunch smoke behavior using the deterministic harness introduced earlier in the phase. Keep the suite narrow and confidence-oriented: the purpose is to prove the critical lifecycle paths are stable, not to simulate every notification-center detail in automation.</action>
  <verify>xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived -destination 'platform=iOS Simulator,name=iPhone 17 Pro' test</verify>
  <done>The highest-risk lifecycle and route regressions are covered by focused automation instead of relying entirely on manual re-checks.</done>
</task>

<task type="auto">
  <name>Task 3: Finish with a release-readiness verification pass and explicit residual risks</name>
  <files>Levain/Features/Shared/RootTabView.swift, Levain/Services/NotificationService.swift, LevainUITests/LifecycleUITests.swift</files>
  <action>Run the final build and test pass for the scheme, then verify the remaining manual points that automation cannot fully guarantee, especially notification behavior with the app foregrounded or relaunched. Record any residual gaps explicitly in the summary instead of burying them, so continued internal testing starts from a truthful confidence baseline.</action>
  <verify>Manual simulator or device review confirms the app relaunches cleanly, missing-entity routes fail safely, and notification-entry behavior remains aligned with the shared router model.</verify>
  <done>Phase 8 ends with a concrete release-readiness baseline and a clear statement of any remaining non-automated risks.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived CODE_SIGNING_ALLOWED=NO build`
- [ ] `xcodebuild -project Levain.xcodeproj -scheme Levain -derivedDataPath /tmp/LevainDerived -destination 'platform=iOS Simulator,name=iPhone 17 Pro' test`
- [ ] Relaunch, deep-link, and notification-entry paths are verified against one shared routing model with any residual risks called out explicitly
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 8 closes with a real internal-testing readiness baseline rather than only roadmap completion
  </success_criteria>

<output>
After completion, create `.planning/phases/08-hardening-uat/08-03-SUMMARY.md`
</output>
